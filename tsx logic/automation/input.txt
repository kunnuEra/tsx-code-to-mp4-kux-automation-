```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene11_AadharBindu',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_AadharBindu: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera zoom in
  const cameraScale = interpolate(frame, [0, durationInFrames], [1, 1.06], {
    extrapolateRight: 'clamp',
  });
  const cameraPanX = interpolate(frame, [0, durationInFrames], [0, -15], {
    extrapolateRight: 'clamp',
  });

  // Background gradient shift
  const gradAngle = interpolate(frame, [0, durationInFrames], [135, 165], {
    extrapolateRight: 'clamp',
  });

  // Pulsing number reveal
  const numberScale = sp(30, { damping: 8, stiffness: 80 });
  const numberGlow = interpolate(
    frame,
    [30, 50, 70, 90],
    [0, 1, 0.6, 1],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );
  const numberPulse = Math.sin(frame * 0.08) * 4;

  // Magnifying glass rotation
  const magRotate = interpolate(frame, [0, durationInFrames], [-10, 10], {
    extrapolateRight: 'clamp',
  });

  // Running man silhouette (people running away)
  const runnerX = interpolate(frame, [60, 180], [400, -300], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const runner2X = interpolate(frame, [75, 200], [500, -400], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Screen shake on "भागते हैं"
  const shakeX =
    frame > 160 && frame < 185
      ? Math.sin(frame * 1.5) * 3 * (1 - (frame - 160) / 25)
      : 0;
  const shakeY =
    frame > 160 && frame < 185
      ? Math.cos(frame * 2) * 2 * (1 - (frame - 160) / 25)
      : 0;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${cameraScale}) translate(${cameraPanX + shakeX}px, ${shakeY}px)`,
        }}
      >
        {/* Dynamic Gradient Background */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0a0a1a 0%, #1a0a2e 30%, #0d1b2a 60%, #1C1C1C 100%)`,
          }}
        />

        {/* Animated Shape Layers */}
        {[...Array(6)].map((_, i) => {
          const seed = seededRandom(i + 111);
          const size = 150 + seed * 350;
          const startX = seed * 1920;
          const startY = seededRandom(i + 222) * 1080;
          const drift = interpolate(
            frame,
            [0, durationInFrames],
            [0, 40 + seed * 60],
            { extrapolateRight: 'clamp' }
          );
          const rot = interpolate(
            frame,
            [0, durationInFrames],
            [0, 20 + seed * 30],
            { extrapolateRight: 'clamp' }
          );
          const breathe = 1 + Math.sin(frame * 0.02 + i) * 0.05;
          const shapes = ['circle', 'polygon', 'rect'];
          const shapeType = shapes[i % 3];

          return (
            <div
              key={`shape-${i}`}
              style={{
                position: 'absolute',
                left: startX + drift * (i % 2 === 0 ? 1 : -1),
                top: startY + Math.sin(frame * 0.01 + i) * 20,
                width: size,
                height: size,
                transform: `rotate(${rot}deg) scale(${breathe})`,
                opacity: 0.04 + seed * 0.04,
              }}
            >
              <svg width={size} height={size} viewBox={`0 0 ${size} ${size}`}>
                {shapeType === 'circle' && (
                  <circle
                    cx={size / 2}
                    cy={size / 2}
                    r={size / 2 - 2}
                    fill="none"
                    stroke="#e9d700"
                    strokeWidth="1.5"
                  />
                )}
                {shapeType === 'polygon' && (
                  <polygon
                    points={`${size / 2},5 ${size - 5},${size - 5} 5,${size - 5}`}
                    fill="none"
                    stroke="#FFC107"
                    strokeWidth="1.5"
                  />
                )}
                {shapeType === 'rect' && (
                  <rect
                    x="3"
                    y="3"
                    width={size - 6}
                    height={size - 6}
                    fill="none"
                    stroke="#9E9E9E"
                    strokeWidth="1"
                    rx="8"
                  />
                )}
              </svg>
            </div>
          );
        })}

        {/* Motion Texture — Concentric Rings */}
        <AbsoluteFill style={{ opacity: 0.06 }}>
          {[...Array(8)].map((_, i) => {
            const ringSize = 200 + i * 120;
            const ringExpand = 1 + Math.sin(frame * 0.015 + i * 0.5) * 0.08;
            return (
              <div
                key={`ring-${i}`}
                style={{
                  position: 'absolute',
                  left: '50%',
                  top: '50%',
                  width: ringSize,
                  height: ringSize,
                  marginLeft: -ringSize / 2,
                  marginTop: -ringSize / 2,
                  borderRadius: '50%',
                  border: '1px solid #e9d700',
                  transform: `scale(${ringExpand})`,
                }}
              />
            );
          })}
        </AbsoluteFill>

        {/* Floating Particles */}
        {[...Array(30)].map((_, i) => {
          const s = seededRandom(i + 500);
          const pSize = 2 + s * 4;
          const px = s * 1920;
          const py = seededRandom(i + 600) * 1080;
          const speed = 0.3 + s * 0.7;
          const yOff = (frame * speed) % 1080;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px + Math.sin(frame * 0.02 + i) * 15,
                top: (py - yOff + 1080) % 1080,
                width: pSize,
                height: pSize,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#e9d700' : '#F5F5F5',
                opacity: 0.15 + s * 0.2,
              }}
            />
          );
        })}

        {/* Main Content Area */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'row',
            alignItems: 'center',
            justifyContent: 'center',
            padding: '60px 100px',
          }}
        >
          {/* Left Side — Giant Number with Magnifying Glass */}
          <div
            style={{
              flex: '0 0 45%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
            }}
          >
            {/* Glow behind number */}
            <div
              style={{
                position: 'absolute',
                width: 400,
                height: 400,
                borderRadius: '50%',
                background: `radial-gradient(circle, rgba(233,215,0,${0.15 * numberGlow}) 0%, transparent 70%)`,
                transform: `scale(${numberScale})`,
              }}
            />

            {/* The Number — आधार बिंदु */}
            <div
              style={{
                transform: `scale(${numberScale}) translateY(${numberPulse}px)`,
                textAlign: 'center',
                position: 'relative',
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 220,
                  fontWeight: 900,
                  color: '#e9d700',
                  lineHeight: 1,
                  textShadow: `0 0 ${30 * numberGlow}px rgba(233,215,0,0.4), 0 0 ${60 * numberGlow}px rgba(233,215,0,0.2)`,
                }}
              >
                ₹
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 42,
                  color: '#9E9E9E',
                  marginTop: 10,
                  letterSpacing: 6,
                  opacity: sp(50),
                }}
              >
                BASE POINT
              </div>
            </div>

            {/* Magnifying Glass SVG */}
            <div
              style={{
                position: 'absolute',
                right: -20,
                top: -30,
                transform: `rotate(${magRotate}deg) scale(${sp(20)})`,
                opacity: sp(20),
              }}
            >
              <svg
                width="160"
                height="160"
                viewBox="0 0 100 100"
                fill="none"
              >
                <circle
                  cx="42"
                  cy="42"
                  r="28"
                  stroke="#FFC107"
                  strokeWidth="4"
                  fill="rgba(255,193,7,0.05)"
                />
                <line
                  x1="62"
                  y1="62"
                  x2="88"
                  y2="88"
                  stroke="#FFC107"
                  strokeWidth="5"
                  strokeLinecap="round"
                />
              </svg>
            </div>
          </div>

          {/* Right Side — Text Content */}
          <div
            style={{
              flex: '0 0 50%',
              paddingLeft: 60,
            }}
          >
            {/* Hindi Headline */}
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 90,
                color: '#F5F5F5',
                lineHeight: 1.2,
                transform: `translateX(${interpolate(sp(10), [0, 1], [80, 0])}px)`,
                opacity: sp(10),
              }}
            >
              आधार{' '}
              <span style={{ color: '#e9d700' }}>बिंदु</span>
            </div>

            {/* Subtext */}
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                color: '#9E9E9E',
                marginTop: 25,
                lineHeight: 1.6,
                transform: `translateX(${interpolate(sp(25), [0, 1], [60, 0])}px)`,
                opacity: sp(25),
              }}
            >
              वो एक अंक जो बताता है
              <br />
              तुम्हारी असली स्थिति क्या है
            </div>

            {/* Divider line */}
            <div
              style={{
                width: interpolate(sp(40), [0, 1], [0, 320]),
                height: 3,
                backgroundColor: '#e9d700',
                marginTop: 30,
                borderRadius: 2,
              }}
            />

            {/* Running away text */}
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 34,
                color: '#ff0000',
                marginTop: 25,
                opacity: sp(70),
                transform: `translateY(${interpolate(sp(70), [0, 1], [30, 0])}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 15,
              }}
            >
              <svg width="30" height="30" viewBox="0 0 24 24" fill="#ff0000">
                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" />
              </svg>
              लोग इस हिसाब से भागते हैं!
            </div>
          </div>
        </AbsoluteFill>

        {/* Running Stick Figures (people fleeing) */}
        {/* Runner 1 */}
        <div
          style={{
            position: 'absolute',
            bottom: 120,
            left: runnerX,
            opacity: interpolate(
              frame,
              [60, 80, 170, 185],
              [0, 0.4, 0.4, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          <svg width="80" height="120" viewBox="0 0 80 120">
            {/* Running stick figure */}
            <circle cx="40" cy="15" r="12" fill="#9E9E9E" />
            <line
              x1="40"
              y1="27"
              x2="40"
              y2="65"
              stroke="#9E9E9E"
              strokeWidth="3"
            />
            {/* Arms swinging */}
            <line
              x1="40"
              y1="40"
              x2={40 + Math.sin(frame * 0.3) * 20}
              y2={50 + Math.cos(frame * 0.3) * 10}
              stroke="#9E9E9E"
              strokeWidth="2.5"
            />
            <line
              x1="40"
              y1="40"
              x2={40 - Math.sin(frame * 0.3) * 20}
              y2={50 - Math.cos(frame * 0.3) * 10}
              stroke="#9E9E9E"
              strokeWidth="2.5"
            />
            {/* Legs running */}
            <line
              x1="40"
              y1="65"
              x2={40 + Math.sin(frame * 0.3) * 18}
              y2={95 + Math.cos(frame * 0.3) * 5}
              stroke="#9E9E9E"
              strokeWidth="2.5"
            />
            <line
              x1="40"
              y1="65"
              x2={40 - Math.sin(frame * 0.3) * 18}
              y2={95 - Math.cos(frame * 0.3) * 5}
              stroke="#9E9E9E"
              strokeWidth="2.5"
            />
          </svg>
        </div>

        {/* Runner 2 */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: runner2X,
            opacity: interpolate(
              frame,
              [75, 95, 185, 200],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
            transform: 'scale(0.7)',
          }}
        >
          <svg width="80" height="120" viewBox="0 0 80 120">
            <circle cx="40" cy="15" r="12" fill="#9E9E9E" />
            <line
              x1="40"
              y1="27"
              x2="40"
              y2="65"
              stroke="#9E9E9E"
              strokeWidth="3"
            />
            <line
              x1="40"
              y1="40"
              x2={40 + Math.sin(frame * 0.35) * 20}
              y2={50 + Math.cos(frame * 0.35) * 10}
              stroke="#9E9E9E"
              strokeWidth="2.5"
            />
            <line
              x1="40"
              y1="40"
              x2={40 - Math.sin(frame * 0.35) * 20}
              y2={50 - Math.cos(frame * 0.35) * 10}
              stroke="#9E9E9E"
              strokeWidth="2.5"
            />
            <line
              x1="40"
              y1="65"
              x2={40 + Math.sin(frame * 0.35) * 18}
              y2={95 + Math.cos(frame * 0.35) * 5}
              stroke="#9E9E9E"
              strokeWidth="2.5"
            />
            <line
              x1="40"
              y1="65"
              x2={40 - Math.sin(frame * 0.35) * 18}
              y2={95 - Math.cos(frame * 0.35) * 5}
              stroke="#9E9E9E"
              strokeWidth="2.5"
            />
          </svg>
        </div>

        {/* Buffett Stick Figure — observing calmly */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            right: 180,
            transform: `scale(${sp(45)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
            opacity: sp(45),
          }}
        >
          <svg width="100" height="160" viewBox="0 0 100 160">
            {/* Golden glow */}
            <circle cx="50" cy="30" r="35" fill="rgba(233,215,0,0.08)" />
            {/* Head */}
            <circle cx="50" cy="30" r="18" fill="none" stroke="#FFC107" strokeWidth="3" />
            {/* Glasses */}
            <circle cx="42" cy="28" r="7" fill="none" stroke="#FFC107" strokeWidth="2" />
            <circle cx="58" cy="28" r="7" fill="none" stroke="#FFC107" strokeWidth="2" />
            <line x1="49" y1="28" x2="51" y2="28" stroke="#FFC107" strokeWidth="1.5" />
            {/* Eyes */}
            <circle cx="42" cy="28" r="2" fill="#FFC107" />
            <circle cx="58" cy="28" r="2" fill="#FFC107" />
            {/* Subtle smile */}
            <path d="M 43 35 Q 50 40 57 35" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            {/* Body */}
            <line x1="50" y1="48" x2="50" y2="100" stroke="#FFC107" strokeWidth="3" />
            {/* Arms — crossed/thinking */}
            <line x1="50" y1="65" x2="30" y2="80" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="30" y1="80" x2="45" y2="75" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="50" y1="65" x2="70" y2="80" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="70" y1="80" x2="55" y2="75" stroke="#FFC107" strokeWidth="2.5" />
            {/* Legs */}
            <line x1="50" y1="100" x2="35" y2="140" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="50" y1="100" x2="65" y2="140" stroke="#FFC107" strokeWidth="2.5" />
            {/* Head nod */}
            <animateTransform
              attributeName="transform"
              type="rotate"
              values="0 50 30;2 50 30;0 50 30;-1 50 30;0 50 30"
              dur="3s"
              repeatCount="indefinite"
            />
          </svg>
        </div>

        {/* Vignette Overlay */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_AadharBindu;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene12_AnkhBandKarke',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene12_AnkhBandKarke: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow pan right
  const camX = interpolate(frame, [0, durationInFrames], [10, -20], {
    extrapolateRight: 'clamp',
  });
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1], {
    extrapolateRight: 'clamp',
  });

  // Gradient shift — dark teal to deep grey
  const gradHue = interpolate(frame, [0, durationInFrames], [200, 220], {
    extrapolateRight: 'clamp',
  });

  // Blindfold animation over eyes
  const blindfoldWidth = interpolate(sp(40), [0, 1], [0, 220]);

  // Bill items falling in
  const bills = [
    { label: 'किराया', icon: '🏠', delay: 50, color: '#ff0000' },
    { label: 'बिजली', icon: '⚡', delay: 70, color: '#FFC107' },
    { label: 'राशन', icon: '🛒', delay: 90, color: '#e9d700' },
    { label: 'फीस', icon: '📚', delay: 110, color: '#ff0000' },
    { label: 'आना-जाना', icon: '🚌', delay: 130, color: '#FFC107' },
  ];

  // Question mark floating
  const qScale = 1 + Math.sin(frame * 0.06) * 0.08;
  const qRotate = Math.sin(frame * 0.04) * 8;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camScale}) translateX(${camX}px)`,
        }}
      >
        {/* Dynamic Gradient Background */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(155deg, hsl(${gradHue}, 30%, 8%) 0%, #1C1C1C 40%, #0d1117 70%, hsl(${gradHue + 20}, 20%, 6%) 100%)`,
          }}
        />

        {/* Animated Shape Layers — Hexagons */}
        {[...Array(5)].map((_, i) => {
          const seed = seededRandom(i + 811);
          const size = 120 + seed * 280;
          const posX = seed * 1800;
          const posY = seededRandom(i + 812) * 1000;
          const drift = interpolate(
            frame,
            [0, durationInFrames],
            [0, 30 + seed * 40],
            { extrapolateRight: 'clamp' }
          );
          const rot = interpolate(
            frame,
            [0, durationInFrames],
            [0, 15 + seed * 25],
            { extrapolateRight: 'clamp' }
          );
          const breathe = 1 + Math.sin(frame * 0.018 + i * 2) * 0.06;

          // Hexagon points
          const hexPoints = Array.from({ length: 6 }, (_, j) => {
            const angle = (Math.PI / 3) * j - Math.PI / 6;
            const r = size / 2 - 4;
            return `${size / 2 + r * Math.cos(angle)},${size / 2 + r * Math.sin(angle)}`;
          }).join(' ');

          return (
            <div
              key={`hex-${i}`}
              style={{
                position: 'absolute',
                left: posX + drift * (i % 2 === 0 ? 1 : -1),
                top: posY + Math.sin(frame * 0.012 + i) * 15,
                width: size,
                height: size,
                transform: `rotate(${rot}deg) scale(${breathe})`,
                opacity: 0.04 + seed * 0.03,
              }}
            >
              <svg width={size} height={size}>
                <polygon
                  points={hexPoints}
                  fill="none"
                  stroke={i % 2 === 0 ? '#e9d700' : '#9E9E9E'}
                  strokeWidth="1.2"
                />
              </svg>
            </div>
          );
        })}

        {/* Motion Texture — Dotted Grid */}
        <AbsoluteFill style={{ opacity: 0.04 }}>
          {[...Array(12)].map((_, row) =>
            [...Array(20)].map((_, col) => {
              const dotPulse =
                1 +
                Math.sin(frame * 0.03 + row * 0.5 + col * 0.3) * 0.5;
              return (
                <div
                  key={`dot-${row}-${col}`}
                  style={{
                    position: 'absolute',
                    left: col * 100 + 10,
                    top: row * 95 + 10,
                    width: 3 * dotPulse,
                    height: 3 * dotPulse,
                    borderRadius: '50%',
                    backgroundColor: '#e9d700',
                  }}
                />
              );
            })
          )}
        </AbsoluteFill>

        {/* Floating Particles */}
        {[...Array(25)].map((_, i) => {
          const s = seededRandom(i + 900);
          const pSize = 2 + s * 3;
          const px = s * 1920;
          const py = seededRandom(i + 901) * 1080;
          const speed = 0.4 + s * 0.6;
          const yOff = (frame * speed) % 1080;
          return (
            <div
              key={`fp-${i}`}
              style={{
                position: 'absolute',
                left: px + Math.sin(frame * 0.025 + i) * 12,
                top: (py + yOff) % 1080,
                width: pSize,
                height: pSize,
                borderRadius: '50%',
                backgroundColor: i % 4 === 0 ? '#FFC107' : '#F5F5F5',
                opacity: 0.1 + s * 0.15,
              }}
            />
          );
        })}

        {/* Main Content — Split Layout */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'row',
            alignItems: 'center',
            padding: '60px 80px',
          }}
        >
          {/* Left Side — Stick Figure with Blindfold */}
          <div
            style={{
              flex: '0 0 40%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
            }}
          >
            <div
              style={{
                transform: `scale(${sp(15)}) translateY(${Math.sin(frame * 0.05) * 3}px)`,
                opacity: sp(15),
                position: 'relative',
              }}
            >
              <svg width="200" height="320" viewBox="0 0 200 320">
                {/* Soft glow */}
                <circle cx="100" cy="55" r="60" fill="rgba(158,158,158,0.06)" />
                {/* Head */}
                <circle
                  cx="100"
                  cy="55"
                  r="32"
                  fill="none"
                  stroke="#F5F5F5"
                  strokeWidth="3.5"
                />
                {/* Eyes — open but confused */}
                <circle cx="88" cy="50" r="4" fill="#F5F5F5" />
                <circle cx="112" cy="50" r="4" fill="#F5F5F5" />
                {/* Confused eyebrows */}
                <line
                  x1="80"
                  y1="40"
                  x2="92"
                  y2="43"
                  stroke="#F5F5F5"
                  strokeWidth="2"
                />
                <line
                  x1="120"
                  y1="43"
                  x2="108"
                  y2="40"
                  stroke="#F5F5F5"
                  strokeWidth="2"
                />
                {/* Mouth — uncertain */}
                <path
                  d="M 88 68 Q 100 63 112 68"
                  fill="none"
                  stroke="#F5F5F5"
                  strokeWidth="2"
                />
                {/* Body */}
                <line
                  x1="100"
                  y1="87"
                  x2="100"
                  y2="190"
                  stroke="#F5F5F5"
                  strokeWidth="3.5"
                />
                {/* Arms — shrugging */}
                <line
                  x1="100"
                  y1="120"
                  x2="55"
                  y2={105 + Math.sin(frame * 0.08) * 8}
                  stroke="#F5F5F5"
                  strokeWidth="3"
                />
                <line
                  x1="100"
                  y1="120"
                  x2="145"
                  y2={105 + Math.sin(frame * 0.08 + 1) * 8}
                  stroke="#F5F5F5"
                  strokeWidth="3"
                />
                {/* Palms up — shrug gesture */}
                <line
                  x1="55"
                  y1={105 + Math.sin(frame * 0.08) * 8}
                  x2="50"
                  y2={95 + Math.sin(frame * 0.08) * 8}
                  stroke="#F5F5F5"
                  strokeWidth="2.5"
                />
                <line
                  x1="145"
                  y1={105 + Math.sin(frame * 0.08 + 1) * 8}
                  x2="150"
                  y2={95 + Math.sin(frame * 0.08 + 1) * 8}
                  stroke="#F5F5F5"
                  strokeWidth="2.5"
                />
                {/* Legs */}
                <line
                  x1="100"
                  y1="190"
                  x2="70"
                  y2="280"
                  stroke="#F5F5F5"
                  strokeWidth="3"
                />
                <line
                  x1="100"
                  y1="190"
                  x2="130"
                  y2="280"
                  stroke="#F5F5F5"
                  strokeWidth="3"
                />
              </svg>

              {/* Blindfold over eyes */}
              <div
                style={{
                  position: 'absolute',
                  top: 32,
                  left: 100 - blindfoldWidth / 2,
                  width: blindfoldWidth,
                  height: 28,
                  backgroundColor: '#ff0000',
                  borderRadius: 6,
                  opacity: 0.85,
                }}
              />

              {/* Question marks floating around */}
              {[0, 1, 2].map((q) => {
                const qOp = sp(60 + q * 20);
                const qY = Math.sin(frame * 0.06 + q * 2) * 15;
                const positions = [
                  { x: -40, y: -20 },
                  { x: 160, y: -10 },
                  { x: 60, y: -50 },
                ];
                return (
                  <div
                    key={`q-${q}`}
                    style={{
                      position: 'absolute',
                      left: positions[q].x,
                      top: positions[q].y + qY,
                      fontFamily: 'Inter',
                      fontSize: 38 + q * 6,
                      fontWeight: 900,
                      color: '#e9d700',
                      opacity: qOp * 0.7,
                      transform: `rotate(${Math.sin(frame * 0.04 + q) * 12}deg)`,
                    }}
                  >
                    ?
                  </div>
                );
              })}
            </div>
          </div>

          {/* Right Side — Falling Bill Items */}
          <div
            style={{
              flex: '0 0 55%',
              paddingLeft: 40,
              position: 'relative',
            }}
          >
            {/* Title */}
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 72,
                color: '#F5F5F5',
                lineHeight: 1.2,
                marginBottom: 35,
                opacity: sp(8),
                transform: `translateY(${interpolate(sp(8), [0, 1], [40, 0])}px)`,
              }}
            >
              <span style={{ color: '#ff0000' }}>जोड़</span> नहीं
              लगाते
            </div>

            {/* Bill items as cards */}
            <div
              style={{
                display: 'flex',
                flexDirection: 'column',
                gap: 14,
              }}
            >
              {bills.map((bill, i) => {
                const cardSp = sp(bill.delay, { damping: 10, stiffness: 100 });
                const slideX = interpolate(cardSp, [0, 1], [300, 0]);
                const cardOp = cardSp;

                return (
                  <div
                    key={`bill-${i}`}
                    style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: 18,
                      backgroundColor: 'rgba(45,45,45,0.7)',
                      borderLeft: `4px solid ${bill.color}`,
                      borderRadius: 10,
                      padding: '14px 28px',
                      transform: `translateX(${slideX}px)`,
                      opacity: cardOp,
                      backdropFilter: 'blur(4px)',
                    }}
                  >
                    {/* SVG Icon instead of emoji */}
                    <svg
                      width="36"
                      height="36"
                      viewBox="0 0 24 24"
                      fill={bill.color}
                    >
                      {i === 0 && (
                        <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z" />
                      )}
                      {i === 1 && (
                        <path d="M7 2v11h3v9l7-12h-4l4-8z" />
                      )}
                      {i === 2 && (
                        <path d="M7 18c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.6L5.2 14c-.1.3-.2.7-.2 1 0 1.1.9 2 2 2h12v-2H7.4l.6-1h7.5c.7 0 1.4-.4 1.7-1l3.6-6.5c.2-.3-.1-.5-.4-.5H5.2L4.3 4H1zm16 16c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2z" />
                      )}
                      {i === 3 && (
                        <path d="M5 13.18v4L12 21l7-3.82v-4L12 17l-7-3.82zM12 3L1 9l11 6 9-4.91V17h2V9L12 3z" />
                      )}
                      {i === 4 && (
                        <path d="M4 16c0 .88.39 1.67 1 2.22V20c0 .55.45 1 1 1h1c.55 0 1-.45 1-1v-1h8v1c0 .55.45 1 1 1h1c.55 0 1-.45 1-1v-1.78c.61-.55 1-1.34 1-2.22V6c0-3.5-3.58-4-8-4S4 2.5 4 6v10zm3.5 1c-.83 0-1.5-.67-1.5-1.5S6.67 14 7.5 14s1.5.67 1.5 1.5S8.33 17 7.5 17zm9 0c-.83 0-1.5-.67-1.5-1.5s.67-1.5 1.5-1.5 1.5.67 1.5 1.5-.67 1.5-1.5 1.5zm1.5-6H6V6h12v5z" />
                      )}
                    </svg>
                    <span
                      style={{
                        fontFamily: 'Noto Sans Devanagari',
                        fontSize: 36,
                        fontWeight: 600,
                        color: '#F5F5F5',
                      }}
                    >
                      {bill.label}
                    </span>
                    <span
                      style={{
                        fontFamily: 'JetBrains Mono',
                        fontSize: 28,
                        color: '#9E9E9E',
                        marginLeft: 'auto',
                      }}
                    >
                      ₹ ???
                    </span>
                  </div>
                );
              })}
            </div>

            {/* Bottom tag — "सब संभल जाएगा" */}
            <div
              style={{
                marginTop: 30,
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 34,
                color: '#e9d700',
                fontStyle: 'italic',
                opacity: sp(160),
                transform: `translateY(${interpolate(sp(160), [0, 1], [20, 0])}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 12,
              }}
            >
              "कहीं न कहीं से सब संभल जाएगा"
              <span
                style={{
                  fontSize: 30,
                  color: '#ff0000',
                  fontWeight: 800,
                  fontStyle: 'normal',
                }}
              >
                ← ग़लत!
              </span>
            </div>
          </div>
        </AbsoluteFill>

        {/* Vignette Overlay */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene12_AnkhBandKarke;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene13_GanitVsBhavna',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene13_GanitVsBhavna: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera orbit effect
  const camRotateY = interpolate(frame, [0, durationInFrames], [-2, 2], {
    extrapolateRight: 'clamp',
  });
  const camScale = interpolate(frame, [0, 80, durationInFrames], [0.96, 1.02, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // VS slam effect
  const vsSp = sp(60, { damping: 6, stiffness: 200, mass: 0.8 });
  const vsShake =
    frame > 60 && frame < 75
      ? Math.sin(frame * 3) * 4 * (1 - (frame - 60) / 15)
      : 0;

  // Scale balance animation
  const balanceTilt = interpolate(frame, [80, 140], [15, -20], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Equation reveal
  const eqOpacity = sp(180);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camScale}) perspective(1200px) rotateY(${camRotateY}deg)`,
        }}
      >
        {/* Dynamic Gradient Background — warm vs cold split */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(90deg, rgba(255,0,0,0.08) 0%, #1C1C1C 50%, rgba(233,215,0,0.08) 100%)`,
          }}
        />
        <AbsoluteFill
          style={{
            background: `linear-gradient(170deg, #0a0a14 0%, transparent 50%, #140a0a 100%)`,
          }}
        />

        {/* Shape Layers — Diamonds */}
        {[...Array(7)].map((_, i) => {
          const seed = seededRandom(i + 1300);
          const size = 100 + seed * 250;
          const posX = seed * 1920;
          const posY = seededRandom(i + 1301) * 1080;
          const drift = Math.sin(frame * 0.008 + i) * 30;
          const rot = 45 + interpolate(frame, [0, durationInFrames], [0, 20 + seed * 15], {
            extrapolateRight: 'clamp',
          });
          const breathe = 1 + Math.sin(frame * 0.02 + i * 1.5) * 0.04;

          return (
            <div
              key={`dia-${i}`}
              style={{
                position: 'absolute',
                left: posX + drift,
                top: posY,
                width: size,
                height: size,
                transform: `rotate(${rot}deg) scale(${breathe})`,
                opacity: 0.04 + seed * 0.03,
                border: `1px solid ${i % 2 === 0 ? '#ff0000' : '#e9d700'}`,
                borderRadius: 4,
              }}
            />
          );
        })}

        {/* Motion Texture — Math symbols floating */}
        <AbsoluteFill style={{ opacity: 0.05 }}>
          {['÷', '×', '+', '−', '=', '%', 'Σ', 'π', '√'].map((sym, i) => {
            const x = seededRandom(i + 1350) * 1920;
            const y = seededRandom(i + 1351) * 1080;
            const drift = Math.sin(frame * 0.015 + i * 0.8) * 25;
            const rot = Math.sin(frame * 0.01 + i) * 20;
            return (
              <div
                key={`sym-${i}`}
                style={{
                  position: 'absolute',
                  left: x + drift,
                  top: y,
                  fontFamily: 'JetBrains Mono',
                  fontSize: 50 + seededRandom(i + 1352) * 40,
                  color: '#e9d700',
                  transform: `rotate(${rot}deg)`,
                }}
              >
                {sym}
              </div>
            );
          })}
        </AbsoluteFill>

        {/* Floating Particles */}
        {[...Array(20)].map((_, i) => {
          const s = seededRandom(i + 1400);
          const pSize = 2 + s * 3;
          return (
            <div
              key={`p13-${i}`}
              style={{
                position: 'absolute',
                left: s * 1920 + Math.sin(frame * 0.02 + i) * 10,
                top:
                  (seededRandom(i + 1401) * 1080 + frame * (0.3 + s * 0.5)) %
                  1080,
                width: pSize,
                height: pSize,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#ff0000' : '#e9d700',
                opacity: 0.12 + s * 0.15,
              }}
            />
          );
        })}

        {/* Main Content — VS Battle Layout */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'row',
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          {/* LEFT — उमीदें / भावनाएं (Emotions) */}
          <div
            style={{
              flex: '0 0 38%',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              opacity: sp(10),
              transform: `translateX(${interpolate(sp(10), [0, 1], [-100, 0])}px)`,
            }}
          >
            {/* Heart icon — emotions */}
            <svg
              width="140"
              height="140"
              viewBox="0 0 24 24"
              style={{
                filter: 'drop-shadow(0 0 20px rgba(255,0,0,0.4))',
                transform: `scale(${1 + Math.sin(frame * 0.1) * 0.06})`,
              }}
            >
              <path
                d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"
                fill="#ff0000"
              />
            </svg>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 80,
                color: '#ff0000',
                marginTop: 20,
                textShadow: '0 0 30px rgba(255,0,0,0.3)',
              }}
            >
              उमीदें
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 32,
                color: '#9E9E9E',
                marginTop: 10,
              }}
            >
              भावनाएं • अंदाज़े
            </div>

            {/* Strikethrough animation */}
            <div
              style={{
                width: interpolate(sp(120), [0, 1], [0, 280]),
                height: 4,
                backgroundColor: '#ff0000',
                marginTop: 15,
                borderRadius: 2,
                transform: `rotate(-5deg)`,
              }}
            />
          </div>

          {/* CENTER — VS */}
          <div
            style={{
              flex: '0 0 14%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
            }}
          >
            <div
              style={{
                transform: `scale(${vsSp}) translateY(${vsShake}px)`,
                position: 'relative',
              }}
            >
              {/* Glow behind VS */}
              <div
                style={{
                  position: 'absolute',
                  width: 200,
                  height: 200,
                  left: -50,
                  top: -60,
                  borderRadius: '50%',
                  background:
                    'radial-gradient(circle, rgba(233,215,0,0.2) 0%, transparent 70%)',
                }}
              />
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 100,
                  fontWeight: 900,
                  color: '#e9d700',
                  textShadow: '0 0 40px rgba(233,215,0,0.5)',
                  letterSpacing: -4,
                }}
              >
                VS
              </div>
            </div>
          </div>

          {/* RIGHT — गणित (Math) */}
          <div
            style={{
              flex: '0 0 38%',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              opacity: sp(25),
              transform: `translateX(${interpolate(sp(25), [0, 1], [100, 0])}px)`,
            }}
          >
            {/* Calculator icon */}
            <svg
              width="130"
              height="130"
              viewBox="0 0 24 24"
              style={{
                filter: 'drop-shadow(0 0 20px rgba(233,215,0,0.4))',
              }}
            >
              <path
                d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-7 2h5v3h-5V5zm-4 9h2v2H8v-2zm0-3h2v2H8v-2zm-3 3h2v2H5v-2zm0-3h2v2H5v-2zm9 7H5v-2h9v2zm2-4h-2v-2h2v2zm0-3h-2v-2h2v2zm3 3h-2v-2h2v2zm0-3h-2v-2h2v2zm0-4H5V5h14v3z"
                fill="#e9d700"
              />
            </svg>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 80,
                color: '#e9d700',
                marginTop: 20,
                textShadow: '0 0 30px rgba(233,215,0,0.3)',
              }}
            >
              गणित
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 32,
                color: '#9E9E9E',
                marginTop: 10,
              }}
            >
              हिसाब • तथ्य
            </div>

            {/* Checkmark animation */}
            <div
              style={{
                marginTop: 15,
                opacity: sp(140),
                transform: `scale(${sp(140, { damping: 8 })})`,
              }}
            >
              <svg width="50" height="50" viewBox="0 0 24 24">
                <path
                  d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"
                  fill="#e9d700"
                  strokeWidth="1"
                />
              </svg>
            </div>
          </div>
        </AbsoluteFill>

        {/* Bottom Bar — "तो करना क्या है?" */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            opacity: eqOpacity,
            transform: `translateY(${interpolate(eqOpacity, [0, 1], [30, 0])}px)`,
          }}
        >
          <div
            style={{
              backgroundColor: 'rgba(45,45,45,0.85)',
              borderRadius: 16,
              padding: '18px 60px',
              border: '2px solid #e9d700',
              backdropFilter: 'blur(8px)',
            }}
          >
            <span
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 44,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              तो{' '}
              <span style={{ color: '#e9d700' }}>करना क्या है?</span>
            </span>
          </div>
        </div>

        {/* Buffett Character — pointing to math side */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            right: 140,
            transform: `scale(${sp(50)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
            opacity: sp(50),
          }}
        >
          <svg width="80" height="135" viewBox="0 0 80 135">
            {/* Glow */}
            <circle cx="40" cy="22" r="30" fill="rgba(233,215,0,0.07)" />
            {/* Head with nod */}
            <g
              transform={`rotate(${Math.sin(frame * 0.04) * 3}, 40, 22)`}
            >
              <circle
                cx="40"
                cy="22"
                r="14"
                fill="none"
                stroke="#FFC107"
                strokeWidth="2.5"
              />
              {/* Glasses */}
              <circle
                cx="34"
                cy="21"
                r="5.5"
                fill="none"
                stroke="#FFC107"
                strokeWidth="1.5"
              />
              <circle
                cx="46"
                cy="21"
                r="5.5"
                fill="none"
                stroke="#FFC107"
                strokeWidth="1.5"
              />
              <line
                x1="39.5"
                y1="21"
                x2="40.5"
                y2="21"
                stroke="#FFC107"
                strokeWidth="1.2"
              />
              {/* Eyes */}
              <circle cx="34" cy="21" r="1.5" fill="#FFC107" />
              <circle cx="46" cy="21" r="1.5" fill="#FFC107" />
              {/* Smile */}
              <path
                d="M 34 28 Q 40 32 46 28"
                fill="none"
                stroke="#FFC107"
                strokeWidth="1.5"
              />
            </g>
            {/* Body */}
            <line
              x1="40"
              y1="36"
              x2="40"
              y2="82"
              stroke="#FFC107"
              strokeWidth="2.5"
            />
            {/* Pointing arm */}
            <line
              x1="40"
              y1="52"
              x2="18"
              y2="45"
              stroke="#FFC107"
              strokeWidth="2"
            />
            {/* Other arm */}
            <line
              x1="40"
              y1="52"
              x2="60"
              y2="68"
              stroke="#FFC107"
              strokeWidth="2"
            />
            {/* Legs */}
            <line
              x1="40"
              y1="82"
              x2="28"
              y2="118"
              stroke="#FFC107"
              strokeWidth="2"
            />
            <line
              x1="40"
              y1="82"
              x2="52"
              y2="118"
              stroke="#FFC107"
              strokeWidth="2"
            />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene13_GanitVsBhavna;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene14_KagazUthao',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene14_KagazUthao: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera tilt down
  const camY = interpolate(frame, [0, durationInFrames], [20, -10], {
    extrapolateRight: 'clamp',
  });
  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1], {
    extrapolateRight: 'clamp',
  });

  // Notepad writing animation
  const lineProgress = (delay: number) =>
    interpolate(frame, [delay, delay + 20], [0, 1], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });

  // Expense items with staggered entrance
  const expenses = [
    { label: 'किराया / घर की किस्त', amount: '₹12,000', delay: 35, icon: 'home' },
    { label: 'राशन', amount: '₹6,000', delay: 60, icon: 'cart' },
    { label: 'बिजली / पानी', amount: '₹2,500', delay: 85, icon: 'bolt' },
    { label: 'आना-जाना', amount: '₹3,000', delay: 110, icon: 'bus' },
    { label: 'बीमा', amount: '₹1,500', delay: 135, icon: 'shield' },
    { label: 'बच्चों की पढ़ाई', amount: '₹5,000', delay: 160, icon: 'book' },
  ];

  // Total calculation reveal
  const totalSp = sp(200, { damping: 8, stiffness: 90 });

  // Pen writing motion
  const penX = interpolate(
    frame,
    [20, 60, 90, 130, 160, 200],
    [0, 100, 40, 120, 60, 140],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );
  const penY = interpolate(
    frame,
    [20, 60, 90, 130, 160, 200],
    [0, 50, 110, 170, 230, 290],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camScale}) translateY(${camY}px)`,
        }}
      >
        {/* Gradient Background — warm paper tone */}
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(160deg, #1a1712 0%, #1C1C1C 35%, #171310 70%, #1C1C1C 100%)',
          }}
        />

        {/* Shape Layers — Rounded rectangles like paper sheets */}
        {[...Array(5)].map((_, i) => {
          const seed = seededRandom(i + 1400);
          const w = 200 + seed * 300;
          const h = 150 + seed * 200;
          const posX = seed * 1700;
          const posY = seededRandom(i + 1401) * 900;
          const rot = interpolate(
            frame,
            [0, durationInFrames],
            [-5 + seed * 10, 5 + seed * 15],
            { extrapolateRight: 'clamp' }
          );
          return (
            <div
              key={`paper-${i}`}
              style={{
                position: 'absolute',
                left: posX,
                top: posY,
                width: w,
                height: h,
                border: '1px solid rgba(233,215,0,0.06)',
                borderRadius: 12,
                transform: `rotate(${rot}deg)`,
                opacity: 0.04,
              }}
            />
          );
        })}

        {/* Motion Texture — Horizontal ruled lines */}
        <AbsoluteFill style={{ opacity: 0.03 }}>
          {[...Array(25)].map((_, i) => (
            <div
              key={`line-${i}`}
              style={{
                position: 'absolute',
                left: 0,
                right: 0,
                top: 44 * i,
                height: 1,
                backgroundColor: '#e9d700',
              }}
            />
          ))}
        </AbsoluteFill>

        {/* Particles */}
        {[...Array(22)].map((_, i) => {
          const s = seededRandom(i + 1500);
          const pSize = 1.5 + s * 3;
          return (
            <div
              key={`pt-${i}`}
              style={{
                position: 'absolute',
                left: s * 1920 + Math.cos(frame * 0.018 + i) * 10,
                top:
                  (seededRandom(i + 1501) * 1080 - frame * (0.2 + s * 0.4) + 1080) %
                  1080,
                width: pSize,
                height: pSize,
                borderRadius: '50%',
                backgroundColor: i % 5 === 0 ? '#FFC107' : '#F5F5F5',
                opacity: 0.1 + s * 0.12,
              }}
            />
          );
        })}

        {/* Main Content — Notepad Layout */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'row',
            alignItems: 'center',
            padding: '50px 80px',
          }}
        >
          {/* Left — Action Title */}
          <div
            style={{
              flex: '0 0 35%',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'flex-start',
              justifyContent: 'center',
              paddingRight: 50,
            }}
          >
            {/* Pen icon */}
            <div
              style={{
                opacity: sp(5),
                transform: `scale(${sp(5)}) rotate(${-15 + Math.sin(frame * 0.03) * 3}deg)`,
                marginBottom: 20,
              }}
            >
              <svg width="80" height="80" viewBox="0 0 24 24" fill="#e9d700">
                <path d="M3 17.25V21h3.75L17.81 9.94l-3.75-3.75L3 17.25zM20.71 7.04c.39-.39.39-1.02 0-1.41l-2.34-2.34c-.39-.39-1.02-.39-1.41 0l-1.83 1.83 3.75 3.75 1.83-1.83z" />
              </svg>
            </div>

            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 78,
                color: '#F5F5F5',
                lineHeight: 1.2,
                opacity: sp(8),
                transform: `translateY(${interpolate(sp(8), [0, 1], [40, 0])}px)`,
              }}
            >
              बैठो,
              <br />
              <span style={{ color: '#e9d700' }}>लिखो</span>
            </div>

            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 32,
                color: '#9E9E9E',
                marginTop: 20,
                lineHeight: 1.6,
                opacity: sp(20),
                transform: `translateY(${interpolate(sp(20), [0, 1], [25, 0])}px)`,
              }}
            >
              एक महीने के वो खर्चे
              <br />
              जिनके बिना जिन्दगी नहीं चल सकती
            </div>

            {/* Stick Figure — writing */}
            <div
              style={{
                marginTop: 40,
                opacity: sp(30),
                transform: `translateY(${Math.sin(frame * 0.05) * 2}px)`,
              }}
            >
              <svg width="90" height="130" viewBox="0 0 90 130">
                <circle cx="45" cy="18" r="14" fill="none" stroke="#FFC107" strokeWidth="2.5" />
                {/* Glasses */}
                <circle cx="39" cy="16" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
                <circle cx="51" cy="16" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
                <line x1="44" y1="16" x2="46" y2="16" stroke="#FFC107" strokeWidth="1" />
                <circle cx="39" cy="16" r="1.5" fill="#FFC107" />
                <circle cx="51" cy="16" r="1.5" fill="#FFC107" />
                {/* Body */}
                <line x1="45" y1="32" x2="45" y2="75" stroke="#FFC107" strokeWidth="2.5" />
                {/* Writing arm — animated */}
                <line
                  x1="45"
                  y1="48"
                  x2={65 + Math.sin(frame * 0.15) * 5}
                  y2={55 + Math.cos(frame * 0.15) * 3}
                  stroke="#FFC107"
                  strokeWidth="2"
                />
                {/* Other arm on desk */}
                <line x1="45" y1="48" x2="25" y2="62" stroke="#FFC107" strokeWidth="2" />
                {/* Legs — seated */}
                <line x1="45" y1="75" x2="30" y2="110" stroke="#FFC107" strokeWidth="2" />
                <line x1="45" y1="75" x2="60" y2="110" stroke="#FFC107" strokeWidth="2" />
                {/* Desk line */}
                <line x1="15" y1="68" x2="85" y2="68" stroke="#9E9E9E" strokeWidth="1.5" />
              </svg>
            </div>
          </div>

          {/* Right — Notepad with expense list */}
          <div
            style={{
              flex: '0 0 60%',
              position: 'relative',
            }}
          >
            {/* Notepad background */}
            <div
              style={{
                backgroundColor: 'rgba(35,32,28,0.9)',
                borderRadius: 20,
                border: '2px solid rgba(233,215,0,0.15)',
                padding: '40px 50px',
                position: 'relative',
                boxShadow: '0 20px 60px rgba(0,0,0,0.4)',
                backdropFilter: 'blur(8px)',
                opacity: sp(15),
                transform: `scale(${sp(15)})`,
              }}
            >
              {/* Notepad header */}
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 28,
                  fontWeight: 700,
                  color: '#e9d700',
                  marginBottom: 8,
                  letterSpacing: 3,
                  textTransform: 'uppercase',
                }}
              >
                Monthly Essentials
              </div>
              <div
                style={{
                  width: '100%',
                  height: 2,
                  backgroundColor: '#e9d700',
                  marginBottom: 25,
                  opacity: 0.4,
                }}
              />

              {/* Expense rows */}
              {expenses.map((exp, i) => {
                const rowSp = sp(exp.delay);
                const checkSp = sp(exp.delay + 15);
                return (
                  <div
                    key={`exp-${i}`}
                    style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: 18,
                      marginBottom: 16,
                      opacity: rowSp,
                      transform: `translateX(${interpolate(rowSp, [0, 1], [60, 0])}px)`,
                    }}
                  >
                    {/* Checkbox */}
                    <div
                      style={{
                        width: 28,
                        height: 28,
                        borderRadius: 6,
                        border: '2px solid #e9d700',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        flexShrink: 0,
                      }}
                    >
                      <svg
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        style={{
                          opacity: checkSp,
                          transform: `scale(${checkSp})`,
                        }}
                      >
                        <path
                          d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"
                          fill="#e9d700"
                        />
                      </svg>
                    </div>

                    {/* Icon */}
                    <svg
                      width="30"
                      height="30"
                      viewBox="0 0 24 24"
                      fill="#FFC107"
                      style={{ flexShrink: 0 }}
                    >
                      {exp.icon === 'home' && (
                        <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z" />
                      )}
                      {exp.icon === 'cart' && (
                        <path d="M7 18c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.6L5.2 14c-.1.3-.2.7-.2 1 0 1.1.9 2 2 2h12v-2H7.4l.6-1h7.5c.7 0 1.4-.4 1.7-1l3.6-6.5c.2-.3-.1-.5-.4-.5H5.2L4.3 4H1zm16 16c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2z" />
                      )}
                      {exp.icon === 'bolt' && (
                        <path d="M7 2v11h3v9l7-12h-4l4-8z" />
                      )}
                      {exp.icon === 'bus' && (
                        <path d="M4 16c0 .88.39 1.67 1 2.22V20c0 .55.45 1 1 1h1c.55 0 1-.45 1-1v-1h8v1c0 .55.45 1 1 1h1c.55 0 1-.45 1-1v-1.78c.61-.55 1-1.34 1-2.22V6c0-3.5-3.58-4-8-4S4 2.5 4 6v10zm3.5 1c-.83 0-1.5-.67-1.5-1.5S6.67 14 7.5 14s1.5.67 1.5 1.5S8.33 17 7.5 17zm9 0c-.83 0-1.5-.67-1.5-1.5s.67-1.5 1.5-1.5 1.5.67 1.5 1.5-.67 1.5-1.5 1.5zm1.5-6H6V6h12v5z" />
                      )}
                      {exp.icon === 'shield' && (
                        <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z" />
                      )}
                      {exp.icon === 'book' && (
                        <path d="M5 13.18v4L12 21l7-3.82v-4L12 17l-7-3.82zM12 3L1 9l11 6 9-4.91V17h2V9L12 3z" />
                      )}
                    </svg>

                    {/* Label */}
                    <span
                      style={{
                        fontFamily: 'Noto Sans Devanagari',
                        fontSize: 34,
                        fontWeight: 600,
                        color: '#F5F5F5',
                        flex: 1,
                      }}
                    >
                      {exp.label}
                    </span>

                    {/* Amount */}
                    <span
                      style={{
                        fontFamily: 'JetBrains Mono',
                        fontSize: 30,
                        fontWeight: 700,
                        color: '#e9d700',
                      }}
                    >
                      {exp.amount}
                    </span>
                  </div>
                );
              })}

              {/* Total divider */}
              <div
                style={{
                  width: interpolate(totalSp, [0, 1], [0, 100]),
                  height: 3,
                  backgroundColor: '#FFC107',
                  marginTop: 20,
                  marginBottom: 15,
                  borderRadius: 2,
                  transition: 'width 0.5s',
                }}
                // width as percentage string
              />
              <div
                style={{
                  width: `${interpolate(totalSp, [0, 1], [0, 100])}%`,
                  height: 3,
                  backgroundColor: '#FFC107',
                  position: 'absolute',
                  bottom: 80,
                  left: 50,
                  borderRadius: 2,
                }}
              />

              {/* Total row */}
              <div
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'space-between',
                  opacity: totalSp,
                  transform: `translateY(${interpolate(totalSp, [0, 1], [15, 0])}px)`,
                  marginTop: 10,
                }}
              >
                <span
                  style={{
                    fontFamily: 'Yantra One',
                    fontSize: 40,
                    color: '#F5F5F5',
                  }}
                >
                  कुल जोड़
                </span>
                <span
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontSize: 42,
                    fontWeight: 900,
                    color: '#e9d700',
                    textShadow: '0 0 15px rgba(233,215,0,0.3)',
                  }}
                >
                  ₹30,000
                </span>
              </div>
            </div>
          </div>
        </AbsoluteFill>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene14_KagazUthao;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene15_BhagDo',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene15_BhagDo: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera zoom into the formula
  const camZoom = interpolate(frame, [0, 100, durationInFrames], [1, 1.08, 1.04], {
    extrapolateRight: 'clamp',
  });

  // Division animation
  const divLineSp = sp(55, { damping: 10, stiffness: 100 });
  const numeratorSp = sp(25);
  const denominatorSp = sp(40);
  const resultSp = sp(100, { damping: 8, stiffness: 80 });

  // Result percentage counter
  const resultPercent = interpolate(frame, [100, 160], [0, 60], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Pulse on result
  const resultPulse =
    frame > 160 ? 1 + Math.sin((frame - 160) * 0.1) * 0.04 : 1;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camZoom})`,
        }}
      >
        {/* Gradient Background — deep blue math feel */}
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(145deg, #0a0d1a 0%, #0f1528 30%, #1C1C1C 60%, #0d0f18 100%)',
          }}
        />

        {/* Shape Layers — Circles like pie charts */}
        {[...Array(4)].map((_, i) => {
          const seed = seededRandom(i + 1550);
          const size = 180 + seed * 300;
          const posX = seed * 1700;
          const posY = seededRandom(i + 1551) * 900;
          const rot = interpolate(frame, [0, durationInFrames], [0, 360 * seed], {
            extrapolateRight: 'clamp',
          });
          const arcEnd = 90 + seed * 200;
          const r = size / 2 - 4;
          const endAngle = (arcEnd * Math.PI) / 180;
          const largeArc = arcEnd > 180 ? 1 : 0;

          return (
            <div
              key={`arc-${i}`}
              style={{
                position: 'absolute',
                left: posX,
                top: posY,
                width: size,
                height: size,
                opacity: 0.04,
                transform: `rotate(${rot}deg)`,
              }}
            >
              <svg width={size} height={size}>
                <path
                  d={`M ${size / 2} ${size / 2} L ${size / 2} 4 A ${r} ${r} 0 ${largeArc} 1 ${size / 2 + r * Math.sin(endAngle)} ${size / 2 - r * Math.cos(endAngle)} Z`}
                  fill="none"
                  stroke="#e9d700"
                  strokeWidth="1.5"
                />
              </svg>
            </div>
          );
        })}

        {/* Motion Texture — Grid lines */}
        <AbsoluteFill style={{ opacity: 0.03 }}>
          {[...Array(10)].map((_, i) => (
            <React.Fragment key={`grid-${i}`}>
              <div
                style={{
                  position: 'absolute',
                  left: i * 192,
                  top: 0,
                  width: 1,
                  height: '100%',
                  backgroundColor: '#e9d700',
                }}
              />
              <div
                style={{
                  position: 'absolute',
                  left: 0,
                  top: i * 108,
                  width: '100%',
                  height: 1,
                  backgroundColor: '#e9d700',
                }}
              />
            </React.Fragment>
          ))}
        </AbsoluteFill>

        {/* Particles */}
        {[...Array(18)].map((_, i) => {
          const s = seededRandom(i + 1600);
          const pSize = 2 + s * 3;
          return (
            <div
              key={`p15-${i}`}
              style={{
                position: 'absolute',
                left: s * 1920 + Math.sin(frame * 0.02 + i) * 12,
                top:
                  (seededRandom(i + 1601) * 1080 + frame * (0.3 + s * 0.4)) %
                  1080,
                width: pSize,
                height: pSize,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#e9d700' : '#F5F5F5',
                opacity: 0.12 + s * 0.1,
              }}
            />
          );
        })}

        {/* Main Content — Division Formula */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          {/* Top label */}
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 36,
              color: '#9E9E9E',
              marginBottom: 50,
              opacity: sp(5),
              letterSpacing: 2,
            }}
          >
            बुनियादी सूत्र
          </div>

          {/* The Formula */}
          <div
            style={{
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              gap: 0,
            }}
          >
            {/* Numerator — जरूरी खर्चे */}
            <div
              style={{
                opacity: numeratorSp,
                transform: `translateY(${interpolate(numeratorSp, [0, 1], [-40, 0])}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 20,
              }}
            >
              <div
                style={{
                  backgroundColor: 'rgba(255,0,0,0.12)',
                  borderRadius: 14,
                  padding: '16px 40px',
                  border: '2px solid rgba(255,0,0,0.3)',
                }}
              >
                <span
                  style={{
                    fontFamily: 'Yantra One',
                    fontSize: 60,
                    color: '#ff0000',
                  }}
                >
                  जरूरी खर्चे
                </span>
              </div>
              <span
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 50,
                  color: '#9E9E9E',
                }}
              >
                =
              </span>
              <span
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 56,
                  fontWeight: 800,
                  color: '#ff0000',
                }}
              >
                ₹30,000
              </span>
            </div>

            {/* Division Line */}
            <div
              style={{
                width: interpolate(divLineSp, [0, 1], [0, 750]),
                height: 5,
                backgroundColor: '#F5F5F5',
                marginTop: 20,
                marginBottom: 20,
                borderRadius: 3,
              }}
            />

            {/* Denominator — कुल आमदनी */}
            <div
              style={{
                opacity: denominatorSp,
                transform: `translateY(${interpolate(denominatorSp, [0, 1], [40, 0])}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 20,
              }}
            >
              <div
                style={{
                  backgroundColor: 'rgba(233,215,0,0.1)',
                  borderRadius: 14,
                  padding: '16px 40px',
                  border: '2px solid rgba(233,215,0,0.3)',
                }}
              >
                <span
                  style={{
                    fontFamily: 'Yantra One',
                    fontSize: 60,
                    color: '#e9d700',
                  }}
                >
                  कुल आमदनी
                </span>
              </div>
              <span
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 50,
                  color: '#9E9E9E',
                }}
              >
                =
              </span>
              <span
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 56,
                  fontWeight: 800,
                  color: '#e9d700',
                }}
              >
                ₹50,000
              </span>
            </div>
          </div>

          {/* Equals Arrow */}
          <div
            style={{
              marginTop: 40,
              opacity: resultSp,
              transform: `scale(${resultSp * resultPulse})`,
              display: 'flex',
              alignItems: 'center',
              gap: 25,
            }}
          >
            <svg width="50" height="50" viewBox="0 0 24 24" fill="#FFC107">
              <path d="M16.01 11H4v2h12.01v3L20 12l-3.99-4z" />
            </svg>
            <div
              style={{
                backgroundColor: 'rgba(233,215,0,0.15)',
                borderRadius: 18,
                padding: '20px 50px',
                border: '3px solid #e9d700',
                boxShadow: '0 0 40px rgba(233,215,0,0.15)',
              }}
            >
              <span
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 80,
                  fontWeight: 900,
                  color: '#e9d700',
                }}
              >
                {Math.round(resultPercent)}%
              </span>
            </div>
          </div>

          {/* Bottom hint */}
          <div
            style={{
              marginTop: 30,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              color: '#9E9E9E',
              opacity: sp(180),
            }}
          >
            60% से ज्यादा है? →{' '}
            <span style={{ color: '#ff0000', fontWeight: 700 }}>
              रुको, पहले खर्चे तराशो!
            </span>
          </div>
        </AbsoluteFill>

        {/* Buffett character — bottom left */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            left: 100,
            opacity: sp(60),
            transform: `scale(${sp(60)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <svg width="75" height="125" viewBox="0 0 75 125">
            <circle cx="37" cy="20" r="28" fill="rgba(233,215,0,0.06)" />
            <g transform={`rotate(${Math.sin(frame * 0.04) * 3}, 37, 20)`}>
              <circle cx="37" cy="20" r="13" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <circle cx="31" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <circle cx="43" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <line x1="36" y1="18" x2="38" y2="18" stroke="#FFC107" strokeWidth="1" />
              <circle cx="31" cy="18" r="1.5" fill="#FFC107" />
              <circle cx="43" cy="18" r="1.5" fill="#FFC107" />
              <path d="M 31 26 Q 37 29 43 26" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            </g>
            <line x1="37" y1="33" x2="37" y2="75" stroke="#FFC107" strokeWidth="2.5" />
            {/* Pointing up */}
            <line x1="37" y1="48" x2="55" y2="35" stroke="#FFC107" strokeWidth="2" />
            <line x1="37" y1="48" x2="20" y2="60" stroke="#FFC107" strokeWidth="2" />
            <line x1="37" y1="75" x2="25" y2="108" stroke="#FFC107" strokeWidth="2" />
            <line x1="37" y1="75" x2="50" y2="108" stroke="#FFC107" strokeWidth="2" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene15_BhagDo;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene16_RukoTarasho',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene16_RukoTarasho: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Screen shake on "रुको"
  const shakeIntensity =
    frame > 20 && frame < 45
      ? (1 - (frame - 20) / 25) * 6
      : 0;
  const shakeX = Math.sin(frame * 2.5) * shakeIntensity;
  const shakeY = Math.cos(frame * 3) * shakeIntensity * 0.6;

  // Camera slow zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], {
    extrapolateRight: 'clamp',
  });

  // Red warning flash
  const warningFlash = interpolate(
    frame,
    [20, 30, 40, 50],
    [0, 0.15, 0, 0.08],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Scissors cutting animation
  const scissorsRot = interpolate(
    frame,
    [100, 120, 130, 150, 160, 180],
    [0, -15, 5, -15, 5, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );
  const scissorsScale = sp(90, { damping: 10 });

  // "60%" meter fill
  const meterFill = interpolate(frame, [60, 120], [0, 75], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Danger zone blink
  const dangerBlink = Math.sin(frame * 0.15) > 0 ? 1 : 0.6;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camScale}) translate(${shakeX}px, ${shakeY}px)`,
        }}
      >
        {/* Gradient Background — warning red tint */}
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(150deg, #1a0505 0%, #1C1C1C 40%, #150808 70%, #1C1C1C 100%)',
          }}
        />

        {/* Red warning flash overlay */}
        <AbsoluteFill
          style={{
            backgroundColor: `rgba(255,0,0,${warningFlash})`,
            pointerEvents: 'none',
          }}
        />

        {/* Shape Layers — Warning triangles */}
        {[...Array(5)].map((_, i) => {
          const seed = seededRandom(i + 1600);
          const size = 140 + seed * 250;
          const posX = seed * 1800;
          const posY = seededRandom(i + 1601) * 1000;
          const rot = interpolate(frame, [0, durationInFrames], [0, 10 + seed * 20], {
            extrapolateRight: 'clamp',
          });
          const breathe = 1 + Math.sin(frame * 0.02 + i * 1.7) * 0.05;
          return (
            <div
              key={`warn-${i}`}
              style={{
                position: 'absolute',
                left: posX,
                top: posY,
                width: size,
                height: size,
                transform: `rotate(${rot}deg) scale(${breathe})`,
                opacity: 0.04 + seed * 0.03,
              }}
            >
              <svg width={size} height={size}>
                <polygon
                  points={`${size / 2},8 ${size - 8},${size - 8} 8,${size - 8}`}
                  fill="none"
                  stroke="#ff0000"
                  strokeWidth="1.5"
                />
              </svg>
            </div>
          );
        })}

        {/* Motion Texture — Diagonal warning stripes */}
        <AbsoluteFill style={{ opacity: 0.035 }}>
          {[...Array(15)].map((_, i) => (
            <div
              key={`stripe-${i}`}
              style={{
                position: 'absolute',
                left: -200 + i * 160,
                top: 0,
                width: 60,
                height: 2000,
                backgroundColor: '#ff0000',
                transform: 'rotate(45deg)',
                transformOrigin: 'top left',
              }}
            />
          ))}
        </AbsoluteFill>

        {/* Particles */}
        {[...Array(20)].map((_, i) => {
          const s = seededRandom(i + 1700);
          return (
            <div
              key={`p16-${i}`}
              style={{
                position: 'absolute',
                left: s * 1920 + Math.sin(frame * 0.02 + i) * 10,
                top: (seededRandom(i + 1701) * 1080 + frame * (0.2 + s * 0.5)) % 1080,
                width: 2 + s * 3,
                height: 2 + s * 3,
                borderRadius: '50%',
                backgroundColor: i % 2 === 0 ? '#ff0000' : '#F5F5F5',
                opacity: 0.1 + s * 0.12,
              }}
            />
          );
        })}

        {/* Main Content */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            padding: '0 100px',
          }}
        >
          {/* "रुको!" — Giant warning text */}
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 200,
              color: '#ff0000',
              textShadow: '0 0 60px rgba(255,0,0,0.4), 0 0 120px rgba(255,0,0,0.15)',
              opacity: sp(5, { damping: 6, stiffness: 180 }),
              transform: `scale(${sp(5, { damping: 6, stiffness: 180 })})`,
              lineHeight: 1,
              marginBottom: 20,
            }}
          >
            रुको!
          </div>

          {/* Progress bar — showing 60%+ danger */}
          <div
            style={{
              width: 800,
              marginBottom: 40,
              opacity: sp(45),
              transform: `translateY(${interpolate(sp(45), [0, 1], [30, 0])}px)`,
            }}
          >
            {/* Label above bar */}
            <div
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                marginBottom: 10,
              }}
            >
              <span
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 28,
                  color: '#9E9E9E',
                }}
              >
                खर्चे / आमदनी
              </span>
              <span
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 30,
                  fontWeight: 800,
                  color: meterFill > 60 ? '#ff0000' : '#e9d700',
                }}
              >
                {Math.round(meterFill)}%
              </span>
            </div>

            {/* Bar track */}
            <div
              style={{
                width: '100%',
                height: 30,
                backgroundColor: '#2D2D2D',
                borderRadius: 15,
                overflow: 'hidden',
                position: 'relative',
              }}
            >
              {/* 60% danger marker */}
              <div
                style={{
                  position: 'absolute',
                  left: '60%',
                  top: 0,
                  width: 3,
                  height: '100%',
                  backgroundColor: '#F5F5F5',
                  opacity: 0.6,
                  zIndex: 2,
                }}
              />
              <div
                style={{
                  position: 'absolute',
                  left: '60%',
                  top: -22,
                  fontFamily: 'JetBrains Mono',
                  fontSize: 18,
                  color: '#F5F5F5',
                  transform: 'translateX(-50%)',
                  opacity: sp(80),
                }}
              >
                60%
              </div>

              {/* Fill bar */}
              <div
                style={{
                  width: `${meterFill}%`,
                  height: '100%',
                  borderRadius: 15,
                  background:
                    meterFill > 60
                      ? `linear-gradient(90deg, #e9d700 0%, #ff0000 100%)`
                      : `linear-gradient(90deg, #e9d700 0%, #FFC107 100%)`,
                  opacity: meterFill > 60 ? dangerBlink : 1,
                  transition: 'background 0.3s',
                }}
              />
            </div>

            {/* Danger zone label */}
            {meterFill > 60 && (
              <div
                style={{
                  textAlign: 'right',
                  marginTop: 8,
                  fontFamily: 'Inter',
                  fontSize: 22,
                  fontWeight: 700,
                  color: '#ff0000',
                  opacity: sp(110) * dangerBlink,
                  letterSpacing: 2,
                }}
              >
                ⚠ DANGER ZONE
              </div>
            )}
          </div>

          {/* Two columns — What NOT to do vs What TO do */}
          <div
            style={{
              display: 'flex',
              gap: 50,
              marginTop: 10,
            }}
          >
            {/* Wrong — निवेश खोजना */}
            <div
              style={{
                backgroundColor: 'rgba(255,0,0,0.08)',
                borderRadius: 16,
                padding: '25px 40px',
                border: '2px solid rgba(255,0,0,0.3)',
                opacity: sp(70),
                transform: `translateX(${interpolate(sp(70), [0, 1], [-50, 0])}px)`,
                position: 'relative',
              }}
            >
              {/* Cross */}
              <div
                style={{
                  position: 'absolute',
                  top: -15,
                  left: -15,
                  width: 40,
                  height: 40,
                  borderRadius: '50%',
                  backgroundColor: '#ff0000',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                <svg width="22" height="22" viewBox="0 0 24 24" fill="#F5F5F5">
                  <path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" />
                </svg>
              </div>
              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 36,
                  color: '#ff0000',
                  fontWeight: 700,
                }}
              >
                निवेश खोजने का
                <br />
                वक्त नहीं!
              </div>
            </div>

            {/* Right — खर्चे तराशना */}
            <div
              style={{
                backgroundColor: 'rgba(233,215,0,0.08)',
                borderRadius: 16,
                padding: '25px 40px',
                border: '2px solid rgba(233,215,0,0.3)',
                opacity: sp(90),
                transform: `translateX(${interpolate(sp(90), [0, 1], [50, 0])}px)`,
                position: 'relative',
              }}
            >
              {/* Check */}
              <div
                style={{
                  position: 'absolute',
                  top: -15,
                  left: -15,
                  width: 40,
                  height: 40,
                  borderRadius: '50%',
                  backgroundColor: '#e9d700',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                <svg width="22" height="22" viewBox="0 0 24 24" fill="#1C1C1C">
                  <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                </svg>
              </div>
              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 36,
                  color: '#e9d700',
                  fontWeight: 700,
                }}
              >
                खर्चे तराशने
                <br />
                का वक्त!
              </div>

              {/* Scissors icon */}
              <div
                style={{
                  position: 'absolute',
                  bottom: -20,
                  right: -20,
                  transform: `rotate(${scissorsRot}deg) scale(${scissorsScale})`,
                }}
              >
                <svg width="50" height="50" viewBox="0 0 24 24" fill="#e9d700">
                  <path d="M9.64 7.64c.23-.5.36-1.05.36-1.64 0-2.21-1.79-4-4-4S2 3.79 2 6s1.79 4 4 4c.59 0 1.14-.13 1.64-.36L10 12l-2.36 2.36C7.14 14.13 6.59 14 6 14c-2.21 0-4 1.79-4 4s1.79 4 4 4 4-1.79 4-4c0-.59-.13-1.14-.36-1.64L12 14l7 7h3v-1L9.64 7.64zM6 8c-1.1 0-2-.89-2-2s.9-2 2-2 2 .89 2 2-.9 2-2 2zm0 12c-1.1 0-2-.89-2-2s.9-2 2-2 2 .89 2 2-.9 2-2 2zm6-7.5c-.28 0-.5-.22-.5-.5s.22-.5.5-.5.5.22.5.5-.22.5-.5.5zM19 3l-6 6 2 2 7-7V3z" />
                </svg>
              </div>
            </div>
          </div>
        </AbsoluteFill>

        {/* Buffett — serious face, bottom right */}
        <div
          style={{
            position: 'absolute',
            bottom: 45,
            right: 120,
            opacity: sp(55),
            transform: `scale(${sp(55)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <svg width="70" height="120" viewBox="0 0 70 120">
            <circle cx="35" cy="18" r="25" fill="rgba(233,215,0,0.06)" />
            <g transform={`rotate(${Math.sin(frame * 0.04) * 2}, 35, 18)`}>
              <circle cx="35" cy="18" r="12" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <circle cx="29" cy="16" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <circle cx="41" cy="16" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <line x1="34" y1="16" x2="36" y2="16" stroke="#FFC107" strokeWidth="1" />
              <circle cx="29" cy="16" r="1.5" fill="#FFC107" />
              <circle cx="41" cy="16" r="1.5" fill="#FFC107" />
              {/* Serious mouth */}
              <line x1="29" y1="25" x2="41" y2="25" stroke="#FFC107" strokeWidth="1.5" />
            </g>
            <line x1="35" y1="30" x2="35" y2="72" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="35" y1="45" x2="18" y2="58" stroke="#FFC107" strokeWidth="2" />
            <line x1="35" y1="45" x2="52" y2="58" stroke="#FFC107" strokeWidth="2" />
            <line x1="35" y1="72" x2="22" y2="105" stroke="#FFC107" strokeWidth="2" />
            <line x1="35" y1="72" x2="48" y2="105" stroke="#FFC107" strokeWidth="2" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.6) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene16_RukoTarasho;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene17_MetroCities',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene17_MetroCities: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera pan left to right — skyline scrolling
  const skylinePan = interpolate(frame, [0, durationInFrames], [0, -120], {
    extrapolateRight: 'clamp',
  });

  // City data
  const cities = [
    { name: 'दिल्ली', rent: '₹18K', percent: 52, delay: 25 },
    { name: 'मुंबई', rent: '₹25K', percent: 65, delay: 55 },
    { name: 'बैंगलूरू', rent: '₹20K', percent: 55, delay: 85 },
  ];

  // Fire/burn effect for "जलते हैं"
  const burnOp = interpolate(frame, [200, 220, 280, 300], [0, 1, 1, 0.7], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(1.03)`,
        }}
      >
        {/* Gradient Background — urban dark blue */}
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(180deg, #050810 0%, #0a1020 30%, #1C1C1C 70%, #0d0a15 100%)',
          }}
        />

        {/* Cityscape Skyline — SVG silhouette */}
        <div
          style={{
            position: 'absolute',
            bottom: 0,
            left: skylinePan,
            width: 3000,
            height: 350,
            opacity: 0.08,
          }}
        >
          <svg width="3000" height="350" viewBox="0 0 3000 350">
            {/* Simplified city skyline */}
            <rect x="50" y="100" width="60" height="250" fill="#e9d700" />
            <rect x="130" y="50" width="45" height="300" fill="#e9d700" />
            <rect x="195" y="130" width="70" height="220" fill="#e9d700" />
            <rect x="290" y="30" width="40" height="320" fill="#e9d700" />
            <rect x="350" y="80" width="80" height="270" fill="#e9d700" />
            <rect x="460" y="60" width="50" height="290" fill="#e9d700" />
            <rect x="530" y="150" width="65" height="200" fill="#e9d700" />
            <rect x="620" y="40" width="55" height="310" fill="#e9d700" />
            <rect x="700" y="90" width="75" height="260" fill="#e9d700" />
            <rect x="800" y="20" width="45" height="330" fill="#e9d700" />
            <rect x="870" y="110" width="60" height="240" fill="#e9d700" />
            <rect x="960" y="70" width="50" height="280" fill="#e9d700" />
            <rect x="1040" y="50" width="70" height="300" fill="#e9d700" />
            <rect x="1140" y="100" width="55" height="250" fill="#e9d700" />
            <rect x="1220" y="35" width="40" height="315" fill="#e9d700" />
            <rect x="1290" y="80" width="80" height="270" fill="#e9d700" />
            <rect x="1400" y="60" width="60" height="290" fill="#e9d700" />
            <rect x="1490" y="120" width="50" height="230" fill="#e9d700" />
            <rect x="1570" y="45" width="65" height="305" fill="#e9d700" />
            <rect x="1660" y="90" width="45" height="260" fill="#e9d700" />
            <rect x="1730" y="25" width="70" height="325" fill="#e9d700" />
          </svg>
        </div>

        {/* Shape Layers — Tall rectangles like buildings */}
        {[...Array(6)].map((_, i) => {
          const seed = seededRandom(i + 1700);
          const w = 30 + seed * 60;
          const h = 200 + seed * 400;
          const posX = seed * 1800;
          const drift = Math.sin(frame * 0.006 + i) * 10;
          return (
            <div
              key={`bld-${i}`}
              style={{
                position: 'absolute',
                left: posX + drift,
                bottom: 0,
                width: w,
                height: h,
                border: `1px solid ${i % 2 === 0 ? '#e9d700' : '#9E9E9E'}`,
                borderRadius: '4px 4px 0 0',
                opacity: 0.04,
              }}
            />
          );
        })}

        {/* Particles */}
        {[...Array(22)].map((_, i) => {
          const s = seededRandom(i + 1800);
          return (
            <div
              key={`p17-${i}`}
              style={{
                position: 'absolute',
                left: s * 1920 + Math.sin(frame * 0.02 + i) * 8,
                top: (seededRandom(i + 1801) * 1080 - frame * (0.3 + s * 0.4) + 1080) % 1080,
                width: 2 + s * 3,
                height: 2 + s * 3,
                borderRadius: '50%',
                backgroundColor: i % 4 === 0 ? '#FFC107' : '#F5F5F5',
                opacity: 0.1 + s * 0.12,
              }}
            />
          );
        })}

        {/* Main Content */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            padding: '0 80px',
          }}
        >
          {/* Title */}
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 80,
              color: '#F5F5F5',
              marginBottom: 50,
              opacity: sp(5),
              transform: `translateY(${interpolate(sp(5), [0, 1], [30, 0])}px)`,
              textAlign: 'center',
            }}
          >
            बड़े शहरों की{' '}
            <span style={{ color: '#ff0000' }}>सच्चाई</span>
          </div>

          {/* City Cards Row */}
          <div
            style={{
              display: 'flex',
              gap: 45,
              justifyContent: 'center',
            }}
          >
            {cities.map((city, i) => {
              const cardSp = sp(city.delay, { damping: 10, stiffness: 100 });
              const barFill = interpolate(
                frame,
                [city.delay + 30, city.delay + 80],
                [0, city.percent],
                { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
              );
              const isDanger = city.percent > 50;

              return (
                <div
                  key={`city-${i}`}
                  style={{
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'center',
                    backgroundColor: 'rgba(45,45,45,0.7)',
                    borderRadius: 20,
                    padding: '35px 40px',
                    border: `2px solid ${isDanger ? 'rgba(255,0,0,0.3)' : 'rgba(233,215,0,0.2)'}`,
                    minWidth: 260,
                    opacity: cardSp,
                    transform: `translateY(${interpolate(cardSp, [0, 1], [50, 0])}px) scale(${cardSp})`,
                    backdropFilter: 'blur(6px)',
                    boxShadow: isDanger
                      ? '0 10px 40px rgba(255,0,0,0.1)'
                      : '0 10px 40px rgba(0,0,0,0.3)',
                  }}
                >
                  {/* City icon */}
                  <svg width="45" height="45" viewBox="0 0 24 24" fill={isDanger ? '#ff0000' : '#e9d700'}>
                    <path d="M15 11V5l-3-3-3 3v2H3v14h18V11h-6zm-8 8H5v-2h2v2zm0-4H5v-2h2v2zm0-4H5V9h2v2zm6 8h-2v-2h2v2zm0-4h-2v-2h2v2zm0-4h-2V9h2v2zm0-4h-2V5h2v2zm6 12h-2v-2h2v2zm0-4h-2v-2h2v2z" />
                  </svg>

                  {/* City name */}
                  <div
                    style={{
                      fontFamily: 'Yantra One',
                      fontSize: 48,
                      color: '#F5F5F5',
                      marginTop: 12,
                    }}
                  >
                    {city.name}
                  </div>

                  {/* Rent */}
                  <div
                    style={{
                      fontFamily: 'JetBrains Mono',
                      fontSize: 32,
                      color: '#FFC107',
                      marginTop: 8,
                    }}
                  >
                    {city.rent}/mo
                  </div>

                  {/* Vertical bar */}
                  <div
                    style={{
                      width: 50,
                      height: 140,
                      backgroundColor: '#2D2D2D',
                      borderRadius: 10,
                      marginTop: 20,
                      position: 'relative',
                      overflow: 'hidden',
                    }}
                  >
                    {/* 50% danger line */}
                    <div
                      style={{
                        position: 'absolute',
                        bottom: '50%',
                        left: 0,
                        right: 0,
                        height: 2,
                        backgroundColor: '#F5F5F5',
                        opacity: 0.4,
                        zIndex: 2,
                      }}
                    />
                    {/* Fill */}
                    <div
                      style={{
                        position: 'absolute',
                        bottom: 0,
                        left: 0,
                        right: 0,
                        height: `${barFill}%`,
                        background: isDanger
                          ? 'linear-gradient(180deg, #ff0000 0%, #FFC107 100%)'
                          : 'linear-gradient(180deg, #e9d700 0%, #FFC107 100%)',
                        borderRadius: 10,
                      }}
                    />
                  </div>

                  {/* Percent */}
                  <div
                    style={{
                      fontFamily: 'JetBrains Mono',
                      fontSize: 36,
                      fontWeight: 900,
                      color: isDanger ? '#ff0000' : '#e9d700',
                      marginTop: 10,
                    }}
                  >
                    {Math.round(barFill)}%
                  </div>
                  <div
                    style={{
                      fontFamily: 'Noto Sans Devanagari',
                      fontSize: 20,
                      color: '#9E9E9E',
                    }}
                  >
                    छत पर
                  </div>
                </div>
              );
            })}
          </div>

          {/* Bottom text — "घंटे और पैसे दोनों जलते हैं" */}
          <div
            style={{
              marginTop: 45,
              display: 'flex',
              alignItems: 'center',
              gap: 15,
              opacity: sp(170),
              transform: `translateY(${interpolate(sp(170), [0, 1], [20, 0])}px)`,
            }}
          >
            {/* Fire icon */}
            <svg
              width="40"
              height="40"
              viewBox="0 0 24 24"
              fill="#ff0000"
              style={{
                opacity: burnOp,
                transform: `scale(${1 + Math.sin(frame * 0.15) * 0.1})`,
              }}
            >
              <path d="M13.5.67s.74 2.65.74 4.8c0 2.06-1.35 3.73-3.41 3.73-2.07 0-3.63-1.67-3.63-3.73l.03-.36C5.21 7.51 4 10.62 4 14c0 4.42 3.58 8 8 8s8-3.58 8-8C20 8.61 17.41 3.8 13.5.67zM11.71 19c-1.78 0-3.22-1.4-3.22-3.14 0-1.62 1.05-2.76 2.81-3.12 1.77-.36 3.6-1.21 4.62-2.58.39 1.29.59 2.65.59 4.04 0 2.65-2.15 4.8-4.8 4.8z" />
            </svg>
            <span
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              आने-जाने में{' '}
              <span style={{ color: '#ff0000' }}>घंटे और पैसे</span>{' '}
              दोनों जलते हैं
            </span>
          </div>
        </AbsoluteFill>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene17_MetroCities;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene18_SawalPucho',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene18_SawalPucho: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera gentle sway
  const camX = Math.sin(frame * 0.008) * 8;
  const camY = Math.cos(frame * 0.006) * 5;

  // Questions data
  const questions = [
    {
      text: 'क्या किराया कम हो सकता है?',
      icon: 'home',
      delay: 20,
      color: '#FFC107',
    },
    {
      text: 'क्या दफ्तर के नज़दीक रहकर खर्चा बचेगा?',
      icon: 'location',
      delay: 65,
      color: '#e9d700',
    },
    {
      text: 'कोई सदस्यता बिना इस्तेमाल पैसे काट रही?',
      icon: 'subscription',
      delay: 115,
      color: '#ff0000',
    },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `translate(${camX}px, ${camY}px)`,
        }}
      >
        {/* Gradient Background — introspective deep purple */}
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(140deg, #0d0818 0%, #1C1C1C 35%, #120a20 65%, #1C1C1C 100%)',
          }}
        />

        {/* Shape Layers — Question mark shaped curves */}
        {[...Array(5)].map((_, i) => {
          const seed = seededRandom(i + 1800);
          const size = 150 + seed * 300;
          const posX = seed * 1700;
          const posY = seededRandom(i + 1801) * 900;
          const rot = interpolate(frame, [0, durationInFrames], [0, 15 + seed * 20], {
            extrapolateRight: 'clamp',
          });
          const breathe = 1 + Math.sin(frame * 0.02 + i * 1.3) * 0.05;
          return (
            <div
              key={`qs-${i}`}
              style={{
                position: 'absolute',
                left: posX,
                top: posY,
                transform: `rotate(${rot}deg) scale(${breathe})`,
                opacity: 0.05,
                fontFamily: 'Inter',
                fontSize: size,
                fontWeight: 900,
                color: '#e9d700',
                lineHeight: 1,
              }}
            >
              ?
            </div>
          );
        })}

        {/* Motion Texture — Concentric squares */}
        <AbsoluteFill style={{ opacity: 0.03 }}>
          {[...Array(6)].map((_, i) => {
            const sqSize = 200 + i * 150;
            const sqBreath = 1 + Math.sin(frame * 0.015 + i * 0.7) * 0.06;
            return (
              <div
                key={`sq-${i}`}
                style={{
                  position: 'absolute',
                  left: '50%',
                  top: '50%',
                  width: sqSize,
                  height: sqSize,
                  marginLeft: -sqSize / 2,
                  marginTop: -sqSize / 2,
                  border: '1px solid #e9d700',
                  borderRadius: 12 + i * 4,
                  transform: `scale(${sqBreath}) rotate(${i * 8 + frame * 0.05}deg)`,
                }}
              />
            );
          })}
        </AbsoluteFill>

        {/* Particles */}
        {[...Array(20)].map((_, i) => {
          const s = seededRandom(i + 1900);
          return (
            <div
              key={`p18-${i}`}
              style={{
                position: 'absolute',
                left: s * 1920 + Math.sin(frame * 0.02 + i) * 10,
                top: (seededRandom(i + 1901) * 1080 + frame * (0.25 + s * 0.5)) % 1080,
                width: 2 + s * 3,
                height: 2 + s * 3,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#e9d700' : '#F5F5F5',
                opacity: 0.1 + s * 0.12,
              }}
            />
          );
        })}

        {/* Main Content — Question Cards */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            padding: '0 120px',
          }}
        >
          {/* Header */}
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 85,
              color: '#F5F5F5',
              marginBottom: 55,
              opacity: sp(5),
              transform: `translateY(${interpolate(sp(5), [0, 1], [30, 0])}px)`,
              textAlign: 'center',
            }}
          >
            <span style={{ color: '#e9d700' }}>सवाल</span> पूछो
            खुद से
          </div>

          {/* Question cards */}
          <div
            style={{
              display: 'flex',
              flexDirection: 'column',
              gap: 25,
              width: '100%',
              maxWidth: 1200,
            }}
          >
            {questions.map((q, i) => {
              const cardSp = sp(q.delay, { damping: 10, stiffness: 100 });
              const slideDir = i % 2 === 0 ? -1 : 1;

              return (
                <div
                  key={`q-${i}`}
                  style={{
                    display: 'flex',
                    alignItems: 'center',
                    gap: 25,
                    backgroundColor: 'rgba(45,45,45,0.6)',
                    borderRadius: 18,
                    padding: '28px 40px',
                    borderLeft: `5px solid ${q.color}`,
                    opacity: cardSp,
                    transform: `translateX(${interpolate(cardSp, [0, 1], [80 * slideDir, 0])}px)`,
                    backdropFilter: 'blur(5px)',
                    boxShadow: `0 8px 30px rgba(0,0,0,0.3), inset 0 0 0 1px rgba(${q.color === '#ff0000' ? '255,0,0' : '233,215,0'},0.1)`,
                  }}
                >
                  {/* Question number bubble */}
                  <div
                    style={{
                      width: 60,
                      height: 60,
                      borderRadius: '50%',
                      backgroundColor: q.color,
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                      flexShrink: 0,
                    }}
                  >
                    <svg width="30" height="30" viewBox="0 0 24 24" fill="#1C1C1C">
                      {q.icon === 'home' && (
                        <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z" />
                      )}
                      {q.icon === 'location' && (
                        <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z" />
                      )}
                      {q.icon === 'subscription' && (
                        <path d="M20 4H4c-1.11 0-2 .89-2 2v12c0 1.11.89 2 2 2h16c1.11 0 2-.89 2-2V6c0-1.11-.89-2-2-2zm0 14H4V6h16v12zm-2-7H6v-2h12v2zm-4 4H6v-2h8v2z" />
                      )}
                    </svg>
                  </div>

                  {/* Question text */}
                  <div
                    style={{
                      fontFamily: 'Noto Sans Devanagari',
                      fontSize: 40,
                      fontWeight: 600,
                      color: '#F5F5F5',
                      lineHeight: 1.4,
                    }}
                  >
                    {q.text}
                  </div>

                  {/* Animated question mark */}
                  <div
                    style={{
                      marginLeft: 'auto',
                      fontFamily: 'Inter',
                      fontSize: 55,
                      fontWeight: 900,
                      color: q.color,
                      opacity: 0.6,
                      transform: `rotate(${Math.sin(frame * 0.05 + i) * 10}deg)`,
                      flexShrink: 0,
                    }}
                  >
                    ?
                  </div>
                </div>
              );
            })}
          </div>

          {/* Bottom insight */}
          <div
            style={{
              marginTop: 40,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 34,
              color: '#9E9E9E',
              opacity: sp(180),
              textAlign: 'center',
            }}
          >
            छोटे सवालों के जवाब से{' '}
            <span
              style={{
                color: '#e9d700',
                fontWeight: 700,
                fontSize: 38,
              }}
            >
              हज़ारों रुपये
            </span>{' '}
            बच सकते हैं
          </div>
        </AbsoluteFill>

        {/* Buffett — thinking pose, bottom left */}
        <div
          style={{
            position: 'absolute',
            bottom: 40,
            left: 80,
            opacity: sp(40),
            transform: `scale(${sp(40)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <svg width="75" height="125" viewBox="0 0 75 125">
            <circle cx="37" cy="20" r="28" fill="rgba(233,215,0,0.06)" />
            <g transform={`rotate(${Math.sin(frame * 0.04) * 3}, 37, 20)`}>
              <circle cx="37" cy="20" r="13" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <circle cx="31" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <circle cx="43" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <line x1="36" y1="18" x2="38" y2="18" stroke="#FFC107" strokeWidth="1" />
              <circle cx="31" cy="18" r="1.5" fill="#FFC107" />
              <circle cx="43" cy="18" r="1.5" fill="#FFC107" />
              {/* Thinking expression */}
              <path d="M 33 27 Q 37 25 41 27" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            </g>
            <line x1="37" y1="33" x2="37" y2="75" stroke="#FFC107" strokeWidth="2.5" />
            {/* Hand on chin — thinking */}
            <line x1="37" y1="50" x2="22" y2="42" stroke="#FFC107" strokeWidth="2" />
            <line x1="22" y1="42" x2="28" y2="30" stroke="#FFC107" strokeWidth="2" />
            <line x1="37" y1="50" x2="55" y2="62" stroke="#FFC107" strokeWidth="2" />
            <line x1="37" y1="75" x2="24" y2="108" stroke="#FFC107" strokeWidth="2" />
            <line x1="37" y1="75" x2="50" y2="108" stroke="#FFC107" strokeWidth="2" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene18_SawalPucho;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene19_DilVsDimag',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene19_DilVsDimag: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow orbit
  const camRot = interpolate(frame, [0, durationInFrames], [-1, 1.5], {
    extrapolateRight: 'clamp',
  });

  // Scale comparison animation — left fading, right growing
  const heartScale = interpolate(
    frame,
    [80, 150, 200, durationInFrames],
    [1, 1, 0.85, 0.75],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );
  const brainScale = interpolate(
    frame,
    [80, 150, 200, durationInFrames],
    [1, 1, 1.12, 1.18],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Arrow pointing from heart to brain
  const arrowProgress = interpolate(frame, [160, 220], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `perspective(1200px) rotateY(${camRot}deg)`,
        }}
      >
        {/* Gradient — split warm/cool */}
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(90deg, rgba(255,0,0,0.05) 0%, #1C1C1C 50%, rgba(233,215,0,0.05) 100%)',
          }}
        />
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(170deg, #0a0a12 0%, transparent 50%, #0a100a 100%)',
          }}
        />

        {/* Shape Layers — Brain neural network patterns */}
        {[...Array(8)].map((_, i) => {
          const seed = seededRandom(i + 1900);
          const x1 = seed * 1920;
          const y1 = seededRandom(i + 1901) * 1080;
          const x2 = seededRandom(i + 1902) * 1920;
          const y2 = seededRandom(i + 1903) * 1080;
          const pulse = 0.04 + Math.sin(frame * 0.02 + i * 1.2) * 0.02;
          return (
            <svg
              key={`neural-${i}`}
              style={{
                position: 'absolute',
                left: 0,
                top: 0,
                width: '100%',
                height: '100%',
                opacity: pulse,
              }}
            >
              <line
                x1={x1}
                y1={y1}
                x2={x2}
                y2={y2}
                stroke={i % 2 === 0 ? '#e9d700' : '#9E9E9E'}
                strokeWidth="1"
              />
              <circle cx={x1} cy={y1} r="3" fill={i % 2 === 0 ? '#e9d700' : '#9E9E9E'} opacity="0.3" />
              <circle cx={x2} cy={y2} r="3" fill={i % 2 === 0 ? '#e9d700' : '#9E9E9E'} opacity="0.3" />
            </svg>
          );
        })}

        {/* Particles */}
        {[...Array(18)].map((_, i) => {
          const s = seededRandom(i + 2000);
          return (
            <div
              key={`p19-${i}`}
              style={{
                position: 'absolute',
                left: s * 1920 + Math.sin(frame * 0.02 + i) * 10,
                top: (seededRandom(i + 2001) * 1080 + frame * (0.3 + s * 0.4)) % 1080,
                width: 2 + s * 3,
                height: 2 + s * 3,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#e9d700' : '#F5F5F5',
                opacity: 0.1 + s * 0.12,
              }}
            />
          );
        })}

        {/* Main Content */}
        <AbsoluteFill
          style={{
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          {/* Top text */}
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 36,
              color: '#9E9E9E',
              marginBottom: 30,
              opacity: sp(5),
              letterSpacing: 2,
            }}
          >
            अंदाज़े vs हिसाब
          </div>

          {/* Heart vs Brain comparison */}
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              gap: 80,
            }}
          >
            {/* Heart — दिल (fading) */}
            <div
              style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                opacity: sp(15),
                transform: `scale(${sp(15) * heartScale})`,
              }}
            >
              <svg
                width="150"
                height="150"
                viewBox="0 0 24 24"
                style={{
                  filter: `drop-shadow(0 0 ${20 * heartScale}px rgba(255,0,0,${0.3 * heartScale}))`,
                }}
              >
                <path
                  d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"
                  fill="#ff0000"
                  opacity={heartScale}
                />
              </svg>
              <div
                style={{
                  fontFamily: 'Yantra One',
                  fontSize: 70,
                  color: '#ff0000',
                  marginTop: 15,
                  opacity: heartScale,
                }}
              >
                दिल
              </div>
              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 28,
                  color: '#9E9E9E',
                  marginTop: 8,
                  opacity: heartScale * 0.8,
                }}
              >
                भावनाओं से फ़ैसले
              </div>

              {/* Strikethrough */}
              <div
                style={{
                  width: interpolate(
                    frame,
                    [180, 220],
                    [0, 200],
                    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
                  ),
                  height: 4,
                  backgroundColor: '#ff0000',
                  marginTop: 12,
                  borderRadius: 2,
                  opacity: 0.8,
                }}
              />
            </div>

            {/* Arrow */}
            <div
              style={{
                opacity: arrowProgress,
                transform: `scaleX(${arrowProgress})`,
              }}
            >
              <svg width="100" height="60" viewBox="0 0 100 60">
                <path
                  d="M 5 30 L 70 30"
                  stroke="#e9d700"
                  strokeWidth="4"
                  fill="none"
                  strokeDasharray="8,6"
                />
                <polygon points="70,15 95,30 70,45" fill="#e9d700" />
              </svg>
            </div>

            {/* Brain — दिमाग (growing) */}
            <div
              style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                opacity: sp(30),
                transform: `scale(${sp(30) * brainScale})`,
              }}
            >
              <svg
                width="150"
                height="150"
                viewBox="0 0 24 24"
                style={{
                  filter: `drop-shadow(0 0 ${25 * brainScale}px rgba(233,215,0,${0.3 * brainScale}))`,
                }}
              >
                <path
                  d="M13 1.07V9h7c0-4.08-3.05-7.44-7-7.93zM4 15c0 4.42 3.58 8 8 8s8-3.58 8-8v-4H4v4zm7-13.93C7.05 1.56 4 4.92 4 9h7V1.07z"
                  fill="#e9d700"
                />
              </svg>
              <div
                style={{
                  fontFamily: 'Yantra One',
                  fontSize: 70,
                  color: '#e9d700',
                  marginTop: 15,
                }}
              >
                दिमाग
              </div>
              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 28,
                  color: '#9E9E9E',
                  marginTop: 8,
                }}
              >
                हिसाब से फ़ैसले
              </div>

              {/* Checkmark */}
              <div
                style={{
                  marginTop: 12,
                  opacity: sp(200),
                  transform: `scale(${sp(200, { damping: 8 })})`,
                }}
              >
                <svg width="40" height="40" viewBox="0 0 24 24">
                  <circle cx="12" cy="12" r="11" fill="rgba(233,215,0,0.15)" stroke="#e9d700" strokeWidth="2" />
                  <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" fill="#e9d700" />
                </svg>
              </div>
            </div>
          </div>

          {/* Bottom wisdom line */}
          <div
            style={{
              marginTop: 60,
              backgroundColor: 'rgba(45,45,45,0.7)',
              borderRadius: 14,
              padding: '18px 50px',
              border: '1px solid rgba(233,215,0,0.2)',
              opacity: sp(140),
              transform: `translateY(${interpolate(sp(140), [0, 1], [20, 0])}px)`,
            }}
          >
            <span
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 38,
                color: '#F5F5F5',
                fontWeight: 600,
              }}
            >
              अंदाज़े →{' '}
              <span style={{ color: '#ff0000' }}>दिल</span> | हिसाब
              →{' '}
              <span style={{ color: '#e9d700' }}>दिमाग</span>
            </span>
          </div>
        </AbsoluteFill>

        {/* Buffett — pointing to brain side */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            right: 100,
            opacity: sp(50),
            transform: `scale(${sp(50)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          }}
        >
          <svg width="70" height="115" viewBox="0 0 70 115">
            <circle cx="35" cy="18" r="25" fill="rgba(233,215,0,0.06)" />
            <g transform={`rotate(${Math.sin(frame * 0.04) * 3}, 35, 18)`}>
              <circle cx="35" cy="18" r="12" fill="none" stroke="#FFC107" strokeWidth="2.5" />
              <circle cx="29" cy="16" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <circle cx="41" cy="16" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <line x1="34" y1="16" x2="36" y2="16" stroke="#FFC107" strokeWidth="1" />
              <circle cx="29" cy="16" r="1.5" fill="#FFC107" />
              <circle cx="41" cy="16" r="1.5" fill="#FFC107" />
              <path d="M 29 24 Q 35 28 41 24" fill="none" stroke="#FFC107" strokeWidth="1.5" />
            </g>
            <line x1="35" y1="30" x2="35" y2="68" stroke="#FFC107" strokeWidth="2.5" />
            {/* Pointing to brain */}
            <line x1="35" y1="44" x2="15" y2="32" stroke="#FFC107" strokeWidth="2" />
            <line x1="35" y1="44" x2="55" y2="56" stroke="#FFC107" strokeWidth="2" />
            <line x1="35" y1="68" x2="23" y2="100" stroke="#FFC107" strokeWidth="2" />
            <line x1="35" y1="68" x2="47" y2="100" stroke="#FFC107" strokeWidth="2" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(
              frame,
              [0, 30, durationInFrames - 15, durationInFrames],
              [0, 0.3, 0.3, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            ),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene19_DilVsDimag;
```

