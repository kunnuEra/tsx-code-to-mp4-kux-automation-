```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene1_DaulatKaSawaal',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_DaulatKaSawaal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera zoom in
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.05, 1.15], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Gradient shift
  const gradAngle = interpolate(frame, [0, durationInFrames], [135, 165], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 40 }, (_, i) => {
    const seed = seededRandom(i + 1);
    const seed2 = seededRandom(i + 50);
    const x = seed * 1920;
    const y = seed2 * 1080;
    const speed = 0.3 + seed * 0.7;
    const size = 2 + seed2 * 4;
    const opacity = interpolate(
      frame,
      [0, 30, durationInFrames - 20, durationInFrames],
      [0, 0.3 + seed * 0.4, 0.3 + seed * 0.4, 0],
      { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
    );
    return { x, y: y + Math.sin(frame * 0.02 * speed + i) * 30 - frame * speed * 0.5, size, opacity };
  });

  // Question mark animation
  const questionScale = sp(20, { damping: 8, stiffness: 80 });
  const questionRotate = interpolate(frame, [20, 80], [0, 15], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Main text
  const titleOp = sp(10);
  const titleY = interpolate(sp(10), [0, 1], [80, 0]);

  const subOp = sp(35);
  const subY = interpolate(sp(35), [0, 1], [60, 0]);

  // Floating icons
  const iconData = [
    { emoji: '📈', x: 150, y: 200, delay: 45, rot: 12 },
    { emoji: '🏠', x: 1650, y: 180, delay: 55, rot: -8 },
    { emoji: '💎', x: 200, y: 750, delay: 65, rot: 15 },
    { emoji: '💰', x: 1700, y: 700, delay: 50, rot: -12 },
  ];

  // Shape layers
  const shapes = [
    { type: 'circle', x: 300, y: 200, size: 350, rotation: frame * 0.15, opacity: 0.04 },
    { type: 'rect', x: 1400, y: 700, size: 280, rotation: -frame * 0.1, opacity: 0.05 },
    { type: 'triangle', x: 1600, y: 150, size: 200, rotation: frame * 0.2, opacity: 0.03 },
    { type: 'circle', x: 900, y: 900, size: 400, rotation: -frame * 0.08, opacity: 0.04 },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${gradAngle}deg, #1C1C1C 0%, #2D2D2D 35%, #1a1a2e 70%, #1C1C1C 100%)`,
          transform: `scale(${cameraScale})`,
        }}
      />

      {/* Animated shape layers */}
      {shapes.map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(233, 215, 0, ${s.opacity})`,
            borderRadius: s.type === 'circle' ? '50%' : s.type === 'rect' ? '12px' : '0',
            transform: `rotate(${s.rotation}deg) scale(${1 + Math.sin(frame * 0.015) * 0.08})`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
            opacity: s.opacity * 3,
          }}
        />
      ))}

      {/* Motion texture - diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        {Array.from({ length: 20 }, (_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: -200 + i * 120 + frame * 0.3,
              top: -200,
              width: 1,
              height: 2000,
              background: '#e9d700',
              transform: 'rotate(35deg)',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#FFC107' : '#F5F5F5',
            opacity: p.opacity,
          }}
        />
      ))}

      {/* Giant question mark - center */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '42%',
          transform: `translate(-50%, -50%) scale(${questionScale}) rotate(${questionRotate}deg)`,
          fontSize: 260,
          fontFamily: 'Yantra One',
          color: '#e9d700',
          textShadow: '0 0 80px rgba(233, 215, 0, 0.3), 0 0 160px rgba(233, 215, 0, 0.1)',
          opacity: interpolate(sp(20), [0, 1], [0, 0.9]),
        }}
      >
        ?
      </div>

      {/* Main title */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '18%',
          transform: `translate(-50%, ${titleY}px)`,
          opacity: titleOp,
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 100,
            color: '#F5F5F5',
            lineHeight: 1.2,
            textShadow: '0 4px 30px rgba(0,0,0,0.5)',
          }}
        >
          दौलत कैसे बनती है<span style={{ color: '#e9d700' }}>?</span>
        </div>
      </div>

      {/* Subtitle */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: '18%',
          transform: `translate(-50%, ${subY}px)`,
          opacity: subOp,
          textAlign: 'center',
          maxWidth: 1200,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 48,
            color: '#9E9E9E',
            lineHeight: 1.5,
          }}
        >
          चमकदार शेयर? जमीन का सौदा? रातों रात करोड़पति?
        </div>
      </div>

      {/* Floating SVG icons */}
      {iconData.map((icon, i) => {
        const iconSp = sp(icon.delay);
        const float = Math.sin(frame * 0.03 + i * 2) * 12;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: icon.x,
              top: icon.y + float,
              fontSize: 72,
              transform: `scale(${iconSp}) rotate(${icon.rot + Math.sin(frame * 0.02 + i) * 5}deg)`,
              opacity: interpolate(iconSp, [0, 1], [0, 0.7]),
              filter: 'drop-shadow(0 4px 20px rgba(0,0,0,0.4))',
            }}
          >
            {/* SVG icon placeholder */}
            <svg width="72" height="72" viewBox="0 0 72 72">
              {i === 0 && (
                <g fill="none" stroke="#e9d700" strokeWidth="3">
                  <polyline points="10,55 25,35 40,45 62,15" />
                  <polyline points="50,15 62,15 62,27" />
                </g>
              )}
              {i === 1 && (
                <g fill="none" stroke="#FFC107" strokeWidth="3">
                  <path d="M36,20 L58,45 L58,60 L14,60 L14,45 Z" />
                  <rect x="28" y="42" width="16" height="18" />
                </g>
              )}
              {i === 2 && (
                <g fill="none" stroke="#e9d700" strokeWidth="3">
                  <polygon points="36,10 46,30 66,34 50,48 54,68 36,58 18,68 22,48 6,34 26,30" />
                </g>
              )}
              {i === 3 && (
                <g fill="none" stroke="#FFC107" strokeWidth="3">
                  <circle cx="36" cy="36" r="20" />
                  <text x="28" y="42" fontSize="22" fill="#FFC107" fontFamily="Inter">₹</text>
                </g>
              )}
            </svg>
          </div>
        );
      })}

      {/* Stick figure - thinking pose */}
      <div
        style={{
          position: 'absolute',
          right: 120,
          bottom: 100,
          transform: `scale(${sp(50)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(50), [0, 1], [0, 1]),
        }}
      >
        <svg width="140" height="200" viewBox="0 0 140 200">
          {/* Golden glow */}
          <circle cx="70" cy="45" r="40" fill="rgba(233, 215, 0, 0.08)" />
          {/* Head */}
          <circle cx="70" cy="45" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          {/* Glasses */}
          <circle cx="60" cy="42" r="8" fill="none" stroke="#e9d700" strokeWidth="2" />
          <circle cx="80" cy="42" r="8" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="68" y1="42" x2="72" y2="42" stroke="#e9d700" strokeWidth="2" />
          {/* Body */}
          <line x1="70" y1="67" x2="70" y2="130" stroke="#F5F5F5" strokeWidth="3" />
          {/* Arms - thinking pose (hand on chin) */}
          <line x1="70" y1="90" x2="40" y2="110" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="70" y1="90" x2="95" y2="75" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="95" y1="75" x2="82" y2="55" stroke="#F5F5F5" strokeWidth="3" />
          {/* Legs */}
          <line x1="70" y1="130" x2="50" y2="175" stroke="#F5F5F5" strokeWidth="3" />
          <line x1="70" y1="130" x2="90" y2="175" stroke="#F5F5F5" strokeWidth="3" />
          {/* Thought bubble */}
          <circle cx="110" cy="20" r="5" fill="rgba(245, 245, 245, 0.3)" />
          <circle cx="120" cy="10" r="3" fill="rgba(245, 245, 245, 0.2)" />
        </svg>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(
            frame,
            [0, 30, durationInFrames - 15, durationInFrames],
            [0, 0.3, 0.3, 0],
            { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
          ),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_DaulatKaSawaal;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_FilmVsReality',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_FilmVsReality: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera pan from left to right
  const cameraPanX = interpolate(frame, [0, durationInFrames], [30, -30], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const gradHue = interpolate(frame, [0, durationInFrames], [0, 30], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Split screen - Film side vs Reality side
  const splitReveal = interpolate(frame, [30, 70], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Divider line
  const dividerH = interpolate(sp(25), [0, 1], [0, 100]);

  // Particles
  const particles = Array.from({ length: 35 }, (_, i) => {
    const s1 = seededRandom(i + 100);
    const s2 = seededRandom(i + 200);
    const x = s1 * 1920;
    const baseY = s2 * 1080;
    const y = baseY + Math.sin(frame * 0.025 + i) * 20 - frame * 0.4;
    const size = 1.5 + s1 * 3.5;
    const op = interpolate(frame, [0, 25, durationInFrames - 15, durationInFrames], [0, 0.2 + s2 * 0.3, 0.2 + s2 * 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
    return { x, y, size, op };
  });

  // Shape layers
  const shapes = [
    { type: 'rect', x: 200, y: 150, size: 300, rot: frame * 0.12, op: 0.04 },
    { type: 'circle', x: 1650, y: 850, size: 250, rot: -frame * 0.1, op: 0.05 },
    { type: 'triangle', x: 100, y: 800, size: 200, rot: frame * 0.18, op: 0.03 },
    { type: 'rect', x: 1500, y: 200, size: 180, rot: -frame * 0.15, op: 0.04 },
  ];

  // VS text
  const vsScale = sp(50, { damping: 6, stiffness: 150 });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${160 + gradHue}deg, #1C1C1C 0%, #2D2D2D 30%, #1a1520 60%, #1C1C1C 100%)`,
          transform: `translateX(${cameraPanX}px)`,
        }}
      />

      {/* Shape layers */}
      {shapes.map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(255, 193, 7, ${s.op})`,
            borderRadius: s.type === 'circle' ? '50%' : '8px',
            transform: `rotate(${s.rot}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
          }}
        />
      ))}

      {/* Motion texture - concentric circles */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        {Array.from({ length: 8 }, (_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              width: 200 + i * 150 + frame * 0.5,
              height: 200 + i * 150 + frame * 0.5,
              borderRadius: '50%',
              border: '1px solid #e9d700',
              transform: 'translate(-50%, -50%)',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y % 1200,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#e9d700' : '#F5F5F5',
            opacity: p.op,
          }}
        />
      ))}

      {/* LEFT SIDE - Film / Fantasy */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          top: 0,
          width: '48%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          transform: `translateX(${interpolate(sp(15), [0, 1], [-100, 0])}px)`,
          opacity: sp(15),
        }}
      >
        {/* Film strip icon */}
        <svg width="120" height="120" viewBox="0 0 120 120" style={{ marginBottom: 30, opacity: 0.8 }}>
          <rect x="20" y="10" width="80" height="100" rx="8" fill="none" stroke="#ff0000" strokeWidth="3" />
          <rect x="30" y="25" width="60" height="35" rx="4" fill="rgba(255,0,0,0.15)" stroke="#ff0000" strokeWidth="2" />
          <rect x="30" y="70" width="60" height="25" rx="4" fill="rgba(255,0,0,0.1)" stroke="#ff0000" strokeWidth="1.5" />
          <circle cx="60" cy="42" r="12" fill="none" stroke="#ff0000" strokeWidth="2" />
          <polygon points="55,36 55,48 68,42" fill="#ff0000" />
        </svg>

        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 72,
            color: '#ff0000',
            textAlign: 'center',
            textShadow: '0 0 40px rgba(255,0,0,0.3)',
          }}
        >
          फिल्मी सोच
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 36,
            color: '#9E9E9E',
            textAlign: 'center',
            marginTop: 20,
            maxWidth: 380,
            lineHeight: 1.5,
            opacity: sp(40),
          }}
        >
          "एक दृश्य में गरीब,<br />अगले में करोड़पति"
        </div>

        {/* Sparkle effects on film side */}
        {[0, 1, 2].map((i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: 100 + i * 150,
              top: 150 + i * 100,
              width: 8,
              height: 8,
              backgroundColor: '#ff0000',
              borderRadius: '50%',
              opacity: Math.abs(Math.sin(frame * 0.08 + i * 2)) * 0.5,
              boxShadow: '0 0 15px rgba(255,0,0,0.5)',
            }}
          />
        ))}
      </div>

      {/* CENTER - VS Divider */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) scale(${vsScale})`,
          zIndex: 10,
        }}
      >
        <div
          style={{
            width: 4,
            height: `${dividerH}%`,
            background: 'linear-gradient(180deg, transparent, #e9d700, transparent)',
            position: 'absolute',
            left: '50%',
            top: '50%',
            transform: 'translate(-50%, -50%)',
            minHeight: 400,
          }}
        />
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 48,
            fontWeight: 900,
            color: '#e9d700',
            textShadow: '0 0 30px rgba(233,215,0,0.5)',
            background: '#1C1C1C',
            padding: '15px 25px',
            borderRadius: 12,
            border: '2px solid #e9d700',
          }}
        >
          VS
        </div>
      </div>

      {/* RIGHT SIDE - Reality */}
      <div
        style={{
          position: 'absolute',
          right: 0,
          top: 0,
          width: '48%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          transform: `translateX(${interpolate(sp(30), [0, 1], [100, 0])}px)`,
          opacity: sp(30),
        }}
      >
        {/* Shield / quiet icon */}
        <svg width="120" height="120" viewBox="0 0 120 120" style={{ marginBottom: 30, opacity: 0.8 }}>
          <path
            d="M60,15 L95,35 L95,65 C95,85 75,105 60,110 C45,105 25,85 25,65 L25,35 Z"
            fill="none"
            stroke="#e9d700"
            strokeWidth="3"
          />
          <polyline points="42,60 55,73 78,48" fill="none" stroke="#e9d700" strokeWidth="4" />
        </svg>

        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 72,
            color: '#e9d700',
            textAlign: 'center',
            textShadow: '0 0 40px rgba(233,215,0,0.3)',
          }}
        >
          असल सच
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 36,
            color: '#9E9E9E',
            textAlign: 'center',
            marginTop: 20,
            maxWidth: 380,
            lineHeight: 1.5,
            opacity: sp(55),
          }}
        >
          "बड़ा फीका है,<br />बड़ा शांत है"
        </div>
      </div>

      {/* Stick figure - watching, neutral expression */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          bottom: 60,
          transform: `scale(${sp(60)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(60), [0, 1], [0, 0.9]),
        }}
      >
        <svg width="100" height="160" viewBox="0 0 100 160">
          <circle cx="50" cy="35" r="30" fill="rgba(233,215,0,0.06)" />
          <circle cx="50" cy="35" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="42" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="58" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="48" y1="33" x2="52" y2="33" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="50" y1="53" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="72" x2="28" y2="92" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="72" x2="72" y2="92" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="105" x2="32" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="105" x2="68" y2="145" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(
            frame,
            [0, 30, durationInFrames - 15, durationInFrames],
            [0, 0.3, 0.3, 0],
            { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
          ),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_FilmVsReality;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene3_HazaaronFaisle',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_HazaaronFaisle: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera tilt
  const cameraTilt = interpolate(frame, [0, durationInFrames], [-1, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Brick/block reveal animation - representing small decisions building up
  const totalBlocks = 24;
  const blocks = Array.from({ length: totalBlocks }, (_, i) => {
    const col = i % 6;
    const row = Math.floor(i / 6);
    const delay = 20 + i * 4;
    const scale = sp(delay, { damping: 14, stiffness: 200 });
    return { col, row, scale, delay };
  });

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => {
    const s1 = seededRandom(i + 300);
    const s2 = seededRandom(i + 400);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + Math.sin(frame * 0.03 + i) * 25 - frame * 0.3) % 1200,
      size: 2 + s1 * 3,
      op: interpolate(frame, [0, 20, durationInFrames - 15, durationInFrames], [0, 0.15 + s2 * 0.3, 0.15 + s2 * 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  // Shape layers
  const shapes = [
    { type: 'circle', x: 1700, y: 200, size: 280, rot: frame * 0.1 },
    { type: 'rect', x: 150, y: 850, size: 220, rot: -frame * 0.13 },
    { type: 'triangle', x: 1750, y: 800, size: 180, rot: frame * 0.16 },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${145 + frame * 0.1}deg, #1C1C1C 0%, #1e2a1e 40%, #2D2D2D 70%, #1C1C1C 100%)`,
          transform: `rotate(${cameraTilt}deg)`,
        }}
      />

      {/* Shape layers */}
      {shapes.map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `1.5px solid rgba(233,215,0,0.05)`,
            borderRadius: s.type === 'circle' ? '50%' : '6px',
            transform: `rotate(${s.rot}deg)`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
          }}
        />
      ))}

      {/* Motion texture - grid dots */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        {Array.from({ length: 15 }, (_, row) =>
          Array.from({ length: 20 }, (_, col) => (
            <div
              key={`${row}-${col}`}
              style={{
                position: 'absolute',
                left: col * 100 + 10,
                top: row * 80 + 10,
                width: 3,
                height: 3,
                borderRadius: '50%',
                backgroundColor: '#e9d700',
                opacity: Math.sin(frame * 0.02 + row + col) > 0.5 ? 1 : 0.3,
              }}
            />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 4 === 0 ? '#FFC107' : '#F5F5F5',
            opacity: p.op,
          }}
        />
      ))}

      {/* Top text - crossed out */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '10%',
          transform: `translate(-50%, ${interpolate(sp(5), [0, 1], [50, 0])}px)`,
          opacity: sp(5),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            color: '#ff0000',
            textDecoration: 'line-through',
            textDecorationThickness: 4,
            opacity: 0.7,
          }}
        >
          एक वीरतापूर्ण फैसला
        </div>
      </div>

      {/* Building blocks visualization */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '55%',
          transform: 'translate(-50%, -50%)',
        }}
      >
        {blocks.map((b, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: b.col * 105 - 300,
              top: -(b.row * 55) + 80,
              width: 95,
              height: 45,
              borderRadius: 6,
              background: `linear-gradient(135deg, rgba(233,215,0,${0.15 + (i / totalBlocks) * 0.4}), rgba(255,193,7,${0.1 + (i / totalBlocks) * 0.3}))`,
              border: '1.5px solid rgba(233,215,0,0.3)',
              transform: `scale(${b.scale})`,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <svg width="20" height="20" viewBox="0 0 20 20">
              <polyline points="4,10 8,14 16,6" fill="none" stroke="#e9d700" strokeWidth="2" />
            </svg>
          </div>
        ))}
      </div>

      {/* Main golden text */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '22%',
          transform: `translate(-50%, ${interpolate(sp(15), [0, 1], [60, 0])}px)`,
          opacity: sp(15),
          textAlign: 'center',
          maxWidth: 1400,
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 90,
            color: '#e9d700',
            lineHeight: 1.2,
            textShadow: '0 0 60px rgba(233,215,0,0.2)',
          }}
        >
          हज़ारों छोटे फैसले
        </div>
      </div>

      {/* Bottom subtitle */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: '12%',
          transform: `translate(-50%, ${interpolate(sp(60), [0, 1], [40, 0])}px)`,
          opacity: sp(60),
          textAlign: 'center',
          maxWidth: 1100,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            color: '#F5F5F5',
            lineHeight: 1.5,
            opacity: 0.8,
          }}
        >
          बिना तालियों के • बिना सुर्खी के • बिना किसी को पता चले
        </div>
      </div>

      {/* Stick figure - stacking blocks */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          bottom: 80,
          transform: `scale(${sp(45)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(45), [0, 1], [0, 1]),
        }}
      >
        <svg width="120" height="180" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="35" fill="rgba(233,215,0,0.06)" />
          <circle cx="60" cy="35" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="52" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="68" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="58" y1="33" x2="62" y2="33" stroke="#e9d700" strokeWidth="1.5" />
          {/* Smile */}
          <path d="M52,40 Q60,47 68,40" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="60" y1="53" x2="60" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arms raised holding block */}
          <line x1="60" y1="78" x2="35" y2="60" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="78" x2="85" y2="60" stroke="#F5F5F5" strokeWidth="2.5" />
          <rect x="35" y="50" width="50" height="15" rx="3" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="60" y1="115" x2="42" y2="160" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="115" x2="78" y2="160" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_HazaaronFaisle;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene4_DaulatBarbad',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_DaulatBarbad: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Screen shake on impact
  const shakeX = frame > 30 && frame < 50 ? Math.sin(frame * 2.5) * (50 - frame) * 0.3 : 0;
  const shakeY = frame > 30 && frame < 50 ? Math.cos(frame * 3) * (50 - frame) * 0.2 : 0;

  // Camera zoom out
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.12, 1.0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Crumbling coins animation
  const coins = Array.from({ length: 12 }, (_, i) => {
    const triggerFrame = 35 + i * 3;
    const fallProgress = interpolate(frame, [triggerFrame, triggerFrame + 60], [0, 1], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });
    const x = 400 + (i % 4) * 300 + seededRandom(i + 500) * 100;
    const startY = 300 + Math.floor(i / 4) * 150;
    const y = startY + fallProgress * fallProgress * 600;
    const rot = fallProgress * 360 * (i % 2 === 0 ? 1 : -1);
    const op = interpolate(fallProgress, [0, 0.8, 1], [0.8, 0.5, 0]);
    return { x, y, rot, op, size: 40 + seededRandom(i) * 20 };
  });

  // Particles
  const particles = Array.from({ length: 35 }, (_, i) => {
    const s1 = seededRandom(i + 600);
    const s2 = seededRandom(i + 700);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + frame * 0.5 + Math.sin(frame * 0.03 + i) * 15) % 1200,
      size: 2 + s1 * 3,
      op: interpolate(frame, [0, 25, durationInFrames - 15, durationInFrames], [0, 0.2 + s2 * 0.25, 0.2 + s2 * 0.25, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  // Warning stripes
  const stripeOffset = frame * 1.5;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient - darker, warning tone */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${170 + frame * 0.08}deg, #1C1C1C 0%, #2a1a1a 35%, #2D2D2D 65%, #1C1C1C 100%)`,
          transform: `scale(${cameraScale}) translate(${shakeX}px, ${shakeY}px)`,
        }}
      />

      {/* Shape layers */}
      {[
        { x: 250, y: 300, size: 350, rot: frame * 0.08 },
        { x: 1600, y: 200, size: 200, rot: -frame * 0.12 },
        { x: 1400, y: 800, size: 300, rot: frame * 0.1 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(255,0,0,0.05)`,
            borderRadius: i === 0 ? '50%' : '10px',
            transform: `rotate(${s.rot}deg)`,
          }}
        />
      ))}

      {/* Motion texture - warning diagonal stripes at bottom */}
      <div
        style={{
          position: 'absolute',
          bottom: 0,
          left: 0,
          width: '100%',
          height: 6,
          overflow: 'hidden',
          opacity: 0.4,
        }}
      >
        <div
          style={{
            width: '200%',
            height: '100%',
            background: `repeating-linear-gradient(90deg, #ff0000 0px, #ff0000 20px, transparent 20px, transparent 40px)`,
            transform: `translateX(${-stripeOffset % 40}px)`,
          }}
        />
      </div>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#ff0000' : '#F5F5F5',
            opacity: p.op,
          }}
        />
      ))}

      {/* Falling coins */}
      {coins.map((c, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: c.x,
            top: c.y,
            transform: `rotate(${c.rot}deg)`,
            opacity: c.op,
          }}
        >
          <svg width={c.size} height={c.size} viewBox="0 0 50 50">
            <circle cx="25" cy="25" r="22" fill="rgba(233,215,0,0.2)" stroke="#e9d700" strokeWidth="2" />
            <text x="17" y="32" fontSize="18" fill="#e9d700" fontFamily="Inter" fontWeight="700">
              ₹
            </text>
          </svg>
        </div>
      ))}

      {/* Main warning text */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '15%',
          transform: `translate(-50%, ${interpolate(sp(8), [0, 1], [60, 0])}px) translate(${shakeX}px, ${shakeY}px)`,
          opacity: sp(8),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 105,
            color: '#ff0000',
            lineHeight: 1.2,
            textShadow: '0 0 60px rgba(255,0,0,0.25)',
          }}
        >
          दौलत बरबाद
        </div>
      </div>

      {/* Subtitle */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '35%',
          transform: `translate(-50%, ${interpolate(sp(25), [0, 1], [40, 0])}px)`,
          opacity: sp(25),
          textAlign: 'center',
          maxWidth: 1200,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 50,
            color: '#F5F5F5',
            lineHeight: 1.5,
          }}
        >
          बड़े धमाके से <span style={{ color: '#ff0000', textDecoration: 'line-through' }}>नहीं</span>
        </div>
      </div>

      {/* Bottom reveal text */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: '15%',
          transform: `translate(-50%, ${interpolate(sp(55), [0, 1], [50, 0])}px)`,
          opacity: sp(55),
          textAlign: 'center',
          maxWidth: 1200,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 48,
            color: '#e9d700',
            lineHeight: 1.5,
          }}
        >
          छोटी-छोटी आदतों से... जो गलत लगती ही नहीं
        </div>
      </div>

      {/* Warning triangle icon */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '55%',
          transform: `translate(-50%, -50%) scale(${sp(40, { damping: 8 })})`,
        }}
      >
        <svg width="160" height="160" viewBox="0 0 160 160">
          <polygon
            points="80,15 150,140 10,140"
            fill="none"
            stroke="#ff0000"
            strokeWidth="4"
            opacity={0.6}
          />
          <text
            x="68"
            y="115"
            fontSize="60"
            fill="#ff0000"
            fontFamily="Inter"
            fontWeight="900"
          >
            !
          </text>
        </svg>
      </div>

      {/* Stick figure - worried */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          bottom: 70,
          transform: `scale(${sp(50)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(50), [0, 1], [0, 1]),
        }}
      >
        <svg width="110" height="170" viewBox="0 0 110 170">
          <circle cx="55" cy="35" r="32" fill="rgba(255,0,0,0.05)" />
          <circle cx="55" cy="35" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="47" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="63" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="53" y1="33" x2="57" y2="33" stroke="#e9d700" strokeWidth="1.5" />
          {/* Worried mouth */}
          <path d="M47,44 Q55,38 63,44" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          {/* Worry lines */}
          <line x1="43" y1="24" x2="48" y2="27" stroke="#F5F5F5" strokeWidth="1" opacity="0.5" />
          <line x1="67" y1="24" x2="62" y2="27" stroke="#F5F5F5" strokeWidth="1" opacity="0.5" />
          <line x1="55" y1="53" x2="55" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Hands on head */}
          <line x1="55" y1="78" x2="35" y2="55" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="55" y1="78" x2="75" y2="55" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="55" y1="110" x2="38" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="55" y1="110" x2="72" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_DaulatBarbad;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene5_MuhallaKhayee',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_MuhallaKhayee: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera orbit effect
  const orbitX = Math.sin(frame * 0.008) * 15;
  const orbitY = Math.cos(frame * 0.008) * 8;

  // Crowd of stick figures walking towards cliff
  const crowdFigures = Array.from({ length: 8 }, (_, i) => {
    const walkPhase = Math.sin(frame * 0.08 + i * 1.5) * 3;
    const startX = 200 + i * 130;
    const moveX = frame * 0.8;
    const x = startX + moveX;
    const y = 550 + walkPhase;
    const opacity = sp(10 + i * 5);
    return { x: x % 1700, y, opacity, walkPhase };
  });

  // Cliff/edge line
  const cliffX = 1400;

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => {
    const s1 = seededRandom(i + 800);
    const s2 = seededRandom(i + 900);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + Math.sin(frame * 0.025 + i) * 20) % 1150,
      size: 2 + s1 * 3,
      op: interpolate(frame, [0, 20, durationInFrames - 15, durationInFrames], [0, 0.15 + s2 * 0.25, 0.15 + s2 * 0.25, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${155 + frame * 0.06}deg, #1C1C1C 0%, #2D2D2D 30%, #1a1a28 65%, #1C1C1C 100%)`,
          transform: `translate(${orbitX}px, ${orbitY}px)`,
        }}
      />

      {/* Shape layers */}
      {[
        { type: 'circle', x: 300, y: 180, size: 320, rot: frame * 0.09, op: 0.04 },
        { type: 'rect', x: 1600, y: 350, size: 250, rot: -frame * 0.11, op: 0.05 },
        { type: 'circle', x: 800, y: 900, size: 280, rot: frame * 0.07, op: 0.03 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(233,215,0,${s.op})`,
            borderRadius: s.type === 'circle' ? '50%' : '8px',
            transform: `rotate(${s.rot}deg) scale(${1 + Math.sin(frame * 0.018) * 0.06})`,
          }}
        />
      ))}

      {/* Motion texture - horizontal scan lines */}
      <AbsoluteFill style={{ opacity: 0.02 }}>
        {Array.from({ length: 40 }, (_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: 0,
              top: i * 28 + (frame * 0.3) % 28,
              width: '100%',
              height: 1,
              backgroundColor: '#F5F5F5',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : '#9E9E9E',
            opacity: p.op,
          }}
        />
      ))}

      {/* Ground / Platform */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          top: 650,
          width: cliffX,
          height: 4,
          background: 'linear-gradient(90deg, transparent, #9E9E9E, #ff0000)',
          opacity: sp(15),
        }}
      />

      {/* Cliff drop */}
      <div
        style={{
          position: 'absolute',
          left: cliffX,
          top: 650,
          width: 4,
          height: 300,
          background: 'linear-gradient(180deg, #ff0000, transparent)',
          opacity: sp(15),
        }}
      />

      {/* Danger zone label */}
      <div
        style={{
          position: 'absolute',
          left: cliffX + 40,
          top: 680,
          fontFamily: 'Inter',
          fontSize: 28,
          fontWeight: 700,
          color: '#ff0000',
          opacity: sp(60) * 0.7,
          transform: `translateY(${interpolate(sp(60), [0, 1], [20, 0])}px)`,
        }}
      >
        आर्थिक खाई ↓
      </div>

      {/* Crowd stick figures */}
      {crowdFigures.map((fig, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: fig.x,
            top: fig.y,
            transform: `scale(${fig.opacity * 0.7})`,
            opacity: interpolate(fig.opacity, [0, 1], [0, 0.5 + i * 0.05]),
          }}
        >
          <svg width="50" height="100" viewBox="0 0 50 100">
            <circle cx="25" cy="12" r="10" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="25" y1="22" x2="25" y2="60" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="25" y1="35" x2="12" y2="50" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="25" y1="35" x2="38" y2="50" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="25" y1="60" x2="15" y2={85 + fig.walkPhase} stroke="#9E9E9E" strokeWidth="2" />
            <line x1="25" y1="60" x2="35" y2={85 - fig.walkPhase} stroke="#9E9E9E" strokeWidth="2" />
          </svg>
        </div>
      ))}

      {/* Main heading */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '10%',
          transform: `translate(-50%, ${interpolate(sp(5), [0, 1], [70, 0])}px)`,
          opacity: sp(5),
          textAlign: 'center',
          maxWidth: 1400,
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 95,
            color: '#F5F5F5',
            lineHeight: 1.2,
          }}
        >
          पूरा मुहल्ला <span style={{ color: '#ff0000' }}>खाई</span> में
        </div>
      </div>

      {/* Subtext */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '30%',
          transform: `translate(-50%, ${interpolate(sp(30), [0, 1], [40, 0])}px)`,
          opacity: sp(30),
          textAlign: 'center',
          maxWidth: 1100,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 48,
            color: '#e9d700',
            lineHeight: 1.5,
          }}
        >
          तो क्या उसके पीछे चलना समझदारी है?
        </div>
      </div>

      {/* One highlighted figure standing apart */}
      <div
        style={{
          position: 'absolute',
          left: 130,
          top: 480,
          transform: `scale(${sp(45)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(45), [0, 1], [0, 1]),
        }}
      >
        <svg width="80" height="140" viewBox="0 0 80 140">
          <circle cx="40" cy="25" r="28" fill="rgba(233,215,0,0.08)" />
          <circle cx="40" cy="25" r="14" fill="none" stroke="#e9d700" strokeWidth="2.5" />
          <circle cx="34" cy="23" r="5" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="46" cy="23" r="5" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="39" y1="23" x2="41" y2="23" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="40" y1="39" x2="40" y2="85" stroke="#e9d700" strokeWidth="2.5" />
          {/* Stop gesture */}
          <line x1="40" y1="55" x2="18" y2="45" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="18" y1="45" x2="15" y2="30" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="40" y1="55" x2="62" y2="65" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="40" y1="85" x2="28" y2="125" stroke="#e9d700" strokeWidth="2.5" />
          <line x1="40" y1="85" x2="52" y2="125" stroke="#e9d700" strokeWidth="2.5" />
          {/* Stop hand */}
          <rect x="8" y="22" width="14" height="14" rx="3" fill="none" stroke="#e9d700" strokeWidth="1.5" />
        </svg>
      </div>

      {/* Arrow pointing away */}
      <div
        style={{
          position: 'absolute',
          left: 200,
          top: 460,
          opacity: sp(70) * 0.6,
          transform: `scale(${sp(70)})`,
        }}
      >
        <svg width="80" height="40" viewBox="0 0 80 40">
          <line x1="0" y1="20" x2="55" y2="20" stroke="#e9d700" strokeWidth="3" />
          <polyline points="45,10 60,20 45,30" fill="none" stroke="#e9d700" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_MuhallaKhayee;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene6_DikhawaVsAzaadi',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_DikhawaVsAzaadi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.08], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 35 }, (_, i) => {
    const s1 = seededRandom(i + 1000);
    const s2 = seededRandom(i + 1100);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + Math.sin(frame * 0.02 + i * 0.8) * 18) % 1150,
      size: 2 + s1 * 4,
      op: interpolate(frame, [0, 25, durationInFrames - 15, durationInFrames], [0, 0.15 + s2 * 0.3, 0.15 + s2 * 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  // Showcase items - icons of material things
  const materialItems = [
    { label: 'नई गाड़ी', x: 250, y: 350, delay: 15 },
    { label: 'बड़ा घर', x: 650, y: 320, delay: 25 },
    { label: 'महंगा फ़ोन', x: 1050, y: 340, delay: 35 },
  ];

  // Cross animation
  const crossDelay = 80;
  const crossProgress = sp(crossDelay, { damping: 10 });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${140 + frame * 0.12}deg, #1C1C1C 0%, #2D2D2D 25%, #251a2d 55%, #1C1C1C 100%)`,
          transform: `scale(${camScale})`,
        }}
      />

      {/* Shape layers */}
      {[
        { type: 'circle', x: 1700, y: 150, size: 300, rot: frame * 0.1 },
        { type: 'rect', x: 200, y: 850, size: 250, rot: -frame * 0.08 },
        { type: 'triangle', x: 950, y: 100, size: 200, rot: frame * 0.14 },
        { type: 'circle', x: 400, y: 200, size: 180, rot: -frame * 0.12 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `1.5px solid rgba(233,215,0,0.04)`,
            borderRadius: s.type === 'circle' ? '50%' : '8px',
            transform: `rotate(${s.rot}deg)`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
          }}
        />
      ))}

      {/* Motion texture - hexagonal grid */}
      <AbsoluteFill style={{ opacity: 0.02 }}>
        {Array.from({ length: 12 }, (_, row) =>
          Array.from({ length: 10 }, (_, col) => (
            <div
              key={`${row}-${col}`}
              style={{
                position: 'absolute',
                left: col * 200 + (row % 2) * 100,
                top: row * 100,
                width: 60,
                height: 60,
                border: '1px solid #e9d700',
                clipPath: 'polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%)',
              }}
            />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 4 === 0 ? '#FFC107' : i % 4 === 1 ? '#e9d700' : '#F5F5F5',
            opacity: p.op,
          }}
        />
      ))}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '8%',
          transform: `translate(-50%, ${interpolate(sp(5), [0, 1], [60, 0])}px)`,
          opacity: sp(5),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 90,
            color: '#F5F5F5',
            lineHeight: 1.2,
          }}
        >
          सबसे बड़ी <span style={{ color: '#ff0000' }}>गलतफ़हमी</span>
        </div>
      </div>

      {/* Subtitle */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '22%',
          transform: `translate(-50%, ${interpolate(sp(18), [0, 1], [30, 0])}px)`,
          opacity: sp(18),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            color: '#9E9E9E',
            lineHeight: 1.4,
          }}
        >
          पैसा = दिखावा?
        </div>
      </div>

      {/* Material items with cross-out */}
      {materialItems.map((item, i) => {
        const itemSp = sp(item.delay);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: item.x,
              top: item.y,
              transform: `scale(${itemSp})`,
              textAlign: 'center',
            }}
          >
            {/* Card */}
            <div
              style={{
                width: 260,
                height: 180,
                background: 'linear-gradient(145deg, rgba(45,45,45,0.9), rgba(28,28,28,0.9))',
                border: '2px solid rgba(255,0,0,0.3)',
                borderRadius: 16,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                position: 'relative',
                overflow: 'hidden',
              }}
            >
              {/* Icon */}
              <svg width="60" height="60" viewBox="0 0 60 60" style={{ marginBottom: 10 }}>
                {i === 0 && (
                  <g fill="none" stroke="#9E9E9E" strokeWidth="2">
                    <rect x="8" y="25" width="44" height="22" rx="4" />
                    <circle cx="18" cy="47" r="6" />
                    <circle cx="42" cy="47" r="6" />
                    <line x1="15" y1="25" x2="22" y2="15" />
                    <line x1="22" y1="15" x2="45" y2="15" />
                    <line x1="45" y1="15" x2="52" y2="25" />
                  </g>
                )}
                {i === 1 && (
                  <g fill="none" stroke="#9E9E9E" strokeWidth="2">
                    <path d="M30,8 L55,28 L55,55 L5,55 L5,28 Z" />
                    <rect x="20" y="35" width="20" height="20" />
                  </g>
                )}
                {i === 2 && (
                  <g fill="none" stroke="#9E9E9E" strokeWidth="2">
                    <rect x="15" y="5" width="30" height="50" rx="6" />
                    <circle cx="30" cy="48" r="3" />
                    <line x1="22" y1="11" x2="38" y2="11" />
                  </g>
                )}
              </svg>

              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 32,
                  color: '#9E9E9E',
                }}
              >
                {item.label}
              </div>

              {/* Red cross overlay */}
              <div
                style={{
                  position: 'absolute',
                  top: 0,
                  left: 0,
                  width: '100%',
                  height: '100%',
                  opacity: crossProgress,
                }}
              >
                <svg width="260" height="180" viewBox="0 0 260 180">
                  <line
                    x1="20"
                    y1="20"
                    x2={20 + 220 * crossProgress}
                    y2={20 + 140 * crossProgress}
                    stroke="#ff0000"
                    strokeWidth="5"
                    strokeLinecap="round"
                  />
                  <line
                    x1="240"
                    y1="20"
                    x2={240 - 220 * crossProgress}
                    y2={20 + 140 * crossProgress}
                    stroke="#ff0000"
                    strokeWidth="5"
                    strokeLinecap="round"
                  />
                </svg>
              </div>
            </div>
          </div>
        );
      })}

      {/* "पड़ोसी को दिखाने के लिए" */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '62%',
          transform: `translate(-50%, ${interpolate(sp(50), [0, 1], [40, 0])}px)`,
          opacity: sp(50),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 42,
            color: '#ff0000',
            opacity: 0.8,
            fontStyle: 'italic',
          }}
        >
          "सब पड़ोसी को दिखाने के लिए"
        </div>
      </div>

      {/* Bottom golden text - real meaning */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: '10%',
          transform: `translate(-50%, ${interpolate(sp(70), [0, 1], [50, 0])}px)`,
          opacity: sp(70),
          textAlign: 'center',
          maxWidth: 1200,
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 68,
            color: '#e9d700',
            lineHeight: 1.3,
            textShadow: '0 0 40px rgba(233,215,0,0.2)',
          }}
        >
          पैसे का असली मतलब = <span style={{ color: '#F5F5F5' }}>आज़ादी</span>
        </div>
      </div>

      {/* Stick figure with golden shield */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          bottom: 100,
          transform: `scale(${sp(55)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(55), [0, 1], [0, 1]),
        }}
      >
        <svg width="130" height="190" viewBox="0 0 130 190">
          <circle cx="65" cy="40" r="35" fill="rgba(233,215,0,0.06)" />
          <circle cx="65" cy="40" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="57" cy="38" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="73" cy="38" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="63" y1="38" x2="67" y2="38" stroke="#e9d700" strokeWidth="1.5" />
          <path d="M57,48 Q65,54 73,48" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="65" y1="60" x2="65" y2="125" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="65" y1="85" x2="40" y2="105" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="65" y1="85" x2="95" y2="75" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Shield in hand */}
          <path d="M95,60 L115,70 L115,90 C115,105 100,115 95,118 C90,115 75,105 75,90 L75,70 Z" fill="rgba(233,215,0,0.15)" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="65" y1="125" x2="48" y2="170" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="65" y1="125" x2="82" y2="170" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_DikhawaVsAzaadi;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene7_PaisaAzaadi',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_PaisaAzaadi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow drift upward
  const camY = interpolate(frame, [0, durationInFrames], [10, -10], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Four pillars of freedom
  const pillars = [
    { icon: '⏰', label: 'अपने वक्त पर हक़', delay: 20 },
    { icon: '✋', label: 'ना कहने की ताक़त', delay: 35 },
    { icon: '😴', label: 'बेफ़िक्र नींद', delay: 50 },
    { icon: '🛡️', label: 'सुरक्षा कवच', delay: 65 },
  ];

  // Golden glow pulse
  const glowPulse = 0.15 + Math.sin(frame * 0.04) * 0.05;

  // Particles
  const particles = Array.from({ length: 40 }, (_, i) => {
    const s1 = seededRandom(i + 1200);
    const s2 = seededRandom(i + 1300);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + Math.sin(frame * 0.02 + i * 0.6) * 25 - frame * 0.3) % 1200,
      size: 2 + s1 * 4,
      op: interpolate(frame, [0, 20, durationInFrames - 15, durationInFrames], [0, 0.2 + s2 * 0.3, 0.2 + s2 * 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient - warm golden tone */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${150 + frame * 0.08}deg, #1C1C1C 0%, #2a2510 30%, #2D2D2D 60%, #1C1C1C 100%)`,
          transform: `translateY(${camY}px)`,
        }}
      />

      {/* Central golden glow */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: 'translate(-50%, -50%)',
          width: 800,
          height: 800,
          borderRadius: '50%',
          background: `radial-gradient(circle, rgba(233,215,0,${glowPulse}) 0%, transparent 70%)`,
        }}
      />

      {/* Shape layers */}
      {[
        { type: 'circle', x: 200, y: 150, size: 280, rot: frame * 0.09 },
        { type: 'rect', x: 1700, y: 800, size: 220, rot: -frame * 0.11 },
        { type: 'triangle', x: 150, y: 850, size: 180, rot: frame * 0.15 },
        { type: 'circle', x: 1650, y: 200, size: 250, rot: -frame * 0.07 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(233,215,0,0.05)`,
            borderRadius: s.type === 'circle' ? '50%' : '8px',
            transform: `rotate(${s.rot}deg) scale(${1 + Math.sin(frame * 0.015 + i) * 0.06})`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
          }}
        />
      ))}

      {/* Motion texture - radiating lines from center */}
      <AbsoluteFill style={{ opacity: 0.025 }}>
        {Array.from({ length: 24 }, (_, i) => {
          const angle = (i / 24) * 360;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: '50%',
                top: '50%',
                width: 1,
                height: 500 + Math.sin(frame * 0.02 + i) * 50,
                backgroundColor: '#e9d700',
                transformOrigin: '0 0',
                transform: `rotate(${angle}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#FFC107' : '#F5F5F5',
            opacity: p.op,
          }}
        />
      ))}

      {/* Main title */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '8%',
          transform: `translate(-50%, ${interpolate(sp(5), [0, 1], [60, 0])}px)`,
          opacity: sp(5),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 110,
            color: '#e9d700',
            lineHeight: 1.2,
            textShadow: '0 0 80px rgba(233,215,0,0.3)',
          }}
        >
          आज़ादी
        </div>
      </div>

      {/* Four pillar cards */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '45%',
          transform: 'translate(-50%, -50%)',
          display: 'flex',
          gap: 40,
        }}
      >
        {pillars.map((p, i) => {
          const pSp = sp(p.delay, { damping: 10, stiffness: 100 });
          const float = Math.sin(frame * 0.03 + i * 1.5) * 6;
          return (
            <div
              key={i}
              style={{
                width: 280,
                height: 220,
                background: 'linear-gradient(160deg, rgba(45,45,45,0.85), rgba(28,28,28,0.9))',
                border: '2px solid rgba(233,215,0,0.25)',
                borderRadius: 20,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                transform: `scale(${pSp}) translateY(${float}px)`,
                boxShadow: '0 10px 40px rgba(0,0,0,0.4), inset 0 1px 0 rgba(233,215,0,0.1)',
              }}
            >
              {/* SVG Icons */}
              <svg width="65" height="65" viewBox="0 0 65 65" style={{ marginBottom: 15 }}>
                {i === 0 && (
                  <g fill="none" stroke="#e9d700" strokeWidth="2.5">
                    <circle cx="32" cy="32" r="26" />
                    <line x1="32" y1="14" x2="32" y2="32" />
                    <line x1="32" y1="32" x2="44" y2="38" />
                    <circle cx="32" cy="32" r="3" fill="#e9d700" />
                  </g>
                )}
                {i === 1 && (
                  <g fill="none" stroke="#e9d700" strokeWidth="2.5">
                    <rect x="18" y="8" width="30" height="48" rx="4" />
                    <line x1="25" y1="22" x2="42" y2="22" />
                    <line x1="25" y1="30" x2="42" y2="30" />
                    <line x1="25" y1="38" x2="35" y2="38" />
                    <polyline points="28,44 33,49 43,39" />
                  </g>
                )}
                {i === 2 && (
                  <g fill="none" stroke="#e9d700" strokeWidth="2.5">
                    <path d="M10,42 Q32,15 55,42" />
                    <circle cx="32" cy="28" r="8" />
                    <line x1="20" y1="52" x2="45" y2="52" />
                  </g>
                )}
                {i === 3 && (
                  <g fill="none" stroke="#e9d700" strokeWidth="2.5">
                    <path d="M32,5 L58,20 L58,40 C58,52 42,60 32,64 C22,60 6,52 6,40 L6,20 Z" />
                    <polyline points="20,34 28,42 44,26" />
                  </g>
                )}
              </svg>

              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 30,
                  color: '#F5F5F5',
                  textAlign: 'center',
                  lineHeight: 1.4,
                  padding: '0 15px',
                }}
              >
                {p.label}
              </div>
            </div>
          );
        })}
      </div>

      {/* Bottom tagline */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: '10%',
          transform: `translate(-50%, ${interpolate(sp(80), [0, 1], [40, 0])}px)`,
          opacity: sp(80),
          textAlign: 'center',
          maxWidth: 1300,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 46,
            color: '#F5F5F5',
            lineHeight: 1.5,
            opacity: 0.85,
          }}
        >
          पैसा <span style={{ color: '#ff0000', textDecoration: 'line-through' }}>गाड़ी</span> के लिए नहीं, <span style={{ color: '#e9d700' }}>सुकून</span> के लिए
        </div>
      </div>

      {/* Stick figure - relaxed, confident */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          bottom: 80,
          transform: `scale(${sp(45)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(45), [0, 1], [0, 1]),
        }}
      >
        <svg width="120" height="180" viewBox="0 0 120 180">
          <circle cx="60" cy="38" r="35" fill="rgba(233,215,0,0.06)" />
          <circle
            cx="60"
            cy="38"
            r="20"
            fill="none"
            stroke="#F5F5F5"
            strokeWidth="2.5"
            transform={`rotate(${Math.sin(frame * 0.04) * 3}, 60, 38)`}
          />
          <circle cx="52" cy="36" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="68" cy="36" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="58" y1="36" x2="62" y2="36" stroke="#e9d700" strokeWidth="1.5" />
          {/* Smile */}
          <path d="M52,46 Q60,53 68,46" fill="none" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="60" y1="58" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Relaxed arms */}
          <line x1="60" y1="80" x2="35" y2="100" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="80" x2="85" y2="100" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="120" x2="42" y2="165" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="120" x2="78" y2="165" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_PaisaAzaadi;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene8_ChintaNahi',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_ChintaNahi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera zoom in slowly
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.12], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Three crisis scenarios appearing one after another
  const crises = [
    { text: 'गाड़ी खराब हो जाए', color: '#ff0000', delay: 15, icon: 'car' },
    { text: 'बाज़ार 20% गिर जाए', color: '#ff0000', delay: 40, icon: 'chart' },
    { text: 'अनचाहा खर्चा आ जाए', color: '#ff0000', delay: 65, icon: 'bill' },
  ];

  // Peace reveal
  const peaceReveal = sp(100, { damping: 8, stiffness: 80 });

  // Particles
  const particles = Array.from({ length: 35 }, (_, i) => {
    const s1 = seededRandom(i + 1400);
    const s2 = seededRandom(i + 1500);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + Math.sin(frame * 0.025 + i) * 20) % 1150,
      size: 2 + s1 * 3.5,
      op: interpolate(frame, [0, 25, durationInFrames - 15, durationInFrames], [0, 0.2 + s2 * 0.25, 0.2 + s2 * 0.25, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  // Heartbeat line
  const heartbeatProgress = interpolate(frame, [90, 180], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${160 + frame * 0.06}deg, #1C1C1C 0%, #1a2a1a 35%, #2D2D2D 65%, #1C1C1C 100%)`,
          transform: `scale(${camScale})`,
        }}
      />

      {/* Shape layers */}
      {[
        { type: 'circle', x: 350, y: 850, size: 320, rot: frame * 0.07 },
        { type: 'rect', x: 1600, y: 150, size: 200, rot: -frame * 0.1 },
        { type: 'triangle', x: 200, y: 200, size: 250, rot: frame * 0.13 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(233,215,0,0.04)`,
            borderRadius: s.type === 'circle' ? '50%' : '8px',
            transform: `rotate(${s.rot}deg)`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
          }}
        />
      ))}

      {/* Motion texture - zigzag pattern */}
      <AbsoluteFill style={{ opacity: 0.02 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 15 }, (_, i) => {
            const y = i * 80 + 20;
            const points = Array.from({ length: 40 }, (_, j) => {
              const px = j * 50;
              const py = y + (j % 2 === 0 ? -15 : 15);
              return `${px},${py}`;
            }).join(' ');
            return (
              <polyline
                key={i}
                points={points}
                fill="none"
                stroke="#e9d700"
                strokeWidth="0.5"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : '#F5F5F5',
            opacity: p.op,
          }}
        />
      ))}

      {/* Crisis scenarios - stacked vertically left side */}
      {crises.map((crisis, i) => {
        const cSp = sp(crisis.delay, { damping: 10 });
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: 100,
              top: 180 + i * 200,
              display: 'flex',
              alignItems: 'center',
              gap: 25,
              transform: `translateX(${interpolate(cSp, [0, 1], [-200, 0])}px)`,
              opacity: cSp,
            }}
          >
            {/* Crisis icon */}
            <div
              style={{
                width: 90,
                height: 90,
                borderRadius: '50%',
                border: `3px solid ${crisis.color}`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                background: `rgba(255,0,0,0.08)`,
              }}
            >
              <svg width="45" height="45" viewBox="0 0 45 45">
                {crisis.icon === 'car' && (
                  <g fill="none" stroke="#ff0000" strokeWidth="2">
                    <rect x="5" y="18" width="35" height="14" rx="3" />
                    <circle cx="13" cy="32" r="4" />
                    <circle cx="32" cy="32" r="4" />
                    <line x1="8" y1="18" x2="14" y2="8" />
                    <line x1="14" y1="8" x2="35" y2="8" />
                    <line x1="35" y1="8" x2="40" y2="18" />
                    {/* X mark */}
                    <line x1="16" y1="12" x2="28" y2="22" stroke="#ff0000" strokeWidth="2.5" />
                    <line x1="28" y1="12" x2="16" y2="22" stroke="#ff0000" strokeWidth="2.5" />
                  </g>
                )}
                {crisis.icon === 'chart' && (
                  <g fill="none" stroke="#ff0000" strokeWidth="2">
                    <polyline points="5,10 15,20 25,12 40,38" />
                    <line x1="5" y1="38" x2="40" y2="38" />
                    <text x="15" y="35" fontSize="14" fill="#ff0000" fontFamily="Inter" fontWeight="700">-20%</text>
                  </g>
                )}
                {crisis.icon === 'bill' && (
                  <g fill="none" stroke="#ff0000" strokeWidth="2">
                    <rect x="8" y="5" width="28" height="35" rx="3" />
                    <line x1="14" y1="14" x2="30" y2="14" />
                    <line x1="14" y1="20" x2="30" y2="20" />
                    <line x1="14" y1="26" x2="24" y2="26" />
                    <text x="20" y="36" fontSize="10" fill="#ff0000" fontFamily="Inter">₹₹₹</text>
                  </g>
                )}
              </svg>
            </div>

            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 42,
                color: '#ff0000',
                lineHeight: 1.3,
                opacity: 0.9,
              }}
            >
              {crisis.text}
            </div>
          </div>
        );
      })}

      {/* Right side - calm face */}
      <div
        style={{
          position: 'absolute',
          right: 150,
          top: '50%',
          transform: `translate(0, -50%) scale(${peaceReveal})`,
          textAlign: 'center',
        }}
      >
        {/* Large calm face */}
        <svg width="250" height="250" viewBox="0 0 250 250">
          <circle cx="125" cy="125" r="100" fill="rgba(233,215,0,0.08)" stroke="#e9d700" strokeWidth="3" />
          {/* Eyes - peaceful/closed */}
          <path d="M80,110 Q95,100 110,110" fill="none" stroke="#e9d700" strokeWidth="3" />
          <path d="M140,110 Q155,100 170,110" fill="none" stroke="#e9d700" strokeWidth="3" />
          {/* Peaceful smile */}
          <path d="M90,145 Q125,175 160,145" fill="none" stroke="#e9d700" strokeWidth="3" />
          {/* Golden glow */}
          <circle cx="125" cy="125" r="110" fill="none" stroke="rgba(233,215,0,0.1)" strokeWidth="20" />
        </svg>

        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 50,
            color: '#e9d700',
            marginTop: 20,
            textShadow: '0 0 30px rgba(233,215,0,0.3)',
          }}
        >
          चिंता = शून्य
        </div>
      </div>

      {/* Flat heartbeat line at bottom */}
      <div
        style={{
          position: 'absolute',
          bottom: 120,
          left: '10%',
          width: '80%',
          height: 60,
          opacity: sp(90) * 0.6,
        }}
      >
        <svg width="100%" height="60" viewBox="0 0 1536 60" preserveAspectRatio="none">
          <polyline
            points={`0,30 ${200 * heartbeatProgress},30 ${250 * heartbeatProgress},30 ${400 * heartbeatProgress},30 ${1536 * heartbeatProgress},30`}
            fill="none"
            stroke="#e9d700"
            strokeWidth="3"
          />
        </svg>
        <div
          style={{
            position: 'absolute',
            right: 0,
            top: 5,
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            color: '#e9d700',
            opacity: heartbeatProgress,
          }}
        >
          ♡ STABLE
        </div>
      </div>

      {/* Stick figure - Buffett with golden glow, calm */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: 70,
          transform: `translate(-50%, 0) scale(${sp(50)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(50), [0, 1], [0, 1]),
        }}
      >
        <svg width="100" height="160" viewBox="0 0 100 160">
          <circle cx="50" cy="30" r="28" fill="rgba(233,215,0,0.06)" />
          <circle cx="50" cy="30" r="16" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="43" cy="28" r="5" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="57" cy="28" r="5" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="48" y1="28" x2="52" y2="28" stroke="#e9d700" strokeWidth="1.5" />
          <path d="M43,37 Q50,43 57,37" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="50" y1="46" x2="50" y2="100" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="65" x2="30" y2="85" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="65" x2="70" y2="85" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="100" x2="35" y2="140" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="100" x2="65" y2="140" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_ChintaNahi;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene9_CompoundInterest',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_CompoundInterest: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera orbit
  const orbitX = Math.sin(frame * 0.007) * 20;

  // Exponential curve animation
  const curveProgress = interpolate(frame, [40, 250], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Generate exponential curve points
  const curvePoints: string[] = [];
  const numPoints = Math.floor(curveProgress * 80);
  for (let i = 0; i <= numPoints; i++) {
    const x = 200 + (i / 80) * 1200;
    const t = i / 80;
    const y = 700 - Math.pow(t, 2.5) * 550;
    curvePoints.push(`${x},${y}`);
  }

  // Sword vs snake duality
  const swordScale = sp(20, { damping: 8, stiffness: 100 });
  const snakeScale = sp(40, { damping: 8, stiffness: 100 });

  // Particles
  const particles = Array.from({ length: 35 }, (_, i) => {
    const s1 = seededRandom(i + 1600);
    const s2 = seededRandom(i + 1700);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + Math.sin(frame * 0.02 + i) * 18 - frame * 0.2) % 1200,
      size: 2 + s1 * 3.5,
      op: interpolate(frame, [0, 25, durationInFrames - 15, durationInFrames], [0, 0.15 + s2 * 0.3, 0.15 + s2 * 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  // Center split glow
  const glowPhase = Math.sin(frame * 0.03);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient - split color */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${90 + frame * 0.1}deg, #1a2a1a 0%, #1C1C1C 40%, #2a1a1a 100%)`,
          transform: `translateX(${orbitX}px)`,
        }}
      />

      {/* Shape layers */}
      {[
        { type: 'circle', x: 250, y: 200, size: 300, rot: frame * 0.08 },
        { type: 'rect', x: 1650, y: 250, size: 250, rot: -frame * 0.1 },
        { type: 'circle', x: 950, y: 900, size: 350, rot: frame * 0.06 },
        { type: 'triangle', x: 1500, y: 850, size: 200, rot: -frame * 0.14 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(${i < 2 ? '233,215,0' : '255,0,0'},0.04)`,
            borderRadius: s.type === 'circle' ? '50%' : '8px',
            transform: `rotate(${s.rot}deg)`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
          }}
        />
      ))}

      {/* Motion texture - circuit-like lines */}
      <AbsoluteFill style={{ opacity: 0.02 }}>
        {Array.from({ length: 10 }, (_, i) => (
          <React.Fragment key={i}>
            <div
              style={{
                position: 'absolute',
                left: 0,
                top: 100 + i * 110,
                width: 300 + i * 50,
                height: 1,
                backgroundColor: '#e9d700',
              }}
            />
            <div
              style={{
                position: 'absolute',
                left: 300 + i * 50,
                top: 100 + i * 110,
                width: 1,
                height: 110,
                backgroundColor: '#e9d700',
              }}
            />
          </React.Fragment>
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#ff0000' : '#F5F5F5',
            opacity: p.op,
          }}
        />
      ))}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '5%',
          transform: `translate(-50%, ${interpolate(sp(5), [0, 1], [70, 0])}px)`,
          opacity: sp(5),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 100,
            color: '#e9d700',
            lineHeight: 1.2,
            textShadow: '0 0 60px rgba(233,215,0,0.3)',
          }}
        >
          चक्रवृद्धि ब्याज
        </div>
      </div>

      {/* Duality - Sword (left) and Snake (right) */}
      <div
        style={{
          position: 'absolute',
          left: 150,
          top: '25%',
          transform: `scale(${swordScale})`,
          textAlign: 'center',
        }}
      >
        <svg width="120" height="160" viewBox="0 0 120 160">
          {/* Sword / Shield - weapon */}
          <line x1="60" y1="10" x2="60" y2="120" stroke="#e9d700" strokeWidth="4" />
          <polygon points="50,10 60,0 70,10" fill="#e9d700" />
          <line x1="40" y1="40" x2="80" y2="40" stroke="#e9d700" strokeWidth="4" />
          <circle cx="60" cy="130" r="10" fill="none" stroke="#e9d700" strokeWidth="2" />
        </svg>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 42,
            color: '#e9d700',
            marginTop: 10,
          }}
        >
          हथियार
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          right: 150,
          top: '25%',
          transform: `scale(${snakeScale})`,
          textAlign: 'center',
        }}
      >
        <svg width="120" height="160" viewBox="0 0 120 160">
          {/* Snake / danger */}
          <path
            d={`M30,140 Q20,100 40,80 Q60,60 40,40 Q20,20 40,10 Q${50 + Math.sin(frame * 0.08) * 8},5 60,10`}
            fill="none"
            stroke="#ff0000"
            strokeWidth="4"
          />
          <circle cx="60" cy="10" r="8" fill="none" stroke="#ff0000" strokeWidth="2" />
          {/* Eyes */}
          <circle cx="57" cy="8" r="2" fill="#ff0000" />
          <circle cx="63" cy="8" r="2" fill="#ff0000" />
          {/* Tongue */}
          <line x1="60" y1="18" x2="60" y2="25" stroke="#ff0000" strokeWidth="1.5" />
          <line x1="58" y1="25" x2="60" y2="22" stroke="#ff0000" strokeWidth="1" />
          <line x1="62" y1="25" x2="60" y2="22" stroke="#ff0000" strokeWidth="1" />
        </svg>
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 42,
            color: '#ff0000',
            marginTop: 10,
          }}
        >
          दुश्मन
        </div>
      </div>

      {/* OR text in center */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '38%',
          transform: `translate(-50%, -50%) scale(${sp(30)})`,
          fontFamily: 'Inter',
          fontSize: 48,
          fontWeight: 900,
          color: '#F5F5F5',
          textShadow: '0 0 20px rgba(245,245,245,0.2)',
        }}
      >
        या
      </div>

      {/* Exponential curve */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          top: 0,
          width: '100%',
          height: '100%',
          opacity: sp(50),
        }}
      >
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {/* Axis lines */}
          <line x1="200" y1="700" x2="1500" y2="700" stroke="#9E9E9E" strokeWidth="2" opacity="0.4" />
          <line x1="200" y1="700" x2="200" y2="100" stroke="#9E9E9E" strokeWidth="2" opacity="0.4" />

          {/* Curve */}
          {curvePoints.length > 1 && (
            <polyline
              points={curvePoints.join(' ')}
              fill="none"
              stroke="#e9d700"
              strokeWidth="4"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          )}

          {/* Glow on tip */}
          {curvePoints.length > 0 && (
            <circle
              cx={parseFloat(curvePoints[curvePoints.length - 1]?.split(',')[0] || '0')}
              cy={parseFloat(curvePoints[curvePoints.length - 1]?.split(',')[1] || '0')}
              r="8"
              fill="#e9d700"
              opacity={0.8}
            >
            </circle>
          )}

          {/* Labels */}
          <text x="820" y="750" fontSize="28" fill="#9E9E9E" fontFamily="Noto Sans Devanagari" textAnchor="middle">
            समय →
          </text>
          <text x="170" y="400" fontSize="28" fill="#9E9E9E" fontFamily="Noto Sans Devanagari" textAnchor="middle" transform="rotate(-90, 170, 400)">
            पूँजी →
          </text>
        </svg>
      </div>

      {/* Bottom text */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: '6%',
          transform: `translate(-50%, ${interpolate(sp(80), [0, 1], [40, 0])}px)`,
          opacity: sp(80),
          textAlign: 'center',
          maxWidth: 1200,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            color: '#F5F5F5',
            lineHeight: 1.5,
          }}
        >
          समय के साथ <span style={{ color: '#e9d700' }}>चमत्कार</span> या <span style={{ color: '#ff0000' }}>तबाही</span>
        </div>
      </div>

      {/* Stick figure - pointing at curve */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          bottom: 100,
          transform: `scale(${sp(55)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(55), [0, 1], [0, 1]),
        }}
      >
        <svg width="100" height="150" viewBox="0 0 100 150">
          <circle cx="50" cy="28" r="25" fill="rgba(233,215,0,0.06)" />
          <circle cx="50" cy="28" r="15" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="44" cy="26" r="5" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="56" cy="26" r="5" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="49" y1="26" x2="51" y2="26" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="50" y1="43" x2="50" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Pointing arm */}
          <line x1="50" y1="62" x2="80" y2="45" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="62" x2="25" y2="75" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="95" x2="35" y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="50" y1="95" x2="65" y2="135" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 40%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_CompoundInterest;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene10_KhokhlaAadatein',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_KhokhlaAadatein: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Screen shake for impact
  const shakeIntensity = frame > 20 && frame < 45 ? (45 - frame) * 0.2 : 0;
  const shakeX = Math.sin(frame * 3) * shakeIntensity;
  const shakeY = Math.cos(frame * 4) * shakeIntensity * 0.6;

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1.0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Hollow tree trunk / structure crumbling
  const crumbleProgress = interpolate(frame, [60, 200], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Termite-like dots eating away
  const termites = Array.from({ length: 20 }, (_, i) => {
    const angle = (i / 20) * Math.PI * 2 + frame * 0.02;
    const radius = 120 + Math.sin(frame * 0.03 + i) * 20;
    const x = 960 + Math.cos(angle) * radius;
    const y = 500 + Math.sin(angle) * radius;
    const size = 4 + seededRandom(i + 2000) * 4;
    const op = interpolate(frame, [40 + i * 3, 50 + i * 3, durationInFrames - 15, durationInFrames], [0, 0.7, 0.7, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
    return { x, y, size, op };
  });

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => {
    const s1 = seededRandom(i + 1800);
    const s2 = seededRandom(i + 1900);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + Math.sin(frame * 0.02 + i) * 15 + frame * 0.3) % 1200,
      size: 2 + s1 * 3,
      op: interpolate(frame, [0, 20, durationInFrames - 15, durationInFrames], [0, 0.15 + s2 * 0.2, 0.15 + s2 * 0.2, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient - dark ominous */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${175 + frame * 0.05}deg, #1C1C1C 0%, #2a1515 30%, #2D2D2D 60%, #1C1C1C 100%)`,
          transform: `scale(${camScale}) translate(${shakeX}px, ${shakeY}px)`,
        }}
      />

      {/* Shape layers */}
      {[
        { type: 'circle', x: 350, y: 180, size: 300, rot: frame * 0.07 },
        { type: 'rect', x: 1600, y: 800, size: 230, rot: -frame * 0.09 },
        { type: 'triangle', x: 1700, y: 200, size: 200, rot: frame * 0.12 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(255,0,0,0.04)`,
            borderRadius: s.type === 'circle' ? '50%' : '8px',
            transform: `rotate(${s.rot}deg)`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
          }}
        />
      ))}

      {/* Motion texture - cracking pattern */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080" viewBox="0 0 1920 1080">
          {Array.from({ length: 8 }, (_, i) => {
            const sx = 400 + seededRandom(i + 50) * 1100;
            const sy = 200 + seededRandom(i + 60) * 600;
            const ex = sx + (seededRandom(i + 70) - 0.5) * 200;
            const ey = sy + seededRandom(i + 80) * 200;
            const crackOp = interpolate(frame, [30 + i * 10, 50 + i * 10], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <line
                key={i}
                x1={sx}
                y1={sy}
                x2={sx + (ex - sx) * crackOp}
                y2={sy + (ey - sy) * crackOp}
                stroke="#ff0000"
                strokeWidth="1"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#ff0000' : '#9E9E9E',
            opacity: p.op,
          }}
        />
      ))}

      {/* Center - hollow structure */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: 'translate(-50%, -50%)',
        }}
      >
        <svg width="300" height="350" viewBox="0 0 300 350">
          {/* Outer structure */}
          <rect
            x="50"
            y="30"
            width="200"
            height="290"
            rx="15"
            fill="none"
            stroke="#9E9E9E"
            strokeWidth="4"
            opacity={0.5}
            strokeDasharray={`${(1 - crumbleProgress) * 1200}`}
          />

          {/* Inner hollow */}
          <rect
            x="80"
            y="60"
            width="140"
            height="230"
            rx="10"
            fill="rgba(28,28,28,0.8)"
            stroke="rgba(255,0,0,0.3)"
            strokeWidth="2"
            opacity={crumbleProgress}
          />

          {/* Cracks */}
          {[
            'M100,80 L130,120 L110,160',
            'M180,70 L160,110 L190,140',
            'M120,200 L150,240 L140,280',
            'M200,180 L170,220 L180,260',
          ].map((d, i) => (
            <path
              key={i}
              d={d}
              fill="none"
              stroke="#ff0000"
              strokeWidth="2"
              opacity={interpolate(crumbleProgress, [i * 0.2, i * 0.2 + 0.3], [0, 0.6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
            />
          ))}

          {/* ₹ symbol fading */}
          <text
            x="125"
            y="200"
            fontSize="80"
            fill="#e9d700"
            fontFamily="Inter"
            fontWeight="700"
            opacity={1 - crumbleProgress * 0.7}
          >
            ₹
          </text>
        </svg>
      </div>

      {/* Termite dots */}
      {termites.map((t, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: t.x,
            top: t.y,
            width: t.size,
            height: t.size,
            borderRadius: '50%',
            backgroundColor: '#ff0000',
            opacity: t.op,
            boxShadow: '0 0 6px rgba(255,0,0,0.4)',
          }}
        />
      ))}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '5%',
          transform: `translate(-50%, ${interpolate(sp(5), [0, 1], [60, 0])}px)`,
          opacity: sp(5),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 85,
            color: '#ff0000',
            lineHeight: 1.2,
            textShadow: '0 0 50px rgba(255,0,0,0.2)',
          }}
        >
          धीरे-धीरे <span style={{ color: '#F5F5F5' }}>खोखला</span>
        </div>
      </div>

      {/* Bottom warning text */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          bottom: '8%',
          transform: `translate(-50%, ${interpolate(sp(70), [0, 1], [40, 0])}px)`,
          opacity: sp(70),
          textAlign: 'center',
          maxWidth: 1300,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 46,
            color: '#F5F5F5',
            lineHeight: 1.5,
          }}
        >
          छोटी आदतें • बिना शोर • <span style={{ color: '#ff0000' }}>आर्थिक ज़िंदगी ख़त्म</span>
        </div>
      </div>

      {/* Stick figure - alarmed, pointing at structure */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          bottom: 100,
          transform: `scale(${sp(50)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(50), [0, 1], [0, 1]),
        }}
      >
        <svg width="120" height="180" viewBox="0 0 120 180">
          <circle cx="60" cy="35" r="32" fill="rgba(255,0,0,0.05)" />
          <circle cx="60" cy="35" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="52" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="68" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="58" y1="33" x2="62" y2="33" stroke="#e9d700" strokeWidth="1.5" />
          {/* Shocked mouth */}
          <circle cx="60" cy="45" r="5" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="60" y1="53" x2="60" y2="115" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Arms pointing */}
          <line x1="60" y1="80" x2="25" y2="60" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="80" x2="90" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="115" x2="42" y2="160" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="60" y1="115" x2="78" y2="160" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Exclamation above head */}
          <line x1="60" y1="5" x2="60" y2="15" stroke="#ff0000" strokeWidth="3" />
          <circle cx="60" cy="20" r="2" fill="#ff0000" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_KhokhlaAadatein;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene11_TankhwahKisko',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene11_TankhwahKisko: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera pan down
  const camY = interpolate(frame, [0, durationInFrames], [-10, 10], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Money flow arrows animation
  const recipients = [
    { label: 'मकान मालिक', icon: 'house', delay: 25, color: '#ff0000' },
    { label: 'बिजली कंपनी', icon: 'bolt', delay: 40, color: '#ff4444' },
    { label: 'दूरसंचार', icon: 'signal', delay: 55, color: '#ff6666' },
    { label: 'मनोरंजन', icon: 'play', delay: 70, color: '#ff8888' },
    { label: 'किराने वाला', icon: 'cart', delay: 85, color: '#ffaaaa' },
  ];

  // Salary bar shrinking
  const barWidth = interpolate(frame, [20, 200], [1000, 100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => {
    const s1 = seededRandom(i + 2100);
    const s2 = seededRandom(i + 2200);
    return {
      x: s1 * 1920,
      y: (s2 * 1080 + Math.sin(frame * 0.02 + i) * 18) % 1150,
      size: 2 + s1 * 3,
      op: interpolate(frame, [0, 20, durationInFrames - 15, durationInFrames], [0, 0.15 + s2 * 0.2, 0.15 + s2 * 0.2, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
    };
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Dynamic gradient */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${145 + frame * 0.08}deg, #1C1C1C 0%, #2D2D2D 35%, #1e1a2a 65%, #1C1C1C 100%)`,
          transform: `translateY(${camY}px)`,
        }}
      />

      {/* Shape layers */}
      {[
        { type: 'circle', x: 1650, y: 200, size: 280, rot: frame * 0.09 },
        { type: 'rect', x: 200, y: 800, size: 220, rot: -frame * 0.11 },
        { type: 'triangle', x: 1500, y: 850, size: 190, rot: frame * 0.13 },
        { type: 'circle', x: 300, y: 200, size: 200, rot: -frame * 0.07 },
      ].map((s, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: s.x - s.size / 2,
            top: s.y - s.size / 2,
            width: s.size,
            height: s.size,
            border: `2px solid rgba(255,0,0,0.04)`,
            borderRadius: s.type === 'circle' ? '50%' : '8px',
            transform: `rotate(${s.rot}deg)`,
            clipPath: s.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : undefined,
          }}
        />
      ))}

      {/* Motion texture - flowing dots going right */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        {Array.from({ length: 25 }, (_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: ((i * 80 + frame * 2) % 2100) - 100,
              top: 400 + Math.sin(i * 1.5) * 200,
              width: 6,
              height: 6,
              borderRadius: '50%',
              backgroundColor: '#ff0000',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: p.y,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#ff0000' : '#F5F5F5',
            opacity: p.op,
          }}
        />
      ))}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '5%',
          transform: `translate(-50%, ${interpolate(sp(5), [0, 1], [50, 0])}px)`,
          opacity: sp(5),
          textAlign: 'center',
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 90,
            color: '#F5F5F5',
            lineHeight: 1.2,
          }}
        >
          तनख्वाह किसे <span style={{ color: '#ff0000' }}>जाती</span> है?
        </div>
      </div>

      {/* Salary bar at center */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '28%',
          transform: 'translate(-50%, 0)',
          opacity: sp(15),
        }}
      >
        <div
          style={{
            width: barWidth,
            height: 55,
            borderRadius: 10,
            background: 'linear-gradient(90deg, #e9d700, #FFC107)',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            transition: 'width 0.1s',
            overflow: 'hidden',
            position: 'relative',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#1C1C1C',
              whiteSpace: 'nowrap',
            }}
          >
            💰 तुम्हारी तनख्वाह
          </div>
        </div>
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 20,
            color: '#9E9E9E',
            textAlign: 'right',
            marginTop: 8,
          }}
        >
          {Math.round((barWidth / 1000) * 100)}% बची
        </div>
      </div>

      {/* Recipients list */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '45%',
          transform: 'translate(-50%, 0)',
        }}
      >
        {recipients.map((r, i) => {
          const rSp = sp(r.delay, { damping: 10 });
          return (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 20,
                marginBottom: 18,
                transform: `translateX(${interpolate(rSp, [0, 1], [300, 0])}px)`,
                opacity: rSp,
              }}
            >
              {/* Arrow */}
              <svg width="40" height="24" viewBox="0 0 40 24">
                <line x1="0" y1="12" x2="30" y2="12" stroke={r.color} strokeWidth="3" />
                <polyline points="24,5 34,12 24,19" fill="none" stroke={r.color} strokeWidth="3" />
              </svg>

              {/* Icon */}
              <div
                style={{
                  width: 50,
                  height: 50,
                  borderRadius: 10,
                  border: `2px solid ${r.color}`,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  background: `rgba(255,0,0,0.08)`,
                }}
              >
                <svg width="28" height="28" viewBox="0 0 28 28">
                  {r.icon === 'house' && (
                    <g fill="none" stroke={r.color} strokeWidth="2">
                      <path d="M14,4 L24,14 L24,24 L4,24 L4,14 Z" />
                      <rect x="10" y="16" width="8" height="8" />
                    </g>
                  )}
                  {r.icon === 'bolt' && (
                    <polygon points="16,2 8,14 14,14 12,26 20,14 14,14" fill="none" stroke={r.color} strokeWidth="2" />
                  )}
                  {r.icon === 'signal' && (
                    <g fill="none" stroke={r.color} strokeWidth="2">
                      <path d="M6,22 Q6,10 14,6" />
                      <path d="M10,22 Q10,14 14,10" />
                      <path d="M14,22 L14,16" />
                      <circle cx="14" cy="22" r="2" fill={r.color} />
                    </g>
                  )}
                  {r.icon === 'play' && (
                    <g fill="none" stroke={r.color} strokeWidth="2">
                      <rect x="4" y="6" width="20" height="16" rx="3" />
                      <polygon points="11,10 11,18 18,14" fill={r.color} opacity="0.5" />
                    </g>
                  )}
                  {r.icon === 'cart' && (
                    <g fill="none" stroke={r.color} strokeWidth="2">
                      <polyline points="4,6 8,6 12,20 22,20" />
                      <circle cx="13" cy="24" r="2" fill={r.color} />
                      <circle cx="21" cy="24" r="2" fill={r.color} />
                      <polyline points="8,6 10,16 24,16 26,8 10,8" />
                    </g>
                  )}
                </svg>
              </div>

              {/* Label */}
              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 38,
                  color: r.color,
                  fontWeight: 600,
                }}
              >
                {r.label}
              </div>
            </div>
          );
        })}
      </div>

      {/* "सब खुश" tag */}
      <div
        style={{
          position: 'absolute',
          right: 120,
          bottom: '20%',
          opacity: sp(100),
          transform: `scale(${sp(100)})`,
        }}
      >
        <div
          style={{
            fontFamily: 'Yantra One',
            fontSize: 52,
            color: '#ff0000',
            textShadow: '0 0 30px rgba(255,0,0,0.2)',
          }}
        >
          सब ख़ुश 😊
        </div>
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 32,
            color: '#9E9E9E',
            marginTop: 8,
          }}
        >
          सिवाय तुम्हारे भविष्य के
        </div>
      </div>

      {/* Stick figure - sad, empty pockets */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          bottom: 60,
          transform: `scale(${sp(45)}) translateY(${Math.sin(frame * 0.05) * 2}px)`,
          opacity: interpolate(sp(45), [0, 1], [0, 1]),
        }}
      >
        <svg width="110" height="170" viewBox="0 0 110 170">
          <circle cx="55" cy="35" r="30" fill="rgba(233,215,0,0.05)" />
          <circle cx="55" cy="35" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
          <circle cx="47" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <circle cx="63" cy="33" r="6" fill="none" stroke="#e9d700" strokeWidth="1.5" />
          <line x1="53" y1="33" x2="57" y2="33" stroke="#e9d700" strokeWidth="1.5" />
          {/* Sad */}
          <path d="M47,45 Q55,40 63,45" fill="none" stroke="#F5F5F5" strokeWidth="1.5" />
          <line x1="55" y1="53" x2="55" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
          {/* Empty pockets gesture */}
          <line x1="55" y1="78" x2="30" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="30" y1="95" x2="28" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="55" y1="78" x2="80" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="80" y1="95" x2="82" y2="110" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="55" y1="110" x2="38" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
          <line x1="55" y1="110" x2="72" y2="155" stroke="#F5F5F5" strokeWidth="2.5" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 38%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30, durationInFrames - 15, durationInFrames], [0, 0.3, 0.3, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene11_TankhwahKisko;
```

