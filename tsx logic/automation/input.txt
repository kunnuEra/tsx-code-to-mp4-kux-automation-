```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene1_24KOffer',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_24KOffer: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Background gradient shift
  const gradAngle = interpolate(frame, [0, durationInFrames], [135, 160], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Floating particles
  const particles = Array.from({ length: 30 }, (_, i) => {
    const seed = seededRandom(i + 1);
    const seed2 = seededRandom(i + 50);
    const x = seed * 1920;
    const y = seed2 * 1080;
    const size = 2 + seed * 4;
    const speed = 0.3 + seed2 * 0.7;
    const floatY = Math.sin((frame * speed * 0.03) + i) * 40;
    const floatX = Math.cos((frame * speed * 0.02) + i * 2) * 20;
    const opacity = interpolate(frame, [0, 30], [0, 0.15 + seed * 0.25], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });
    return { x, y, size, floatY, floatX, opacity };
  });

  // Shape layers
  const shapes = [
    {
      type: 'circle',
      x: 150,
      y: 200,
      size: 350,
      rotation: frame * 0.15,
      opacity: 0.04,
    },
    {
      type: 'rect',
      x: 1500,
      y: 700,
      size: 280,
      rotation: -frame * 0.1,
      opacity: 0.05,
    },
    {
      type: 'triangle',
      x: 1700,
      y: 150,
      size: 200,
      rotation: frame * 0.2,
      opacity: 0.03,
    },
    {
      type: 'circle',
      x: 400,
      y: 800,
      size: 180,
      rotation: -frame * 0.12,
      opacity: 0.05,
    },
  ];

  // ₹24,000 text animation
  const amountScale = sp(15, { damping: 10, stiffness: 150 });
  const amountY = interpolate(sp(15), [0, 1], [80, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Question mark entrance
  const qScale = sp(50, { damping: 8, stiffness: 100 });

  // Fire icon pulse
  const firePulse = 1 + Math.sin(frame * 0.12) * 0.08;

  // Clock countdown visual
  const clockRotation = interpolate(frame, [0, durationInFrames], [0, 360], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Subtitle entrance
  const subtitleOp = interpolate(frame, [60, 80], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const subtitleY = interpolate(frame, [60, 80], [30, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Screen shake on "जल जाएगा"
  const shakeIntensity = interpolate(frame, [130, 140, 160, 170], [0, 6, 6, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const shakeX = Math.sin(frame * 2.5) * shakeIntensity;
  const shakeY = Math.cos(frame * 3.1) * shakeIntensity;

  // Money notes falling
  const notes = Array.from({ length: 12 }, (_, i) => {
    const s = seededRandom(i + 100);
    const s2 = seededRandom(i + 200);
    const startDelay = 20 + i * 8;
    const noteOp = interpolate(frame, [startDelay, startDelay + 20, durationInFrames - 40, durationInFrames - 20], [0, 0.7, 0.7, 0], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });
    const noteY = interpolate(frame, [startDelay, durationInFrames], [-50, 1200], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });
    const noteX = 100 + s * 1700;
    const noteRotate = frame * (0.5 + s2 * 2) * (i % 2 === 0 ? 1 : -1);
    return { noteOp, noteY, noteX, noteRotate, s };
  });

  // Glow pulse behind amount
  const glowPulse = 0.3 + Math.sin(frame * 0.08) * 0.15;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Camera wrapper */}
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camZoom}) translate(${shakeX}px, ${shakeY}px)`,
          position: 'absolute',
          top: 0,
          left: 0,
        }}
      >
        {/* Dynamic gradient background */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0a0a0a 0%, #1C1C1C 35%, #2D2D2D 70%, #1a1a2e 100%)`,
          }}
        />

        {/* Animated shape layers */}
        {shapes.map((shape, i) => (
          <div
            key={`shape-${i}`}
            style={{
              position: 'absolute',
              left: shape.x,
              top: shape.y,
              width: shape.size,
              height: shape.size,
              opacity: shape.opacity,
              transform: `rotate(${shape.rotation}deg) scale(${1 + Math.sin(frame * 0.01 + i) * 0.05})`,
              border:
                shape.type === 'circle'
                  ? '2px solid #FFC107'
                  : shape.type === 'rect'
                    ? '2px solid #e9d700'
                    : '2px solid #F5F5F5',
              borderRadius: shape.type === 'circle' ? '50%' : shape.type === 'rect' ? '8px' : '0',
              clipPath:
                shape.type === 'triangle'
                  ? 'polygon(50% 0%, 0% 100%, 100% 100%)'
                  : undefined,
            }}
          />
        ))}

        {/* Motion texture — diagonal lines */}
        <AbsoluteFill style={{ opacity: 0.03 }}>
          {Array.from({ length: 20 }, (_, i) => (
            <div
              key={`line-${i}`}
              style={{
                position: 'absolute',
                left: -200 + i * 120 + Math.sin(frame * 0.01 + i) * 10,
                top: 0,
                width: 1,
                height: '150%',
                background: '#F5F5F5',
                transform: 'rotate(25deg)',
                transformOrigin: 'top left',
              }}
            />
          ))}
        </AbsoluteFill>

        {/* Floating particles */}
        {particles.map((p, i) => (
          <div
            key={`particle-${i}`}
            style={{
              position: 'absolute',
              left: p.x + p.floatX,
              top: p.y + p.floatY,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#FFC107' : i % 3 === 1 ? '#e9d700' : '#F5F5F5',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Falling money notes */}
        {notes.map((note, i) => (
          <div
            key={`note-${i}`}
            style={{
              position: 'absolute',
              left: note.noteX,
              top: note.noteY,
              opacity: note.noteOp,
              transform: `rotate(${note.noteRotate}deg)`,
            }}
          >
            <svg width="60" height="32" viewBox="0 0 60 32">
              <rect
                x="2"
                y="2"
                width="56"
                height="28"
                rx="4"
                fill="none"
                stroke="#FFC107"
                strokeWidth="1.5"
                opacity="0.7"
              />
              <text
                x="30"
                y="20"
                textAnchor="middle"
                fill="#FFC107"
                fontSize="12"
                fontFamily="Inter, sans-serif"
                fontWeight="700"
              >
                ₹
              </text>
            </svg>
          </div>
        ))}

        {/* Glow behind main amount */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '32%',
            transform: 'translate(-50%, -50%)',
            width: 600,
            height: 300,
            borderRadius: '50%',
            background: 'radial-gradient(circle, rgba(255,193,7,0.15) 0%, transparent 70%)',
            opacity: glowPulse,
          }}
        />

        {/* Main content — ₹24,000 */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '28%',
            transform: `translate(-50%, ${amountY}px) scale(${amountScale})`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 220,
              fontWeight: 900,
              color: '#FFC107',
              textShadow: '0 0 60px rgba(255,193,7,0.4), 0 4px 20px rgba(0,0,0,0.6)',
              lineHeight: 1,
            }}
          >
            ₹24,000
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 48,
              fontWeight: 700,
              color: '#F5F5F5',
              marginTop: 10,
              opacity: interpolate(frame, [30, 50], [0, 1], {
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
              }),
              transform: `translateY(${interpolate(frame, [30, 50], [20, 0], {
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
              })}px)`,
            }}
          >
            रोज़ सुबह मिलेंगे
          </div>
        </div>

        {/* Clock icon with rotation */}
        <div
          style={{
            position: 'absolute',
            left: '18%',
            top: '55%',
            opacity: interpolate(frame, [40, 60], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
            transform: `scale(${sp(40)})`,
          }}
        >
          <svg width="140" height="140" viewBox="0 0 140 140">
            <circle
              cx="70"
              cy="70"
              r="60"
              fill="none"
              stroke="#e9d700"
              strokeWidth="4"
              opacity="0.8"
            />
            <circle cx="70" cy="70" r="4" fill="#e9d700" />
            {/* Hour hand */}
            <line
              x1="70"
              y1="70"
              x2={70 + 25 * Math.cos(((clockRotation * 0.5 - 90) * Math.PI) / 180)}
              y2={70 + 25 * Math.sin(((clockRotation * 0.5 - 90) * Math.PI) / 180)}
              stroke="#F5F5F5"
              strokeWidth="3"
              strokeLinecap="round"
            />
            {/* Minute hand */}
            <line
              x1="70"
              y1="70"
              x2={70 + 40 * Math.cos(((clockRotation - 90) * Math.PI) / 180)}
              y2={70 + 40 * Math.sin(((clockRotation - 90) * Math.PI) / 180)}
              stroke="#FFC107"
              strokeWidth="2"
              strokeLinecap="round"
            />
            <text
              x="70"
              y="115"
              textAnchor="middle"
              fill="#9E9E9E"
              fontSize="14"
              fontFamily="Inter, sans-serif"
            >
              12:00 AM
            </text>
          </svg>
        </div>

        {/* Fire icon — "जल जाएगा" */}
        <div
          style={{
            position: 'absolute',
            right: '16%',
            top: '50%',
            opacity: interpolate(frame, [80, 100], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
            transform: `scale(${sp(80) * firePulse})`,
          }}
        >
          <svg width="120" height="150" viewBox="0 0 120 150">
            <path
              d="M60 10 C60 10, 15 60, 20 90 C25 120, 45 140, 60 140 C75 140, 95 120, 100 90 C105 60, 60 10, 60 10Z"
              fill="#ff0000"
              opacity="0.8"
            />
            <path
              d="M60 50 C60 50, 38 80, 40 100 C42 115, 52 130, 60 130 C68 130, 78 115, 80 100 C82 80, 60 50, 60 50Z"
              fill="#FFC107"
              opacity="0.9"
            />
            <path
              d="M60 80 C60 80, 48 95, 50 108 C51 115, 55 122, 60 122 C65 122, 69 115, 70 108 C72 95, 60 80, 60 80Z"
              fill="#e9d700"
              opacity="1"
            />
          </svg>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 28,
              fontWeight: 700,
              color: '#ff0000',
              textAlign: 'center',
              marginTop: 5,
              textShadow: '0 0 20px rgba(255,0,0,0.5)',
            }}
          >
            जल जाएगा!
          </div>
        </div>

        {/* Question text */}
        <div
          style={{
            position: 'absolute',
            bottom: '12%',
            left: '50%',
            transform: `translate(-50%, ${subtitleY}px) scale(${qScale})`,
            opacity: subtitleOp,
            textAlign: 'center',
            width: '80%',
          }}
        >
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 64,
              fontWeight: 800,
              color: '#F5F5F5',
              textShadow: '0 2px 15px rgba(0,0,0,0.5)',
            }}
          >
            तो तुम क्या करोगे?
          </div>
        </div>

        {/* Stick figure — thinking pose */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            bottom: '28%',
            transform: `translate(-50%, 0) scale(${sp(45)})`,
            opacity: interpolate(frame, [45, 65], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="80" height="130" viewBox="0 0 80 130">
            {/* Golden glow */}
            <circle cx="40" cy="22" r="28" fill="rgba(255,193,7,0.1)" />
            {/* Head */}
            <circle
              cx="40"
              cy="22"
              r="16"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
            />
            {/* Golden glasses */}
            <circle
              cx="34"
              cy="20"
              r="5"
              fill="none"
              stroke="#FFC107"
              strokeWidth="1.5"
            />
            <circle
              cx="46"
              cy="20"
              r="5"
              fill="none"
              stroke="#FFC107"
              strokeWidth="1.5"
            />
            <line x1="39" y1="20" x2="41" y2="20" stroke="#FFC107" strokeWidth="1" />
            {/* Body */}
            <line
              x1="40"
              y1="38"
              x2="40"
              y2="80"
              stroke="#F5F5F5"
              strokeWidth="3"
              strokeLinecap="round"
            />
            {/* Left arm — hand on chin (thinking) */}
            <path
              d="M40 55 L25 48 L30 38"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="3"
              strokeLinecap="round"
            />
            {/* Right arm */}
            <line
              x1="40"
              y1="55"
              x2="58"
              y2="68"
              stroke="#F5F5F5"
              strokeWidth="3"
              strokeLinecap="round"
            />
            {/* Legs */}
            <line
              x1="40"
              y1="80"
              x2="28"
              y2="120"
              stroke="#F5F5F5"
              strokeWidth="3"
              strokeLinecap="round"
            />
            <line
              x1="40"
              y1="80"
              x2="52"
              y2="120"
              stroke="#F5F5F5"
              strokeWidth="3"
              strokeLinecap="round"
            />
            {/* Thinking dots */}
            <circle
              cx="18"
              cy="12"
              r="3"
              fill="#FFC107"
              opacity={0.4 + Math.sin(frame * 0.1) * 0.4}
            />
            <circle
              cx="12"
              cy="4"
              r="2"
              fill="#FFC107"
              opacity={0.4 + Math.sin(frame * 0.1 + 1) * 0.4}
            />
            {/* Breathing */}
            <animateTransform
              attributeName="transform"
              type="translate"
              values={`0 ${Math.sin(frame * 0.05) * 2}`}
            />
          </svg>
        </div>

        {/* Vignette overlay */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: interpolate(frame, [0, 30], [0, 0.3], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_24KOffer;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_TimeIsCurrency',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_TimeIsCurrency: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera pan from left to right
  const camX = interpolate(frame, [0, durationInFrames], [30, -30], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Background gradient
  const gradHue = interpolate(frame, [0, durationInFrames], [220, 250], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 25 }, (_, i) => {
    const s1 = seededRandom(i + 300);
    const s2 = seededRandom(i + 400);
    return {
      x: s1 * 1920,
      y: s2 * 1080,
      size: 2 + s1 * 3,
      floatY: Math.sin(frame * 0.04 + i * 0.8) * 30,
      floatX: Math.cos(frame * 0.03 + i * 1.2) * 15,
      opacity: 0.1 + s2 * 0.2,
    };
  });

  // Shape layers — hexagons and diamonds
  const shapeBreath = 1 + Math.sin(frame * 0.015) * 0.03;

  // Split screen concept: LEFT = ₹ (money), RIGHT = ⏰ (time)
  const dividerX = interpolate(frame, [30, 60], [1920, 960], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Left side elements
  const leftOp = interpolate(frame, [20, 45], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const leftScale = sp(20);

  // Right side elements
  const rightOp = interpolate(frame, [60, 85], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const rightScale = sp(60);

  // Equals sign
  const equalsOp = interpolate(frame, [120, 145], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const equalsScale = sp(120, { damping: 8, stiffness: 200 });

  // 24 hours grid
  const hourBlocks = Array.from({ length: 24 }, (_, i) => {
    const blockDelay = 80 + i * 3;
    const blockOp = interpolate(frame, [blockDelay, blockDelay + 15], [0, 1], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });
    const blockScale = sp(blockDelay);
    return { blockOp, blockScale };
  });

  // Bottom text
  const bottomTextOp = interpolate(frame, [160, 185], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const bottomTextY = interpolate(frame, [160, 185], [40, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Grid texture
  const gridOp = 0.04 + Math.sin(frame * 0.02) * 0.01;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Camera pan wrapper */}
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `translateX(${camX}px)`,
          position: 'absolute',
          top: 0,
          left: 0,
        }}
      >
        {/* Dynamic gradient background */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(180deg, #0d0d1a 0%, #1C1C1C 40%, #1a1a2e 80%, #0a0a15 100%)`,
          }}
        />

        {/* Grid texture */}
        <AbsoluteFill style={{ opacity: gridOp }}>
          {Array.from({ length: 15 }, (_, i) => (
            <React.Fragment key={`grid-h-${i}`}>
              <div
                style={{
                  position: 'absolute',
                  left: 0,
                  top: i * 80,
                  width: '100%',
                  height: 1,
                  background: '#F5F5F5',
                }}
              />
              <div
                style={{
                  position: 'absolute',
                  left: i * 140,
                  top: 0,
                  width: 1,
                  height: '100%',
                  background: '#F5F5F5',
                }}
              />
            </React.Fragment>
          ))}
        </AbsoluteFill>

        {/* Shape layers — hexagons */}
        <div
          style={{
            position: 'absolute',
            left: 80,
            top: 100,
            width: 300,
            height: 300,
            opacity: 0.05,
            transform: `rotate(${frame * 0.08}deg) scale(${shapeBreath})`,
          }}
        >
          <svg viewBox="0 0 300 300">
            <polygon
              points="150,10 280,85 280,215 150,290 20,215 20,85"
              fill="none"
              stroke="#FFC107"
              strokeWidth="2"
            />
          </svg>
        </div>
        <div
          style={{
            position: 'absolute',
            right: 100,
            bottom: 150,
            width: 250,
            height: 250,
            opacity: 0.04,
            transform: `rotate(${-frame * 0.1}deg) scale(${shapeBreath})`,
          }}
        >
          <svg viewBox="0 0 250 250">
            <rect
              x="30"
              y="30"
              width="190"
              height="190"
              fill="none"
              stroke="#e9d700"
              strokeWidth="2"
              transform="rotate(45 125 125)"
            />
          </svg>
        </div>

        {/* Floating particles */}
        {particles.map((p, i) => (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: p.x + p.floatX,
              top: p.y + p.floatY,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#FFC107' : '#e9d700',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Divider line */}
        <div
          style={{
            position: 'absolute',
            left: dividerX,
            top: '10%',
            width: 2,
            height: '50%',
            background: 'linear-gradient(180deg, transparent, #FFC107, transparent)',
            opacity: 0.6,
          }}
        />

        {/* LEFT — Money concept */}
        <div
          style={{
            position: 'absolute',
            left: '12%',
            top: '18%',
            opacity: leftOp,
            transform: `scale(${leftScale})`,
            textAlign: 'center',
          }}
        >
          <svg width="160" height="160" viewBox="0 0 160 160">
            <circle
              cx="80"
              cy="80"
              r="70"
              fill="none"
              stroke="#FFC107"
              strokeWidth="3"
              opacity="0.6"
            />
            <text
              x="80"
              y="95"
              textAnchor="middle"
              fill="#FFC107"
              fontSize="70"
              fontFamily="Inter, sans-serif"
              fontWeight="800"
            >
              ₹
            </text>
          </svg>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 42,
              fontWeight: 700,
              color: '#F5F5F5',
              marginTop: 15,
            }}
          >
            मुद्रा
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 30,
              fontWeight: 500,
              color: '#9E9E9E',
              marginTop: 5,
            }}
          >
            हिसाब रखोगे
          </div>
        </div>

        {/* Equals sign */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '28%',
            transform: `translate(-50%, -50%) scale(${equalsScale})`,
            opacity: equalsOp,
          }}
        >
          <div
            style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: 120,
              fontWeight: 900,
              color: '#e9d700',
              textShadow: '0 0 40px rgba(233,215,0,0.3)',
            }}
          >
            =
          </div>
        </div>

        {/* RIGHT — Time concept */}
        <div
          style={{
            position: 'absolute',
            right: '12%',
            top: '18%',
            opacity: rightOp,
            transform: `scale(${rightScale})`,
            textAlign: 'center',
          }}
        >
          <svg width="160" height="160" viewBox="0 0 160 160">
            <circle
              cx="80"
              cy="80"
              r="70"
              fill="none"
              stroke="#e9d700"
              strokeWidth="3"
              opacity="0.6"
            />
            <circle cx="80" cy="80" r="4" fill="#e9d700" />
            <line
              x1="80"
              y1="80"
              x2="80"
              y2="35"
              stroke="#F5F5F5"
              strokeWidth="3"
              strokeLinecap="round"
            />
            <line
              x1="80"
              y1="80"
              x2="115"
              y2="80"
              stroke="#FFC107"
              strokeWidth="2"
              strokeLinecap="round"
            />
          </svg>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 42,
              fontWeight: 700,
              color: '#F5F5F5',
              marginTop: 15,
            }}
          >
            समय
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 30,
              fontWeight: 500,
              color: '#9E9E9E',
              marginTop: 5,
            }}
          >
            24 घंटे रोज़
          </div>
        </div>

        {/* 24 Hour blocks grid */}
        <div
          style={{
            position: 'absolute',
            bottom: '14%',
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            flexWrap: 'wrap',
            gap: 8,
            width: 750,
            justifyContent: 'center',
          }}
        >
          {hourBlocks.map((block, i) => (
            <div
              key={`hour-${i}`}
              style={{
                width: 52,
                height: 52,
                borderRadius: 6,
                border: '1.5px solid',
                borderColor: i < 8 ? '#FFC107' : i < 16 ? '#e9d700' : '#9E9E9E',
                backgroundColor:
                  i < 8
                    ? 'rgba(255,193,7,0.15)'
                    : i < 16
                      ? 'rgba(233,215,0,0.08)'
                      : 'rgba(158,158,158,0.05)',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: 16,
                fontWeight: 600,
                color: i < 8 ? '#FFC107' : i < 16 ? '#e9d700' : '#9E9E9E',
                opacity: block.blockOp,
                transform: `scale(${block.blockScale})`,
              }}
            >
              {i + 1}
            </div>
          ))}
        </div>

        {/* Bottom text */}
        <div
          style={{
            position: 'absolute',
            bottom: '4%',
            left: '50%',
            transform: `translate(-50%, ${bottomTextY}px)`,
            opacity: bottomTextOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 44,
              fontWeight: 700,
              color: '#F5F5F5',
            }}
          >
            पैसा नहीं,{' '}
            <span style={{ color: '#FFC107' }}>समय</span> मिलता है
          </div>
        </div>

        {/* Stick figure — pointing */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '42%',
            transform: `translate(-50%, ${Math.sin(frame * 0.05) * 2}px) scale(${sp(55)})`,
            opacity: interpolate(frame, [55, 75], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="70" height="110" viewBox="0 0 70 110">
            <circle cx="35" cy="18" r="22" fill="rgba(255,193,7,0.08)" />
            <circle
              cx="35"
              cy="18"
              r="13"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth="2.5"
            />
            <circle
              cx="30"
              cy="16"
              r="4"
              fill="none"
              stroke="#FFC107"
              strokeWidth="1.2"
            />
            <circle
              cx="40"
              cy="16"
              r="4"
              fill="none"
              stroke="#FFC107"
              strokeWidth="1.2"
            />
            <line x1="34" y1="16" x2="36" y2="16" stroke="#FFC107" strokeWidth="0.8" />
            <line
              x1="35"
              y1="31"
              x2="35"
              y2="70"
              stroke="#F5F5F5"
              strokeWidth="2.5"
              strokeLinecap="round"
            />
            {/* Arms pointing both sides */}
            <line
              x1="35"
              y1="45"
              x2="10"
              y2="38"
              stroke="#F5F5F5"
              strokeWidth="2.5"
              strokeLinecap="round"
            />
            <line
              x1="35"
              y1="45"
              x2="60"
              y2="38"
              stroke="#F5F5F5"
              strokeWidth="2.5"
              strokeLinecap="round"
            />
            <line
              x1="35"
              y1="70"
              x2="22"
              y2="100"
              stroke="#F5F5F5"
              strokeWidth="2.5"
              strokeLinecap="round"
            />
            <line
              x1="35"
              y1="70"
              x2="48"
              y2="100"
              stroke="#F5F5F5"
              strokeWidth="2.5"
              strokeLinecap="round"
            />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.65) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_TimeIsCurrency;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene3_EqualDistribution',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_EqualDistribution: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow zoom out
  const camZoom = interpolate(frame, [0, durationInFrames], [1.08, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Gradient
  const gradShift = interpolate(frame, [0, durationInFrames], [0, 30], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => {
    const s1 = seededRandom(i + 500);
    const s2 = seededRandom(i + 600);
    return {
      x: s1 * 1920,
      y: s2 * 1080,
      size: 2 + s1 * 4,
      dy: Math.sin(frame * 0.035 + i) * 35,
      dx: Math.cos(frame * 0.025 + i * 1.5) * 18,
      opacity: 0.08 + s2 * 0.2,
    };
  });

  // Balance scale animation
  const scaleEntrance = sp(20, { damping: 10, stiffness: 100 });
  const scaleBalance = Math.sin(frame * 0.04) * 3; // slight tilt back and forth

  // Person silhouettes — different types
  const people = [
    { label: 'CEO', x: 200, delay: 50 },
    { label: 'किसान', x: 480, delay: 65 },
    { label: 'छात्र', x: 760, delay: 80 },
    { label: 'मजदूर', x: 1040, delay: 95 },
    { label: 'तुम', x: 1320, delay: 110 },
  ];

  // "24h" label for each
  const labelDelay = (d: number) =>
    interpolate(frame, [d + 20, d + 40], [0, 1], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });

  // Hero text
  const heroOp = interpolate(frame, [150, 175], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const heroScale = sp(150, { damping: 8, stiffness: 150 });

  // Connecting line
  const lineWidth = interpolate(frame, [130, 170], [0, 1100], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Shape layers — concentric circles
  const circleBreath = 1 + Math.sin(frame * 0.012) * 0.04;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camZoom})`,
          position: 'absolute',
          top: 0,
          left: 0,
        }}
      >
        {/* Gradient background */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${150 + gradShift}deg, #0a0f1a 0%, #1C1C1C 30%, #1a1520 60%, #0d0d1a 100%)`,
          }}
        />

        {/* Shape layers — concentric circles */}
        {[400, 550, 700].map((r, i) => (
          <div
            key={`conc-${i}`}
            style={{
              position: 'absolute',
              left: '50%',
              top: '45%',
              width: r,
              height: r,
              borderRadius: '50%',
              border: `1.5px solid ${i === 0 ? '#FFC107' : i === 1 ? '#e9d700' : '#9E9E9E'}`,
              opacity: 0.04 + i * 0.01,
              transform: `translate(-50%, -50%) scale(${circleBreath}) rotate(${frame * (0.05 + i * 0.02)}deg)`,
            }}
          />
        ))}

        {/* Motion texture — dot grid */}
        <AbsoluteFill style={{ opacity: 0.03 }}>
          {Array.from({ length: 100 }, (_, i) => {
            const row = Math.floor(i / 10);
            const col = i % 10;
            return (
              <div
                key={`dot-${i}`}
                style={{
                  position: 'absolute',
                  left: 140 + col * 180,
                  top: 80 + row * 110,
                  width: 3,
                  height: 3,
                  borderRadius: '50%',
                  backgroundColor: '#F5F5F5',
                }}
              />
            );
          })}
        </AbsoluteFill>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={`part-${i}`}
            style={{
              position: 'absolute',
              left: p.x + p.dx,
              top: p.y + p.dy,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#FFC107' : '#e9d700',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Balance Scale SVG */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '8%',
            transform: `translate(-50%, 0) scale(${scaleEntrance})`,
            opacity: interpolate(frame, [20, 40], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="200" height="120" viewBox="0 0 200 120">
            {/* Pillar */}
            <line
              x1="100"
              y1="10"
              x2="100"
              y2="100"
              stroke="#FFC107"
              strokeWidth="3"
            />
            {/* Beam */}
            <line
              x1="30"
              y1={50 + scaleBalance}
              x2="170"
              y2={50 - scaleBalance}
              stroke="#F5F5F5"
              strokeWidth="3"
              strokeLinecap="round"
            />
            {/* Left pan */}
            <path
              d={`M20,${55 + scaleBalance} Q50,${70 + scaleBalance} 40,${55 + scaleBalance}`}
              fill="none"
              stroke="#e9d700"
              strokeWidth="2"
            />
            <text
              x="30"
              y={48 + scaleBalance}
              fill="#FFC107"
              fontSize="16"
              fontFamily="Inter"
              fontWeight="700"
              textAnchor="middle"
            >
              24h
            </text>
            {/* Right pan */}
            <path
              d={`M160,${55 - scaleBalance} Q190,${70 - scaleBalance} 180,${55 - scaleBalance}`}
              fill="none"
              stroke="#e9d700"
              strokeWidth="2"
            />
            <text
              x="170"
              y={48 - scaleBalance}
              fill="#FFC107"
              fontSize="16"
              fontFamily="Inter"
              fontWeight="700"
              textAnchor="middle"
            >
              24h
            </text>
            {/* Top triangle */}
            <polygon points="92,8 108,8 100,0" fill="#FFC107" />
          </svg>
        </div>

        {/* People silhouettes with 24h labels */}
        {people.map((person, i) => (
          <div
            key={`person-${i}`}
            style={{
              position: 'absolute',
              left: person.x,
              top: '38%',
              textAlign: 'center',
              opacity: interpolate(
                frame,
                [person.delay, person.delay + 25],
                [0, 1],
                { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
              ),
              transform: `scale(${sp(person.delay)}) translateY(${Math.sin(frame * 0.05 + i) * 2}px)`,
            }}
          >
            {/* Person SVG */}
            <svg width="80" height="120" viewBox="0 0 80 120">
              <circle cx="40" cy="18" r="14" fill="none" stroke={i === 4 ? '#FFC107' : '#F5F5F5'} strokeWidth="2.5" />
              {i === 4 && (
                <>
                  <circle cx="34" cy="16" r="4" fill="none" stroke="#FFC107" strokeWidth="1.2" />
                  <circle cx="46" cy="16" r="4" fill="none" stroke="#FFC107" strokeWidth="1.2" />
                  <line x1="38" y1="16" x2="42" y2="16" stroke="#FFC107" strokeWidth="0.8" />
                </>
              )}
              <line x1="40" y1="32" x2="40" y2="72" stroke={i === 4 ? '#FFC107' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
              <line x1="40" y1="48" x2="22" y2="60" stroke={i === 4 ? '#FFC107' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
              <line x1="40" y1="48" x2="58" y2="60" stroke={i === 4 ? '#FFC107' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
              <line x1="40" y1="72" x2="26" y2="105" stroke={i === 4 ? '#FFC107' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
              <line x1="40" y1="72" x2="54" y2="105" stroke={i === 4 ? '#FFC107' : '#F5F5F5'} strokeWidth="2.5" strokeLinecap="round" />
            </svg>

            {/* Label */}
            <div
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 28,
                fontWeight: 700,
                color: i === 4 ? '#FFC107' : '#F5F5F5',
                marginTop: 8,
              }}
            >
              {person.label}
            </div>

            {/* 24h badge */}
            <div
              style={{
                marginTop: 10,
                opacity: labelDelay(person.delay),
                display: 'inline-block',
                padding: '6px 18px',
                borderRadius: 20,
                border: '2px solid #FFC107',
                backgroundColor: 'rgba(255,193,7,0.1)',
              }}
            >
              <span
                style={{
                  fontFamily: "'JetBrains Mono', monospace",
                  fontSize: 22,
                  fontWeight: 700,
                  color: '#FFC107',
                }}
              >
                24h
              </span>
            </div>
          </div>
        ))}

        {/* Connecting line across all people */}
        <div
          style={{
            position: 'absolute',
            left: 200,
            top: '73%',
            width: lineWidth,
            height: 2,
            background: 'linear-gradient(90deg, #FFC107, #e9d700, #FFC107)',
            opacity: 0.5,
          }}
        />

        {/* Hero text */}
        <div
          style={{
            position: 'absolute',
            bottom: '6%',
            left: '50%',
            transform: `translate(-50%, 0) scale(${heroScale})`,
            opacity: heroOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 90,
              fontWeight: 900,
              color: '#F5F5F5',
              textShadow: '0 0 40px rgba(255,193,7,0.2)',
            }}
          >
            बिल्कुल{' '}
            <span style={{ color: '#FFC107' }}>बराबर</span>{' '}
            बटवारा
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 36,
              fontWeight: 500,
              color: '#9E9E9E',
              marginTop: 8,
            }}
          >
            हर रोज़, बिना अपवाद
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_EqualDistribution;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene4_TwoPathsDiverge',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_TwoPathsDiverge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera tilt
  const camRotate = interpolate(frame, [0, durationInFrames], [0.5, -0.5], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.03], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => {
    const s1 = seededRandom(i + 700);
    const s2 = seededRandom(i + 800);
    return {
      x: s1 * 1920,
      y: s2 * 1080,
      size: 2 + s1 * 3.5,
      dy: Math.sin(frame * 0.04 + i * 0.9) * 25,
      dx: Math.cos(frame * 0.03 + i * 1.3) * 15,
      opacity: 0.1 + s2 * 0.18,
    };
  });

  // Rising graph line for left side (success)
  const graphProgress = interpolate(frame, [50, 180], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Flat line for right side (stagnant)
  const flatProgress = interpolate(frame, [70, 200], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Left person entrance
  const leftPersonScale = sp(25);
  const leftPersonOp = interpolate(frame, [25, 50], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Right person entrance
  const rightPersonScale = sp(45);
  const rightPersonOp = interpolate(frame, [45, 70], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Badge items for left person
  const badges = [
    { icon: '📊', text: 'आर्थिक मजबूती', delay: 80 },
    { icon: '🎯', text: 'नए हुनर', delay: 100 },
    { icon: '🚪', text: 'विकल्प ही विकल्प', delay: 120 },
  ];

  // "8 साल बाद" text
  const yearOp = interpolate(frame, [10, 30], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const yearScale = sp(10, { damping: 10, stiffness: 140 });

  // Divider
  const dividerH = interpolate(frame, [20, 60], [0, 700], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Shape layers — triangles and arcs
  const shapeRot = frame * 0.06;

  // Arrow up for left side
  const arrowScale = sp(140, { damping: 8, stiffness: 130 });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camZoom}) rotate(${camRotate}deg)`,
          position: 'absolute',
          top: 0,
          left: 0,
        }}
      >
        {/* Gradient */}
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(160deg, #0a1a0a 0%, #1C1C1C 35%, #2D2D2D 65%, #1a0a0a 100%)',
          }}
        />

        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            left: 100,
            top: 300,
            width: 280,
            height: 280,
            opacity: 0.04,
            transform: `rotate(${shapeRot}deg)`,
          }}
        >
          <svg viewBox="0 0 280 280">
            <polygon
              points="140,10 270,270 10,270"
              fill="none"
              stroke="#FFC107"
              strokeWidth="2"
            />
          </svg>
        </div>
        <div
          style={{
            position: 'absolute',
            right: 120,
            top: 120,
            width: 320,
            height: 320,
            opacity: 0.05,
            transform: `rotate(${-shapeRot * 0.8}deg) scale(${1 + Math.sin(frame * 0.01) * 0.03})`,
          }}
        >
          <svg viewBox="0 0 320 320">
            <circle
              cx="160"
              cy="160"
              r="150"
              fill="none"
              stroke="#ff0000"
              strokeWidth="1.5"
              strokeDasharray="15 10"
            />
          </svg>
        </div>

        {/* Motion texture — cross hatching */}
        <AbsoluteFill style={{ opacity: 0.02 }}>
          {Array.from({ length: 12 }, (_, i) => (
            <React.Fragment key={`cross-${i}`}>
              <div
                style={{
                  position: 'absolute',
                  left: -100 + i * 180,
                  top: 0,
                  width: 1,
                  height: '150%',
                  background: '#F5F5F5',
                  transform: 'rotate(35deg)',
                  transformOrigin: 'top left',
                }}
              />
              <div
                style={{
                  position: 'absolute',
                  right: -100 + i * 180,
                  top: 0,
                  width: 1,
                  height: '150%',
                  background: '#F5F5F5',
                  transform: 'rotate(-35deg)',
                  transformOrigin: 'top right',
                }}
              />
            </React.Fragment>
          ))}
        </AbsoluteFill>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={`p4-${i}`}
            style={{
              position: 'absolute',
              left: p.x + p.dx,
              top: p.y + p.dy,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i < 11 ? '#FFC107' : '#ff0000',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* "8 साल बाद" header */}
        <div
          style={{
            position: 'absolute',
            top: '5%',
            left: '50%',
            transform: `translate(-50%, 0) scale(${yearScale})`,
            opacity: yearOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 32,
              fontWeight: 600,
              color: '#9E9E9E',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            AFTER
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 130,
              fontWeight: 900,
              color: '#FFC107',
              lineHeight: 1,
              textShadow: '0 0 50px rgba(255,193,7,0.3)',
            }}
          >
            8 साल
          </div>
        </div>

        {/* Center divider */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '25%',
            width: 3,
            height: dividerH,
            background:
              'linear-gradient(180deg, #FFC107, rgba(255,193,7,0.2), transparent)',
            transform: 'translateX(-50%)',
          }}
        />

        {/* LEFT SIDE — Success person */}
        <div
          style={{
            position: 'absolute',
            left: '8%',
            top: '30%',
            opacity: leftPersonOp,
            transform: `scale(${leftPersonScale})`,
          }}
        >
          {/* Person with glow */}
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div
              style={{
                position: 'absolute',
                left: '50%',
                top: '50%',
                width: 120,
                height: 120,
                borderRadius: '50%',
                background: 'radial-gradient(circle, rgba(255,193,7,0.15) 0%, transparent 70%)',
                transform: 'translate(-50%, -50%)',
              }}
            />
            <svg width="90" height="140" viewBox="0 0 90 140">
              <circle cx="45" cy="20" r="15" fill="none" stroke="#FFC107" strokeWidth="3" />
              {/* Glasses */}
              <circle cx="39" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <circle cx="51" cy="18" r="5" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <line x1="44" y1="18" x2="46" y2="18" stroke="#FFC107" strokeWidth="1" />
              {/* Smile */}
              <path d="M39,24 Q45,30 51,24" fill="none" stroke="#FFC107" strokeWidth="1.5" />
              <line x1="45" y1="35" x2="45" y2="80" stroke="#FFC107" strokeWidth="3" strokeLinecap="round" />
              {/* Arms up (victory) */}
              <line x1="45" y1="50" x2="20" y2="35" stroke="#FFC107" strokeWidth="3" strokeLinecap="round" />
              <line x1="45" y1="50" x2="70" y2="35" stroke="#FFC107" strokeWidth="3" strokeLinecap="round" />
              <line x1="45" y1="80" x2="30" y2="125" stroke="#FFC107" strokeWidth="3" strokeLinecap="round" />
              <line x1="45" y1="80" x2="60" y2="125" stroke="#FFC107" strokeWidth="3" strokeLinecap="round" />
            </svg>
          </div>

          {/* Rising graph */}
          <svg
            width="350"
            height="150"
            viewBox="0 0 350 150"
            style={{ marginTop: 20, marginLeft: -40 }}
          >
            <path
              d={`M10,140 Q80,120 140,100 Q200,75 250,40 Q300,10 340,5`}
              fill="none"
              stroke="#FFC107"
              strokeWidth="3"
              strokeDasharray={500}
              strokeDashoffset={500 - 500 * graphProgress}
              strokeLinecap="round"
            />
            {/* Glow under graph */}
            <path
              d={`M10,140 Q80,120 140,100 Q200,75 250,40 Q300,10 340,5 L340,150 L10,150 Z`}
              fill="url(#greenGrad)"
              opacity={0.15 * graphProgress}
            />
            <defs>
              <linearGradient id="greenGrad" x1="0" y1="0" x2="0" y2="1">
                <stop offset="0%" stopColor="#FFC107" />
                <stop offset="100%" stopColor="transparent" />
              </linearGradient>
            </defs>
            {/* Arrow at end */}
            <polygon
              points="335,0 345,8 330,8"
              fill="#FFC107"
              opacity={graphProgress > 0.9 ? 1 : 0}
              transform={`scale(${arrowScale})`}
              style={{ transformOrigin: '335px 4px' }}
            />
          </svg>
        </div>

        {/* Badges for success person */}
        <div
          style={{
            position: 'absolute',
            left: '8%',
            top: '72%',
          }}
        >
          {badges.map((badge, i) => (
            <div
              key={`badge-${i}`}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 12,
                marginBottom: 12,
                opacity: interpolate(
                  frame,
                  [badge.delay, badge.delay + 20],
                  [0, 1],
                  { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
                ),
                transform: `translateX(${interpolate(
                  frame,
                  [badge.delay, badge.delay + 20],
                  [-40, 0],
                  { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
                )}px)`,
              }}
            >
              <div
                style={{
                  width: 42,
                  height: 42,
                  borderRadius: '50%',
                  backgroundColor: 'rgba(255,193,7,0.15)',
                  border: '2px solid #FFC107',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                <svg width="20" height="20" viewBox="0 0 20 20">
                  <path
                    d="M5,10 L9,14 L15,6"
                    fill="none"
                    stroke="#FFC107"
                    strokeWidth="2.5"
                    strokeLinecap="round"
                  />
                </svg>
              </div>
              <span
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontSize: 32,
                  fontWeight: 600,
                  color: '#F5F5F5',
                }}
              >
                {badge.text}
              </span>
            </div>
          ))}
        </div>

        {/* RIGHT SIDE — Stagnant person */}
        <div
          style={{
            position: 'absolute',
            right: '12%',
            top: '30%',
            opacity: rightPersonOp,
            transform: `scale(${rightPersonScale})`,
            textAlign: 'center',
          }}
        >
          {/* Person slouched */}
          <svg width="90" height="140" viewBox="0 0 90 140">
            <circle cx="45" cy="20" r="15" fill="none" stroke="#9E9E9E" strokeWidth="3" />
            {/* Sad face */}
            <path d="M38,25 Q45,21 52,25" fill="none" stroke="#9E9E9E" strokeWidth="1.5" />
            <circle cx="39" cy="17" r="1.5" fill="#9E9E9E" />
            <circle cx="51" cy="17" r="1.5" fill="#9E9E9E" />
            <line x1="45" y1="35" x2="45" y2="80" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
            {/* Arms drooping */}
            <line x1="45" y1="50" x2="22" y2="70" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
            <line x1="45" y1="50" x2="68" y2="70" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
            <line x1="45" y1="80" x2="30" y2="125" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
            <line x1="45" y1="80" x2="60" y2="125" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
          </svg>

          {/* Flat line graph */}
          <svg
            width="350"
            height="150"
            viewBox="0 0 350 150"
            style={{ marginTop: 20, marginLeft: -40 }}
          >
            <line
              x1="10"
              y1="100"
              x2={10 + 330 * flatProgress}
              y2={100 + Math.sin(frame * 0.05) * 5}
              stroke="#9E9E9E"
              strokeWidth="3"
              strokeLinecap="round"
              strokeDasharray="8 6"
            />
          </svg>
        </div>

        {/* Right side labels */}
        <div
          style={{
            position: 'absolute',
            right: '8%',
            top: '72%',
            textAlign: 'right',
          }}
        >
          {[
            { text: 'वही नौकरी', delay: 130 },
            { text: 'वही तनख्वाह', delay: 145 },
            { text: 'कुछ नहीं बदला', delay: 160 },
          ].map((item, i) => (
            <div
              key={`right-label-${i}`}
              style={{
                marginBottom: 12,
                opacity: interpolate(
                  frame,
                  [item.delay, item.delay + 20],
                  [0, 1],
                  { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
                ),
                transform: `translateX(${interpolate(
                  frame,
                  [item.delay, item.delay + 20],
                  [40, 0],
                  { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
                )}px)`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'flex-end',
                gap: 12,
              }}
            >
              <span
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontSize: 32,
                  fontWeight: 600,
                  color: '#9E9E9E',
                }}
              >
                {item.text}
              </span>
              <div
                style={{
                  width: 42,
                  height: 42,
                  borderRadius: '50%',
                  backgroundColor: 'rgba(255,0,0,0.1)',
                  border: '2px solid #ff0000',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                <svg width="18" height="18" viewBox="0 0 18 18">
                  <line x1="4" y1="4" x2="14" y2="14" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
                  <line x1="14" y1="4" x2="4" y2="14" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
                </svg>
              </div>
            </div>
          ))}
        </div>

        {/* VS text center */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            transform: `translate(-50%, -50%) scale(${sp(90, { damping: 8 })})`,
            opacity: interpolate(frame, [90, 110], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <div
            style={{
              fontFamily: "'Inter', sans-serif",
              fontSize: 60,
              fontWeight: 900,
              color: '#2D2D2D',
              textShadow: '0 0 30px rgba(255,193,7,0.2)',
              border: '3px solid #FFC107',
              borderRadius: '50%',
              width: 100,
              height: 100,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              backgroundColor: 'rgba(255,193,7,0.08)',
            }}
          >
            VS
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.65) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_TwoPathsDiverge;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene5_StuckInPlace',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_StuckInPlace: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow push in
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Dark moody gradient
  const gradAngle = interpolate(frame, [0, durationInFrames], [180, 200], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Particles — dimmer, slower
  const particles = Array.from({ length: 18 }, (_, i) => {
    const s1 = seededRandom(i + 900);
    const s2 = seededRandom(i + 1000);
    return {
      x: s1 * 1920,
      y: s2 * 1080,
      size: 2 + s1 * 2.5,
      dy: Math.sin(frame * 0.025 + i) * 20,
      opacity: 0.06 + s2 * 0.1,
    };
  });

  // Hamster wheel concept
  const wheelRotation = frame * 1.5;

  // Repeating text — same job, same salary loop
  const loopTexts = ['वही नौकरी', 'वही तनख्वाह', 'कुछ नहीं बदला', 'वही नौकरी', 'वही तनख्वाह'];
  const scrollY = interpolate(frame, [0, durationInFrames], [0, -300], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Center stuck person
  const personOp = interpolate(frame, [15, 35], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const personScale = sp(15);

  // Chains SVG
  const chainOp = interpolate(frame, [60, 80], [0, 0.6], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // "कड़वहट" text impact
  const bitterOp = interpolate(frame, [160, 180], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const bitterScale = sp(160, { damping: 6, stiffness: 200 });

  // Screen shake at bitter moment
  const shakeI = interpolate(frame, [165, 175, 195, 205], [0, 5, 5, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const sx = Math.sin(frame * 3) * shakeI;
  const sy = Math.cos(frame * 3.7) * shakeI;

  // Shape layers — broken rectangles
  const shapeRot = frame * 0.07;

  // Red tint pulse
  const redPulse = interpolate(frame, [140, 170, 200, 230], [0, 0.08, 0.08, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `scale(${camZoom}) translate(${sx}px, ${sy}px)`,
          position: 'absolute',
          top: 0,
          left: 0,
        }}
      >
        {/* Dark gradient */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(${gradAngle}deg, #0a0a0a 0%, #1C1C1C 40%, #1a0d0d 70%, #0d0a0a 100%)`,
          }}
        />

        {/* Red tint overlay */}
        <AbsoluteFill
          style={{
            backgroundColor: '#ff0000',
            opacity: redPulse,
          }}
        />

        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            left: 150,
            top: 200,
            width: 250,
            height: 250,
            opacity: 0.04,
            transform: `rotate(${shapeRot}deg)`,
          }}
        >
          <svg viewBox="0 0 250 250">
            <rect
              x="20"
              y="20"
              width="210"
              height="210"
              fill="none"
              stroke="#ff0000"
              strokeWidth="2"
              strokeDasharray="20 15"
            />
          </svg>
        </div>
        <div
          style={{
            position: 'absolute',
            right: 200,
            bottom: 250,
            width: 200,
            height: 200,
            opacity: 0.05,
            transform: `rotate(${-shapeRot * 1.2}deg)`,
          }}
        >
          <svg viewBox="0 0 200 200">
            <circle
              cx="100"
              cy="100"
              r="90"
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="1.5"
              strokeDasharray="12 8"
            />
          </svg>
        </div>

        {/* Motion texture — horizontal scan lines */}
        <AbsoluteFill style={{ opacity: 0.03 }}>
          {Array.from({ length: 40 }, (_, i) => (
            <div
              key={`scanline-${i}`}
              style={{
                position: 'absolute',
                left: 0,
                top: i * 28,
                width: '100%',
                height: 1,
                background: '#F5F5F5',
              }}
            />
          ))}
        </AbsoluteFill>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={`p5-${i}`}
            style={{
              position: 'absolute',
              left: p.x,
              top: p.y + p.dy,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: '#9E9E9E',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Hamster wheel */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '42%',
            transform: `translate(-50%, -50%)`,
            opacity: interpolate(frame, [20, 50], [0, 0.3], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="500" height="500" viewBox="0 0 500 500">
            <circle
              cx="250"
              cy="250"
              r="220"
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="2"
              opacity="0.4"
            />
            {/* Spokes */}
            {Array.from({ length: 12 }, (_, i) => {
              const angle = (i * 30 + wheelRotation) * (Math.PI / 180);
              return (
                <line
                  key={`spoke-${i}`}
                  x1="250"
                  y1="250"
                  x2={250 + 220 * Math.cos(angle)}
                  y2={250 + 220 * Math.sin(angle)}
                  stroke="#9E9E9E"
                  strokeWidth="1"
                  opacity="0.2"
                />
              );
            })}
          </svg>
        </div>

        {/* Central stuck person */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '38%',
            transform: `translate(-50%, -50%) scale(${personScale})`,
            opacity: personOp,
          }}
        >
          <svg width="100" height="150" viewBox="0 0 100 150">
            {/* Dim glow */}
            <circle cx="50" cy="25" r="30" fill="rgba(158,158,158,0.06)" />
            <circle cx="50" cy="25" r="16" fill="none" stroke="#9E9E9E" strokeWidth="3" />
            {/* Sad eyes */}
            <circle cx="44" cy="22" r="1.5" fill="#9E9E9E" />
            <circle cx="56" cy="22" r="1.5" fill="#9E9E9E" />
            {/* Frown */}
            <path d="M43,30 Q50,26 57,30" fill="none" stroke="#9E9E9E" strokeWidth="1.5" />
            {/* Body — walking on wheel */}
            <line x1="50" y1="41" x2="50" y2="85" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
            {/* Arms swinging */}
            <line
              x1="50"
              y1="55"
              x2={50 - 20 + Math.sin(frame * 0.12) * 8}
              y2="72"
              stroke="#9E9E9E"
              strokeWidth="3"
              strokeLinecap="round"
            />
            <line
              x1="50"
              y1="55"
              x2={50 + 20 - Math.sin(frame * 0.12) * 8}
              y2="72"
              stroke="#9E9E9E"
              strokeWidth="3"
              strokeLinecap="round"
            />
            {/* Legs walking */}
            <line
              x1="50"
              y1="85"
              x2={50 - 15 + Math.sin(frame * 0.15) * 10}
              y2="125"
              stroke="#9E9E9E"
              strokeWidth="3"
              strokeLinecap="round"
            />
            <line
              x1="50"
              y1="85"
              x2={50 + 15 - Math.sin(frame * 0.15) * 10}
              y2="125"
              stroke="#9E9E9E"
              strokeWidth="3"
              strokeLinecap="round"
            />
          </svg>
        </div>

        {/* Chain links */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '58%',
            transform: 'translate(-50%, 0)',
            opacity: chainOp,
          }}
        >
          <svg width="200" height="60" viewBox="0 0 200 60">
            {Array.from({ length: 5 }, (_, i) => (
              <React.Fragment key={`chain-${i}`}>
                <ellipse
                  cx={20 + i * 40}
                  cy="30"
                  rx="18"
                  ry="12"
                  fill="none"
                  stroke="#9E9E9E"
                  strokeWidth="3"
                />
              </React.Fragment>
            ))}
          </svg>
        </div>

        {/* Scrolling repeated text — loop feel */}
        <div
          style={{
            position: 'absolute',
            right: '8%',
            top: '25%',
            overflow: 'hidden',
            height: 350,
            width: 400,
            opacity: interpolate(frame, [50, 75], [0, 0.6], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <div style={{ transform: `translateY(${scrollY}px)` }}>
            {[...loopTexts, ...loopTexts].map((text, i) => (
              <div
                key={`loop-${i}`}
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontSize: 38,
                  fontWeight: 600,
                  color: '#9E9E9E',
                  padding: '12px 0',
                  opacity: 0.5 + (i % 3) * 0.15,
                  borderBottom: '1px solid rgba(158,158,158,0.1)',
                }}
              >
                ↻ {text}
              </div>
            ))}
          </div>
        </div>

        {/* "कड़वहट" impact text */}
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            left: '50%',
            transform: `translate(-50%, 0) scale(${bitterScale})`,
            opacity: bitterOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 110,
              fontWeight: 900,
              color: '#ff0000',
              textShadow: '0 0 60px rgba(255,0,0,0.4), 0 0 120px rgba(255,0,0,0.2)',
            }}
          >
            कड़वाहट
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 34,
              fontWeight: 500,
              color: '#9E9E9E',
              marginTop: 8,
            }}
          >
            कुछ नहीं बदलता...
          </div>
        </div>

        {/* Vignette — heavier */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.75) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_StuckInPlace;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene6_TheDifference',
  durationInSeconds: 6,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_TheDifference: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera orbit effect
  const camX = Math.sin(frame * 0.015) * 15;
  const camY = Math.cos(frame * 0.012) * 8;

  // Particles
  const particles = Array.from({ length: 20 }, (_, i) => {
    const s1 = seededRandom(i + 1100);
    const s2 = seededRandom(i + 1200);
    return {
      x: s1 * 1920,
      y: s2 * 1080,
      size: 2 + s1 * 3,
      dy: Math.sin(frame * 0.04 + i * 0.7) * 22,
      opacity: 0.08 + s2 * 0.15,
    };
  });

  // Strikethrough animations
  const strike1Progress = interpolate(frame, [20, 50], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const strike2Progress = interpolate(frame, [40, 70], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // "NOT" X marks
  const xMark1 = sp(25, { damping: 8, stiffness: 180 });
  const xMark2 = sp(45, { damping: 8, stiffness: 180 });

  // Reveal "शाम के 4-5 घंटे"
  const revealOp = interpolate(frame, [80, 105], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const revealScale = sp(80, { damping: 10, stiffness: 130 });
  const revealY = interpolate(frame, [80, 105], [50, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Glow pulse behind reveal
  const glowPulse = 0.3 + Math.sin(frame * 0.1) * 0.15;

  // Shape layers — pentagon and line burst
  const shapeRot = frame * 0.09;

  // Screen shake on reveal
  const revealShake = interpolate(frame, [82, 88, 100, 108], [0, 4, 4, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const rsx = Math.sin(frame * 4) * revealShake;
  const rsy = Math.cos(frame * 5) * revealShake;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `translate(${camX + rsx}px, ${camY + rsy}px)`,
          position: 'absolute',
          top: 0,
          left: 0,
        }}
      >
        {/* Gradient */}
        <AbsoluteFill
          style={{
            background:
              'linear-gradient(140deg, #1C1C1C 0%, #0d1a0d 30%, #1C1C1C 60%, #1a1a0d 100%)',
          }}
        />

        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            left: '70%',
            top: '15%',
            width: 300,
            height: 300,
            opacity: 0.04,
            transform: `rotate(${shapeRot}deg)`,
          }}
        >
          <svg viewBox="0 0 300 300">
            <polygon
              points="150,15 280,105 230,280 70,280 20,105"
              fill="none"
              stroke="#FFC107"
              strokeWidth="2"
            />
          </svg>
        </div>
        <div
          style={{
            position: 'absolute',
            left: '5%',
            bottom: '10%',
            width: 200,
            height: 200,
            opacity: 0.06,
            transform: `rotate(${-shapeRot}deg) scale(${1 + Math.sin(frame * 0.015) * 0.04})`,
          }}
        >
          <svg viewBox="0 0 200 200">
            {/* Star burst lines */}
            {Array.from({ length: 8 }, (_, i) => {
              const a = (i * 45 * Math.PI) / 180;
              return (
                <line
                  key={`burst-${i}`}
                  x1="100"
                  y1="100"
                  x2={100 + 90 * Math.cos(a)}
                  y2={100 + 90 * Math.sin(a)}
                  stroke="#e9d700"
                  strokeWidth="1.5"
                />
              );
            })}
          </svg>
        </div>

        {/* Motion texture — radial dots */}
        <AbsoluteFill style={{ opacity: 0.025 }}>
          {Array.from({ length: 24 }, (_, i) => {
            const angle = (i * 15 * Math.PI) / 180;
            const radius = 300 + Math.sin(frame * 0.02 + i) * 20;
            return (
              <div
                key={`rdot-${i}`}
                style={{
                  position: 'absolute',
                  left: 960 + radius * Math.cos(angle),
                  top: 540 + radius * Math.sin(angle),
                  width: 4,
                  height: 4,
                  borderRadius: '50%',
                  backgroundColor: '#F5F5F5',
                }}
              />
            );
          })}
        </AbsoluteFill>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={`p6-${i}`}
            style={{
              position: 'absolute',
              left: p.x,
              top: p.y + p.dy,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: '#FFC107',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* NOT प्रतिभा */}
        <div
          style={{
            position: 'absolute',
            left: '15%',
            top: '22%',
            opacity: interpolate(frame, [5, 25], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
            transform: `scale(${sp(5)})`,
          }}
        >
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 72,
                fontWeight: 800,
                color: '#9E9E9E',
              }}
            >
              प्रतिभा
            </div>
            {/* Strikethrough */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: `${strike1Progress * 100}%`,
                height: 5,
                backgroundColor: '#ff0000',
                borderRadius: 3,
              }}
            />
          </div>
          {/* X mark */}
          <div
            style={{
              position: 'absolute',
              right: -60,
              top: '25%',
              transform: `scale(${xMark1})`,
            }}
          >
            <svg width="50" height="50" viewBox="0 0 50 50">
              <line x1="10" y1="10" x2="40" y2="40" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
              <line x1="40" y1="10" x2="10" y2="40" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
            </svg>
          </div>
        </div>

        {/* NOT किस्मत */}
        <div
          style={{
            position: 'absolute',
            right: '15%',
            top: '22%',
            opacity: interpolate(frame, [25, 45], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
            transform: `scale(${sp(25)})`,
          }}
        >
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 72,
                fontWeight: 800,
                color: '#9E9E9E',
              }}
            >
              किस्मत
            </div>
            {/* Strikethrough */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: `${strike2Progress * 100}%`,
                height: 5,
                backgroundColor: '#ff0000',
                borderRadius: 3,
              }}
            />
          </div>
          <div
            style={{
              position: 'absolute',
              right: -60,
              top: '25%',
              transform: `scale(${xMark2})`,
            }}
          >
            <svg width="50" height="50" viewBox="0 0 50 50">
              <line x1="10" y1="10" x2="40" y2="40" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
              <line x1="40" y1="10" x2="10" y2="40" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
            </svg>
          </div>
        </div>

        {/* Glow behind reveal */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '60%',
            width: 800,
            height: 300,
            borderRadius: '50%',
            background: 'radial-gradient(circle, rgba(255,193,7,0.12) 0%, transparent 70%)',
            transform: 'translate(-50%, -50%)',
            opacity: glowPulse * (revealOp),
          }}
        />

        {/* REVEAL — "शाम के 4-5 घंटे" */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '52%',
            transform: `translate(-50%, ${revealY}px) scale(${revealScale})`,
            opacity: revealOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 48,
              fontWeight: 600,
              color: '#9E9E9E',
              marginBottom: 10,
            }}
          >
            ये फर्क है...
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 140,
              fontWeight: 900,
              color: '#FFC107',
              textShadow: '0 0 80px rgba(255,193,7,0.4), 0 4px 30px rgba(0,0,0,0.5)',
              lineHeight: 1.1,
            }}
          >
            शाम के 4-5
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 120,
              fontWeight: 900,
              color: '#e9d700',
              textShadow: '0 0 60px rgba(233,215,0,0.3)',
            }}
          >
            घंटों का
          </div>
        </div>

        {/* Stick figure — Buffett nodding */}
        <div
          style={{
            position: 'absolute',
            right: '5%',
            bottom: '15%',
            transform: `translateY(${Math.sin(frame * 0.05) * 2}px) scale(${sp(50)})`,
            opacity: interpolate(frame, [50, 70], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="70" height="110" viewBox="0 0 70 110">
            <circle cx="35" cy="18" r="22" fill="rgba(255,193,7,0.08)" />
            <circle cx="35" cy="18" r="13" fill="none" stroke="#F5F5F5" strokeWidth="2.5" />
            <circle cx="30" cy="16" r="4" fill="none" stroke="#FFC107" strokeWidth="1.2" />
            <circle cx="40" cy="16" r="4" fill="none" stroke="#FFC107" strokeWidth="1.2" />
            <line x1="34" y1="16" x2="36" y2="16" stroke="#FFC107" strokeWidth="0.8" />
            {/* Nod */}
            <g transform={`rotate(${Math.sin(frame * 0.04) * 3}, 35, 18)`}>
              <line x1="35" y1="31" x2="35" y2="68" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
            </g>
            <line x1="35" y1="45" x2="15" y2="58" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="35" y1="45" x2="55" y2="58" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="35" y1="68" x2="22" y2="100" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
            <line x1="35" y1="68" x2="48" y2="100" stroke="#F5F5F5" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_TheDifference;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  spring,
  AbsoluteFill,
  Easing,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene7_WhereDidEveningGo',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_WhereDidEveningGo: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
  );

  // Camera slow drift right
  const camX = interpolate(frame, [0, durationInFrames], [15, -25], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Background — sunset to dark gradient animation
  const skyProgress = interpolate(frame, [0, durationInFrames], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const skyColor1 = `hsl(${30 - skyProgress * 30}, ${60 - skyProgress * 40}%, ${20 - skyProgress * 12}%)`;
  const skyColor2 = '#1C1C1C';

  // Particles
  const particles = Array.from({ length: 24 }, (_, i) => {
    const s1 = seededRandom(i + 1300);
    const s2 = seededRandom(i + 1400);
    return {
      x: s1 * 1920,
      y: s2 * 1080,
      size: 2 + s1 * 3,
      dy: Math.sin(frame * 0.035 + i * 0.6) * 28,
      dx: Math.cos(frame * 0.025 + i) * 12,
      opacity: 0.1 + s2 * 0.15,
    };
  });

  // Water drain concept — time flowing down
  const drainParticles = Array.from({ length: 15 }, (_, i) => {
    const s = seededRandom(i + 1500);
    const startFrame = 40 + i * 6;
    const yPos = interpolate(frame, [startFrame, startFrame + 120], [-20, 1200], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });
    const xBase = 860 + s * 200;
    const xWobble = Math.sin(frame * 0.06 + i * 2) * 15;
    const op = interpolate(frame, [startFrame, startFrame + 15, startFrame + 100, startFrame + 120], [0, 0.6, 0.6, 0], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });
    return { x: xBase + xWobble, y: yPos, opacity: op, size: 6 + s * 8 };
  });

  // Hourglass
  const hourglassScale = sp(15, { damping: 10, stiffness: 110 });
  const sandLevel = interpolate(frame, [30, durationInFrames - 30], [0.9, 0.15], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Phone icon entrance
  const phoneScale = sp(80, { damping: 10 });
  const phoneOp = interpolate(frame, [80, 100], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Sofa icon entrance
  const sofaScale = sp(100, { damping: 10 });
  const sofaOp = interpolate(frame, [100, 120], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Main question text
  const questionOp = interpolate(frame, [180, 210], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });
  const questionScale = sp(180, { damping: 8, stiffness: 160 });

  // Shape layers — curved lines
  const curveShift = frame * 0.08;

  // Pulsing question mark
  const qPulse = 1 + Math.sin(frame * 0.12) * 0.06;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#1C1C1C',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          transform: `translateX(${camX}px)`,
          position: 'absolute',
          top: 0,
          left: 0,
        }}
      >
        {/* Gradient — sunset fading to dark */}
        <AbsoluteFill
          style={{
            background: `linear-gradient(180deg, ${skyColor1} 0%, #1C1C1C 50%, #0a0a0a 100%)`,
          }}
        />

        {/* Shape layers — flowing curves */}
        <AbsoluteFill style={{ opacity: 0.04 }}>
          <svg width="1920" height="1080" viewBox="0 0 1920 1080">
            <path
              d={`M0,${400 + Math.sin(curveShift) * 30} Q480,${300 + Math.cos(curveShift) * 40} 960,${400 + Math.sin(curveShift * 1.2) * 35} Q1440,${500 + Math.cos(curveShift * 0.8) * 30} 1920,${400 + Math.sin(curveShift * 1.5) * 25}`}
              fill="none"
              stroke="#FFC107"
              strokeWidth="2"
            />
            <path
              d={`M0,${600 + Math.cos(curveShift * 0.7) * 25} Q480,${700 + Math.sin(curveShift * 1.1) * 30} 960,${600 + Math.cos(curveShift) * 35} Q1440,${500 + Math.sin(curveShift * 0.9) * 28} 1920,${600 + Math.cos(curveShift * 1.3) * 20}`}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1.5"
            />
          </svg>
        </AbsoluteFill>

        {/* Particles */}
        {particles.map((p, i) => (
          <div
            key={`p7-${i}`}
            style={{
              position: 'absolute',
              left: p.x + p.dx,
              top: p.y + p.dy,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#FFC107' : '#9E9E9E',
              opacity: p.opacity,
            }}
          />
        ))}

        {/* Drain particles — time flowing away */}
        {drainParticles.map((dp, i) => (
          <div
            key={`drain-${i}`}
            style={{
              position: 'absolute',
              left: dp.x,
              top: dp.y,
              width: dp.size,
              height: dp.size,
              borderRadius: '50%',
              border: '1.5px solid #FFC107',
              opacity: dp.opacity,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: dp.size * 0.5,
              color: '#FFC107',
              fontWeight: 600,
            }}
          >
            {i % 3 === 0 ? 'h' : ''}
          </div>
        ))}

        {/* Hourglass */}
        <div
          style={{
            position: 'absolute',
            left: '15%',
            top: '20%',
            transform: `scale(${hourglassScale})`,
            opacity: interpolate(frame, [15, 40], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="160" height="250" viewBox="0 0 160 250">
            {/* Top glass */}
            <path
              d="M30,10 L130,10 L90,120 L70,120 Z"
              fill="none"
              stroke="#FFC107"
              strokeWidth="3"
            />
            {/* Bottom glass */}
            <path
              d="M30,240 L130,240 L90,130 L70,130 Z"
              fill="none"
              stroke="#FFC107"
              strokeWidth="3"
            />
            {/* Sand top — decreasing */}
            <path
              d={`M${40 + (1 - sandLevel) * 15},${10 + (1 - sandLevel) * 60} L${120 - (1 - sandLevel) * 15},${10 + (1 - sandLevel) * 60} L90,120 L70,120 Z`}
              fill="rgba(255,193,7,0.3)"
            />
            {/* Sand bottom — increasing */}
            <path
              d={`M${40 + sandLevel * 15},${240 - sandLevel * 60} L${120 - sandLevel * 15},${240 - sandLevel * 60} L130,240 L30,240 Z`}
              fill="rgba(255,193,7,0.4)"
            />
            {/* Sand stream */}
            <line
              x1="80"
              y1="120"
              x2="80"
              y2="130"
              stroke="#FFC107"
              strokeWidth="2"
              opacity={0.5 + Math.sin(frame * 0.2) * 0.3}
            />
            {/* Frame */}
            <line x1="25" y1="8" x2="135" y2="8" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
            <line x1="25" y1="242" x2="135" y2="242" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          </svg>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 28,
              fontWeight: 600,
              color: '#9E9E9E',
              textAlign: 'center',
              marginTop: 10,
            }}
          >
            बहता समय
          </div>
        </div>

        {/* "मूल्यहीन" tag */}
        <div
          style={{
            position: 'absolute',
            left: '45%',
            top: '18%',
            opacity: interpolate(frame, [50, 70], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
            transform: `scale(${sp(50)})`,
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 90,
              fontWeight: 900,
              color: '#F5F5F5',
              textShadow: '0 2px 20px rgba(0,0,0,0.5)',
            }}
          >
            बहा देते हैं
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 40,
              fontWeight: 500,
              color: '#ff0000',
              marginTop: 5,
            }}
          >
            जैसे कोई मूल्यहीन चीज़ हो
          </div>
        </div>

        {/* Phone icon */}
        <div
          style={{
            position: 'absolute',
            right: '22%',
            top: '42%',
            opacity: phoneOp,
            transform: `scale(${phoneScale})`,
          }}
        >
          <svg width="80" height="120" viewBox="0 0 80 120">
            <rect x="10" y="5" width="60" height="110" rx="12" fill="none" stroke="#9E9E9E" strokeWidth="3" />
            <rect x="20" y="18" width="40" height="70" fill="rgba(158,158,158,0.1)" />
            <circle cx="40" cy="100" r="5" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            {/* Scroll notification dots */}
            {[28, 45, 62].map((y, idx) => (
              <rect
                key={`notif-${idx}`}
                x="25"
                y={y}
                width="30"
                height={4}
                rx="2"
                fill="#9E9E9E"
                opacity={0.3 + Math.sin(frame * 0.1 + idx) * 0.2}
              />
            ))}
          </svg>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 22,
              color: '#9E9E9E',
              textAlign: 'center',
              marginTop: 8,
            }}
          >
            स्क्रॉलिंग
          </div>
        </div>

        {/* Sofa icon */}
        <div
          style={{
            position: 'absolute',
            right: '8%',
            top: '42%',
            opacity: sofaOp,
            transform: `scale(${sofaScale})`,
          }}
        >
          <svg width="100" height="80" viewBox="0 0 100 80">
            <path
              d="M10,60 L10,30 Q10,15 25,15 L75,15 Q90,15 90,30 L90,60"
              fill="none"
              stroke="#9E9E9E"
              strokeWidth="3"
            />
            <line x1="5" y1="60" x2="95" y2="60" stroke="#9E9E9E" strokeWidth="3" />
            <line x1="10" y1="60" x2="10" y2="72" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
            <line x1="90" y1="60" x2="90" y2="72" stroke="#9E9E9E" strokeWidth="3" strokeLinecap="round" />
            {/* Armrests */}
            <path d="M5,35 Q0,35 0,45 L0,60 L10,60 L10,35 Z" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <path d="M95,35 Q100,35 100,45 L100,60 L90,60 L90,35 Z" fill="none" stroke="#9E9E9E" strokeWidth="2" />
          </svg>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 22,
              color: '#9E9E9E',
              textAlign: 'center',
              marginTop: 8,
            }}
          >
            सोफ़ा
          </div>
        </div>

        {/* Question — "कल की शाम कहां गई?" */}
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            left: '50%',
            transform: `translate(-50%, 0) scale(${questionScale * qPulse})`,
            opacity: questionOp,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 100,
              fontWeight: 900,
              color: '#FFC107',
              textShadow: '0 0 50px rgba(255,193,7,0.35)',
            }}
          >
            कल की शाम
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', sans-serif",
              fontSize: 110,
              fontWeight: 900,
              color: '#F5F5F5',
              textShadow: '0 0 40px rgba(245,245,245,0.15)',
            }}
          >
            कहाँ गई?
          </div>
        </div>

        {/* Stick figure on sofa — lazy pose */}
        <div
          style={{
            position: 'absolute',
            left: '52%',
            top: '52%',
            transform: `translateY(${Math.sin(frame * 0.05) * 1.5}px) scale(${sp(65)})`,
            opacity: interpolate(frame, [65, 85], [0, 0.7], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="100" height="80" viewBox="0 0 100 80">
            {/* Lying person */}
            <circle cx="20" cy="25" r="10" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <line x1="30" y1="28" x2="70" y2="35" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            <line x1="50" y1="32" x2="45" y2="50" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            <line x1="70" y1="35" x2="80" y2="50" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            <line x1="70" y1="35" x2="75" y2="55" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
            {/* Phone in hand */}
            <rect x="38" y="47" width="12" height="18" rx="2" fill="none" stroke="#9E9E9E" strokeWidth="1.5" />
          </svg>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background:
              'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.7) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: "'Inter', sans-serif",
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
          }}
        >
          ProBuffet HQ
        </div>
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_WhereDidEveningGo;
```

